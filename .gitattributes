# DECISIONS_LOG — append-only журнал (L-5): параллельные PR аппендят в хвост,
# обычный merge конфликтует на каждой паре. union сохраняет обе стороны автоматически.
# ВАЖНО: дисциплина append-only обязательна — правки в СЕРЕДИНЕ файла union мержит молча.
# IMPL_DEBT union НЕ давать: там правки in-place, конфликт должен видеть человек.
DECISIONS_LOG.md merge=union

# Канон дизайна фермерской зоны — реестр правил R-N append-only в хвост таблицы:
# параллельные дизайн-PR конфликтовали на каждой новой строке. union убирает конфликт.
# Побочка: union может молча слить ДВА одинаковых R-N (было: R-24 дважды) — от этого
# страхует CHECK 10 в cross_check.sh (падает на дублях R-N → назначь следующий свободный).
# §1–6 правятся редко и обычно аппендом буллета; при правке ОДНОГО буллета из двух PR
# union их склеит — это ловится ревью diff'а канона.
Docs/AGOS-DesignRules-FarmerCabinet.md merge=union

# IMPLEMENTATION DEBT — AgOS (code ≠ canon)

> Source: DOC_DRIFT_AUDIT-2026-06-22.md (adversarially verified). Generated as part of Doc-Reconciliation Phase 1.
> These are CODE gaps. Phase 1 (docs) does not fix them. Each is a Phase-2 work item; keep the audit id when actioned.
> Authority model: microsteps = canon (Identity/Membership/Governance/TSP); Doks = canon elsewhere; code = reality.
> Article-171 / data-isolation leak VET-02 is being fixed out-of-band (spawned task task_bf722ce0) — see note below.

> **🚀 ДЕПЛОЙ 2026-07-07 — первый полный успешный прогон `deploy_sql.py` (staging `mwtbozflyldcadypherr`).**
> До этой сессии полный реплей канона (d01→d02→d03→d04→d05→d07→d08→d09→d11) НИКОГДА не проходил
> целиком без ошибок — деплой останавливался на первой не-идемпотентной команде и никто не доходил
> до конца, чтобы увидеть следующую. Прогон вручную (пароль БД предоставлен CEO) вскрыл и починил
> систематический класс + 8 отдельных настоящих багов, независимых от задач этой сессии:
>
> **Систематический класс (идемпотентность, ~320 правок, все аддитивные/безопасные):**
> - `CREATE INDEX`/`CREATE UNIQUE INDEX` без `IF NOT EXISTS` — 231 случай (d01/d02/d03/d04/d05).
> - `CREATE POLICY` без предшествующего `DROP POLICY IF EXISTS <name> ON <table>` — 193 случая
>   (d01/d02/d03/d04/d05/d09/d10, включая `storage.objects`).
> - `CREATE TRIGGER` без `DROP TRIGGER IF EXISTS` — 74 случая (d01/d02/d03/d04/d05/d08).
> - `CREATE OR REPLACE FUNCTION`, меняющая параметры с defaults — PostgreSQL запрещает убирать
>   default через `CREATE OR REPLACE` (требует `DROP FUNCTION` первым) — 96 функций получили
>   `drop function if exists` guard (d01/d02/d03/d04/d05/d07/d09/d11), включая
>   `claim_pending_notifications`.
> - Seed-инсерты без `ON CONFLICT` — 29 случаев в d05 (`phase_templates` ×15, `task_templates` ×13,
>   `kpi_templates` ×1) — первый успешный прогон УЖЕ закоммитил эти строки; повторный прогон без
>   ON CONFLICT давал duplicate key. `phase_templates` — `DO UPDATE ... RETURNING INTO` (переменные
>   используются дочерними INSERT); `task_templates`/`kpi_templates` — `DO NOTHING` (без RETURNING).
>
> **Настоящие баги (не идемпотентность — логические/синтаксические ошибки файлов):**
>
> | что | файл | суть | фикс |
> |---|---|---|---|
> | RLS `ecm_read`/`ecm_write_org` | d01_kernel.sql:~5800 | ссылались на `memberships.user_id`/`.is_active`/`.role` — таких колонок нет (`memberships` = членство ОРГАНИЗАЦИИ, не user↔org линк) | `ecm_read` → `fn_my_org_ids()`; `ecm_write_org` → `user_organization_roles.role in ('owner','manager')` |
> | `rpc_get_user_phone` | d07_ai_gateway.sql:2969 | тот же баг: `join memberships m on m.user_id=u.id` + `m.status='active'` (нет ни `user_id`, ни `status` на `memberships`) | `join user_organization_roles uor on uor.user_id=u.id` |
> | Заголовки файлов | d02/d03/d04/d05 | пропущен `--` в многострочном комментарии-описании файла → строка воспринималась как SQL, синтаксис-ошибка | добавлен `--` |
> | `ration_versions` constraint | d03_feed.sql:2076 | `add constraint if not exists` — невалидный синтаксис PostgreSQL (в отличие от index/policy, у constraint нет такой клаузы) | `drop constraint if exists` + `add constraint` |
> | `idx_fcn_valid` | d03_feed.sql:1635 | partial index `where valid_to is null or valid_to > current_date` — `current_date` не IMMUTABLE, недопустим в предикате индекса | предикат сужен до `where valid_to is null` |
> | `farm_phases.sort_order` | d05_ops_edu.sql | колонка используется в индексе, но не объявлена в CREATE TABLE — таблица уже жила без неё | `alter table ... add column if not exists sort_order int not null default 0` |
> | `sop_documents.domain/source/sort_order` | d05_ops_edu.sql | три колонки используются в seed INSERT, не объявлены в CREATE TABLE | три `add column if not exists` |
> | `fn_preview_cascade` CTE | d05_ops_edu.sql:~2998 | `cascade_preview` — рекурсивный CTE (`union all` на себя), но объявлен без `RECURSIVE` → Postgres отклонял forward-reference | `with` → `with recursive` |
> | `consulting_reference_data` CHECK | d09_consulting.sql:~798 | промежуточное пересборка constraint не включала `'livestock_prices'` (добавлена позже), а живые данные её уже содержали → ADD CONSTRAINT падал на существующих строках | список категорий синхронизирован с более поздним блоком |
> | `rpc_name_registry` insert | d11_norms.sql:156 | INSERT использовал устаревшие колонки `rpc_id/canonical_name/alias_of/source_file/description` — таблица давно имеет `sql_name/dok3_name/dok5_tool_name/created_in/notes` (+ trigger-derived `supabase_call`) | переписан на актуальную схему, `on conflict (sql_name) do update` |
>
> **Итог:** полный канон (d01,d02,d03,d04,d05,d07,d08,d09,d11) + `d10_public_site.sql` (не входит в
> `deploy_sql.py`, DEPLOY-PIPE-01) + миграции `20260701150000_tsp_district_sort.sql`,
> `20260706120000_tsp_batch_reviews.sql` — все применены к `mwtbozflyldcadypherr` без ошибок.
> `cross_check.sh` 0 critical на каждом шаге.
>
> **✅ ДОЗАКРЫТО 2026-07-07 (продолжение сессии):** все 22 оставшихся `supabase/migrations/*.sql`
> просканированы на тот же класс идемпотентности (CREATE INDEX/POLICY/TRIGGER без guard, seed-инсерты
> без ON CONFLICT) — **0 находок**, миграции уже были чисты (в отличие от канонических d0x-файлов).
> TSP-FLOW-08/09 задеплоены точечно: извлечены и применены 3 конкретных стейтмента из
> `20260622120000_tsp_canonical_rebind.sql` (`fn_tsp_region_id`, `rpc_create_batch` text-sig +
> comment/revoke/grant, `rpc_self_activate_pool_request` + comment/revoke/grant) — НЕ реплей всего
> 1830-строчного файла целиком (не в скоупе точечного фикса, риск чужого дрейфа). Верифицировано
> прямым запросом к `pg_proc.prosrc` на `mwtbozflyldcadypherr`: все 4 признака фикса присутствуют.
> См. записи TSP-FLOW-08/09 ниже.

> **🔄 RECONCILIATION 2026-06-24 (TSP M6-A, E2E write-path verified vs prod `mwtbozflyldcadypherr`, rollback-tx):**
> The 2026-06-22 audit is STALE for several TSP rows — Slice C (2026-06-23) closed them. Verified-closed by live E2E:
> **TSP-FLOW-01** (published→offering reachable ✅), **TSP-SCHEMA-02** (offer acceptable from offering ✅),
> **TSP-FLOW-05** (reveal fires on adapter close: `mpk_contact_revealed_at` set by `rpc_self_accept_offer`/`rpc_self_auto_match_batch`, surfaced via `fn_tsp_batch_json` ✅),
> **MARKET-UI-02** (`status.ts` already on 11-state FSM ✅). **TSP-FLOW-03** = PARTIAL (ready_from/ready_to + farmer_price_per_kg ARE stored/read; only forward-contract pieces remain).
> NEW defect **TSP-FLOW-07** added below. Full proof: DECISIONS_LOG 2026-06-24 + `apex-brain/.../tsp-farmer-sell-flow.md`. Epic: Linear ARS-94.
>
> | id | domain | conflict | what | evidence | action |
> |----|--------|----------|------|----------|--------|
> | TSP-FLOW-07 | TSP-flow | code↔canon | canon `rpc_accept_offer` auto-close does NOT set `mpk_contact_revealed_at`; pools closed via canon path never reveal buyer to farmer (adapter accept/auto-match DO set it) | d02_tsp.sql `rpc_accept_offer` auto-close block; verified live 2026-06-24 (reveal=`<null>` after canon accept→confirmed) | **✅ FIXED 2026-06-24 (ARS-95)** — added `mpk_contact_revealed_at = coalesce(mpk_contact_revealed_at, now())` to canon auto-close UPDATE (`d02_tsp.sql:2849`, additive/P7-safe). Verified rollback-tx: `FIX_VERIFIED buyer=Админ`. cross_check 0 critical. **✅ DEPLOYED 2026-06-24** prod `mwtbozflyldcadypherr` (apply_migration `tsp_flow_07_canon_accept_offer_reveal`); post-deploy verified `POSTDEPLOY_PASS buyer=Админ`, 1 overload (no PGRST203). Repo commit `11613a1` pending merge to main. |
> | TSP-FLOW-08 | TSP-flow | code↔canon | (DEFECT-B) self-serve `rpc_self_activate_pool_request` only created pool+pool_lines — it never wrote `pool_regions` nor swept already-`published` batches. Self-serve pools thus only matched batches created AFTER pool activation (batch-initiated `rpc_self_auto_match_batch`); a pool created after a batch was published would silently never pick it up | `supabase/migrations/20260622120000_tsp_canonical_rebind.sql` `rpc_self_activate_pool_request` (pre-fix: only pools+pool_lines inserts) | **✅ FIXED 2026-06-25** — (a) transfer `pool_requests.region_id`→`pool_regions` (region_type='oblast'; NULL/«все области» writes no row, intentional per NOT NULL schema); (b) added a FIFO sweep over `published` batches mirroring `rpc_self_auto_match_batch` (direct match bid≥ask→matched/auto-close→confirmed, else broadcast offer→offering), pool-initiated so it does NOT gate batch owner. **Strict grade equality `=` kept** (per user decision). Returns `sweptMatched`/`sweptOffered`. **✅ DEPLOYED + VERIFIED 2026-07-07** — точечно применён к `mwtbozflyldcadypherr` (только эта функция + comment/revoke/grant, без реплея всего файла); `pg_proc.prosrc` подтверждает `pool_regions` и `sweptMatched` в теле. |
> | TSP-FLOW-09 | TSP-flow | code↔canon | (DEFECT-A) `fn_tsp_region_id` resolves district free-text via `regions.name_ru` only; no active rayon rows exist, so wizard districts (e.g. «Сайрамский район») → NULL → ALL batches stored with `region_id=NULL`, breaking canon hard `pool_regions` EXISTS region targeting (harmless to self-serve soft predicate) | `…_tsp_canonical_rebind.sql` `fn_tsp_region_id` + `rpc_create_batch` (`v_region_id` always NULL for rayon input) | **✅ FIXED 2026-06-25** — (a) `fn_tsp_region_id` normalizes input (strips «район/город/г./область/обл.», collapses spaces) for robust name match; (b) `rpc_create_batch` falls back to the farmer org's oblast `organizations.region_id` when district doesn't resolve — every batch now gets an oblast-level region_id (the granularity MPK pools target). Rayon precision deferred (would require seeding rayon rows + parent_id into `public.regions`). **✅ DEPLOYED + VERIFIED 2026-07-07** — точечно применены `fn_tsp_region_id` + `rpc_create_batch` (text-sig) к `mwtbozflyldcadypherr`; `pg_proc.prosrc` подтверждает нормализацию «район» и фолбэк на `organizations.region_id`. |

> **🧪 QA-ПРОГОН 2026-07-05 (backend-слой qa/scenarios/, staging `mwtbozflyldcadypherr`, REST + anon/JWT, эмпирически):**
> Прогон реестра тест-кейсов (`qa/`) нашёл новые расхождения код↔канон, НЕ зарегистрированные ранее. Два — **critical (эмпирика, не статика)**.
> Полные отчёты: `qa/runs/undated-0{5,6,7,8}-*-backend.md`. Кейсы этих находок переведены в `blocked:<id>` до фикса.
>
> | id | domain | severity | conflict | what | evidence | action |
> |----|--------|----------|----------|------|----------|--------|
> | SEC-STORAGE-01 | security/storage | 🔴 critical | code↔canon (Data Isolation, P-AI-2) | bucket `membership-documents` (private) НЕ изолирован по orgId: любой аутентиф. пользователь листингует папки всех org и **скачивает** чужие банковские/идент. документы | эмпирика: QA-юзер без членства → листинг корня + чужой `bank_details.png` HTTP 200 (476 КБ). qa/runs/undated-07-security-cross-backend.md SEC-RLS-03 | Storage-политика на bucket: SELECT/download только если `(storage.foldername(name))[1]::uuid = any(fn_my_org_ids())` или админ. Прогнать SEC-RLS-03 до PASS. **✅ FIXED + DEPLOYED + VERIFIED 2026-07-06 (ветка `claude/epic-lovelace-14b503`, канонический фикс)** — `fn_storage_org_id()` + `membership_documents_{select,insert,update}_org` в `d10_public_site.sql`, **применено напрямую к `mwtbozflyldcadypherr`** (psycopg2 DDL, не через `deploy_sql.py` — DEPLOY-PIPE-01 остаётся отдельным долгом для файла целиком) и эмпирически верифицировано (`SET LOCAL ROLE authenticated` + синтетический JWT): атакующий без членства 31/31→0/31, admin 31/31 (не задет), реальный член org — ровно свои файлы, без регрессии. Подробности: `d10_public_site.sql` §7, `IMPL_DEBT.md`/`DECISIONS_LOG.md` на `claude/epic-lovelace-14b503`, `qa/scenarios/07-security-cross.md` SEC-RLS-03→`active` (этот воркетри). **✅ КОНСОЛИДИРОВАНО 2026-07-06 (этот воркетри, `claude/confident-napier-599473`):** код-фикс `d10_public_site.sql` перенесён из `claude/epic-lovelace-14b503` в этот branch (git apply, файлы были байт-идентичны на общем предке `d570d38`) — теперь живёт в git, а не только на живой БД. Параллельная избыточная попытка на `claude/trusting-visvesvaraya-8d48aa` (`fn_safe_uuid()`) **отброшена, не применена** — по решению CEO 2026-07-06 (см. AskUserQuestion в сессии): её DDL никогда не должен попасть в БД поверх этого фикса. |
> | SEC-RPC-ORGTRUST-01 | security/TSP-rpc | 🔴 critical | code↔canon (RLS mandatory) | SECURITY DEFINER self-RPC (`rpc_cancel_batch`/`rpc_create_batch`/`rpc_publish_batch`/`rpc_lower_batch_price`/`rpc_get_org_batches`) доверяют `p_organization_id` и не сверяют с `fn_my_org_ids()` → кросс-org мутация любым аутентиф. юзером; табличный RLS DEFINER обходит | эмпирика: юзер A вызвал `rpc_cancel_batch(org_B,batch_B)`→true, партия B отменена. SEC-RLS-01/SEC-GATE-01 | Guard в каждом self-RPC: `if not (p_organization_id = any(fn_my_org_ids()) or fn_is_admin()) then raise 'FORBIDDEN'`. Аддитивно (P7), сигнатуры не менять. Аналог фикса VET-02. **✅ FIXED 2026-07-06, КОНСОЛИДИРОВАНО в `claude/confident-napier-599473`** — guard добавлен во все 5 RPC (`rpc_cancel_batch`/`rpc_lower_batch_price` в d02; `rpc_get_org_batches`/`rpc_create_batch`/`rpc_publish_batch` в d07) + `or auth.role() = 'service_role'` bypass, проверено по коду `ai_gateway/config.py` — Gateway ходит через `supabase-py create_client(service_role_key)` → PostgREST → `auth.role()='service_role'`, обход корректен, AI Gateway не ломается. Канонический фикс взят из ветки `claude/trusting-visvesvaraya-8d48aa` (git apply в этот worktree, файлы были байт-идентичны). **Параллельная попытка `claude/bold-payne-93aa8d`** независимо чинила то же самое иначе (guard только для 2 d02-функций + `revoke execute from authenticated` вместо guard для 3 d07-функций, рассудив, что guard сломает AI Gateway) — технически это неверное обоснование (см. выше про service_role bypass), но её `revoke ... from authenticated` для d07-сигнатур **дополнительно взят как второй слой защиты**: проверено по коду, что живой self-serve `rpc_create_batch` (визард фермера) зовёт ДРУГУЮ перегрузку без `p_organization_id` вовсе (безопасна по конструкции), а `/cabinet-legacy/market` (единственный вызывающий d07-сигнатуру с org-параметром) — orphan, нигде не импортируется/не роутится → revoke безопасен, регрессии нет. **✅ DEPLOYED + VERIFIED 2026-07-07** к `mwtbozflyldcadypherr` — прямой запрос к `pg_proc.prosrc` подтвердил guard во всех 5 функций. **Уточнение по факту деплоя:** `revoke ... from authenticated` для 3 d07-сигнатур оказался **no-op** — `has_function_privilege('public', ..., 'execute')` = true (PUBLIC-грант никогда не отзывался, только anon), поэтому revoke от authenticated не перекрывает наследуемый PUBLIC-доступ. Реальная защита — исключительно guard в теле функции, он подтверждён рабочим; второй слой защиты не сработал как задумано (не критично — основной guard достаточен), но заявление о «втором слое» в коммите `bc83d84` было неточным — фиксирую здесь для честности. Кейсы SEC-RLS-01/SEC-GATE-01 переведены в `active`. |
> | SEC-GATE-MEMBERSHIP-01 | security/TSP-gate | 🟠 significant | code↔canon (MS6 Step 0) | `rpc_create_batch`/`rpc_publish_batch` без backend membership-гейта — не-член публикует партию (гейт только в UF) | эмпирика: не-член создал draft. SEC-GATE-01 | **✅ FIXED 2026-07-06, КОНСОЛИДИРОВАНО в `claude/confident-napier-599473`** — membership-check (гейт по легаси-колонке `memberships.level <> 'registered'`, пилотный стенд-ин до MEMBERSHIP-01) добавлен в ТРИ места: живой self-serve `rpc_create_batch` (`supabase/migrations/20260701150000_tsp_district_sort.sql` — реальный путь визарда фермера, это и есть эмпирически найденный SEC-GATE-01) + d07 `rpc_create_batch`/`rpc_publish_batch` (AI-Gateway путь, для полноты). Взято из ветки `claude/bold-payne-93aa8d` (git apply/ручной перенос, файлы были байт-идентичны). Стенд-ин по легаси-колонке подтверждён CEO 2026-07-06 (ветка не нашла подтверждения в DECISIONS_LOG/SPRINT_STATUS на момент фикса — CEO подтвердил решение постфактум в этой сессии); при закрытии MEMBERSHIP-01 (канон `state`) — заменить условие на `state IN ('active','grace_period')` во всех трёх местах. **✅ DEPLOYED + VERIFIED 2026-07-07** — `pg_proc.prosrc` содержит `MEMBERSHIP_REQUIRED` в self-serve `rpc_create_batch` и d07-пути. Кейс SEC-GATE-01 → `active`. |
> | SEC-GATE-LIMIT-01 | security/TSP-gate | 🟠 significant | code↔canon (MS6 4a лимит) | лимита активных партий на RPC нет (создано 7 draft подряд); поля лимита нет в `tsp_config` | эмпирика SEC-GATE-02 | Добавить лимит в tsp_config (standards-as-data) + проверку count(active batches) в rpc_create_batch |
> | SEC-SELFDEAL-01 | security/TSP-match | 🟠 significant | code↔canon (MS4 §7 антигейт) | матчинг не исключает `pool.organization_id = batch.organization_id` — мульти-тип org может сматчить свою партию в свой пул | статика: rpc_self_auto_match_batch L465-477, broadcast L500-517, fn_tsp_alloc_chunk. SEC-MISC-02 | Добавить условие `<>` во все ветки матчинга/broadcast/accept |
> | TSP-FLOW-10 | TSP-flow | 🟠 significant | code↔canon (BT-14/D-TSP-10) | underfill не возвращает партии: `rpc_self_close_due_pools` флипает пул в filled/closed, но matched-партии зависают с deal_price на мёртвом пуле (нет возврата в published, нет reveal); `awaiting_mpk_decision` недостижим, `rpc_pool_accept_partial`/`return_batches` неисполнимы; `mpk_decision_window_hours` из tsp_config не используется | эмпирика: пул 10/30 (33%)→'filled' без решения МПК. qa/runs/undated-0{5,6,8} TSPF-LIFE-11 / TSPM-CLOSE-03 / TSPM-POOL-07 / E2E-TSP-04 | Реализовать underfill-ветку канона (awaiting_mpk_decision + дефолт return по окну) в self-serve адаптере; PT-04 пустой пул → expired_empty |
> | BUG-GETORGBATCHES-01 | TSP-rpc | 🟠 significant | bug (code broken) | `rpc_get_org_batches` ссылается на `ts.description_ru`, которой нет в `tsp_skus` → RPC всегда падает `42703 column does not exist` | эмпирика (anon+auth). d07:1103 | Убрать/заменить `ts.description_ru` на существующую колонку tsp_skus. **✅ FIXED + DEPLOYED + VERIFIED 2026-07-11 (ARS-232, ветка `claude/tsp-defekt-rpc-supply-demand-ce4fbd`)** — колонки `description_ru` в `tsp_skus` нет ни в схеме, ни в каноне (Microstep 4 не определяет имя/описание SKU); это выдумка кода, а не schema≠canon на уровне сущности. По ruling'у Architect'а payload-ключ `sku_name` теперь несёт `ts.sku_code` (композиция RU-лейбла в SQL отклонена — P8/P4: нет таблицы-справочника для breed/sex/age, лейблы уже живут во фронте `RulesTab.tsx`). Тот же дефект `ts.description_ru` был **в 3 RPC** — все пофикшены в одном PR (L-2): `rpc_get_price_grid` (d07:959), `rpc_get_aggregated_supply` (d07:1015), `rpc_get_org_batches` (d07:1132). Канонический фикс в `d07_ai_gateway.sql`, применён напрямую к `mwtbozflyldcadypherr` (CREATE OR REPLACE, сигнатуры не менялись — P7), верифицирован прямым вызовом (`sku_name`=`sku_code`, напр. "TSP-0003"). **Phase-2 enhancement (не в этом фиксе):** богатый лейбл SKU должен быть data-driven (P8) — добавить `tsp_skus.name_ru` из источника-справочника или вью `tsp_sku_labels`; до тех пор `sku_name == sku_code` by design. |
> | BUG-AGGSUPPLYDEMAND-01 | TSP-rpc | 🟠 significant | bug (code broken) | `rpc_get_aggregated_supply` (42703 `ts.description_ru`) и `rpc_get_aggregated_demand` (42803 вложенные агрегаты `count/sum` внутри `jsonb_agg`) падали при любом вызове | эмпирика (rollback-tx на проде, ARS-215→ARS-232). d07:1015/1067 | **✅ FIXED + DEPLOYED + VERIFIED 2026-07-11 (ARS-232)** — supply: колонка→`sku_code` + агрегаты вынесены во внутренний grouped-подзапрос (класс FARM-01-bis/PR#77); demand: та же реструктуризация (`count/sum/count filter` в подзапросе, `jsonb_agg` поверх производных строк). Payload-ключи и сигнатуры не изменены (P7). Оба возвращают jsonb с реальными строками на проде (supply 1 ряд, demand 1 ряд; пустые массивы легальны). Канон `d07_ai_gateway.sql`, накат на `mwtbozflyldcadypherr`. |
> | MEMBERSHIP-07 | membership | 🟠 significant | code↔canon (MS2 ALREADY_ACTIVE) | `rpc_submit_membership_application` ловит ALREADY_ACTIVE для declared_supplier+, но НЕ для `observer` — уровня, который на пилоте = активное членство (выдаёт rpc_pay_membership_dues) → активный член подаёт повторную заявку observer→observer | эмпирика. d01_kernel.sql:3502. MEM-SUB-05 | **✅ FIXED 2026-07-06, КОНСОЛИДИРОВАНО в `claude/confident-napier-599473`** (из ветки `claude/kind-darwin-31a3e8`, task_8b750f23) — гейт сужен до `v_current_level <> 'registered'` (было `not in ('registered','observer')`); зеркалирует идемпотентный гейт `rpc_pay_membership_dues`. **✅ DEPLOYED + VERIFIED 2026-07-07** — `pg_proc.prosrc` подтверждает `v_current_level <> 'registered'`. Кейс MEM-SUB-05 → `active`. |
> | NOTE-ANON-EXEC-01 | deploy-drift | 🟡 note | deployed↔repo | на staging EXECUTE у anon на rpc_get_org_batches НЕ отозван, хотя репо содержит `revoke ... from anon` (d07:1344) — deployed БД отстаёт от репо | эмпирика (anon дошёл до тела) | Пере-применить revoke на staging; сверить deploy-pipeline |
>
> **Смена статуса кейсов (устаревшие ожидания):** MEM-ADM-04 / SEC-GATE-03 — код и канон Dok6-Slice2 отдают `FORBIDDEN`, кейсы ожидали `UNAUTHORIZED` → поправить текст кейсов (не код). SEC-RVL-02 — «анонимная репутация ★» помечена TODO в коде (migration 20260622120000:1675) → уточнить ожидание/blocked.

> **🧪 QA-ПРОГОН 2026-07-05 — ВОЛНА 2 (UI-слой, реальный backend через .env, staging):**
> Продолжение прогона выше — UI+rpc кейсы против живого staging (не demo-режим). 16 новых находок, не пересекающихся с волной 1. Полный отчёт: `qa/runs/undated-SUMMARY.md`.
>
> | id | domain | severity | conflict | what | evidence | action |
> |----|--------|----------|----------|------|----------|--------|
> | BUG-PUBRESULT-VARIANT-01 | TSP-ui | 🟠 significant | bug | `BatchWizard.tsx:138-139`: `variant = delayed ? 'D' : 'B'` не проверяет `match?.matched` → вариант A («Покупатель найден!») физически недостижим даже при реальном автоматче | эмпирика: batches.status='matched', deal_price=2500, экран всё равно показал вариант B. TSPF-RES-01 | **✅ FIXED 2026-07-06, КОНСОЛИДИРОВАНО в `claude/confident-napier-599473`** (из ветки `claude/vigilant-rosalind-df2f93`, task_1f0007a7) — `variant = delayed ? 'D' : (batch.state==='matched' ? 'A' : 'B')`. Фронтенд-only, деплой = обычный релиз кода. |
> | GAP-REVIEW-MOCK-01 | TSP-review | 🟠 significant | code lags (D-M6-11) | Изначальная находка была неточной: `rpc_submit_review` (миграция 20260622120000, контракт = `useBatches.ts`) УЖЕ существовал и писал в `batches.notes.review` — гэп был ýже: `fn_tsp_batch_json` не отдавал это поле обратно фронту (отзыв технически сохранялся, но кнопка не персистилась после reload), а МПК-сторона (`PoolMonitorModal.tsx` myRating) не звала RPC вовсе. Отдельно — полная каноническая система (`review_dimensions`/`deal_reviews`/`deal_review_dimension_scores`/`rpc_submit_deal_review`, d02_tsp.sql §7.13-7.15) существует, но orphan (0 вызовов из src/) — как и Success.tsx | TSPF-LIFE-16, TSPM-CLOSE-05 | **✅ FIXED 2026-07-06/07, КОНСОЛИДИРОВАНО в `claude/confident-napier-599473`** — миграция `20260706120000_tsp_batch_reviews.sql`: (1) `fn_tsp_batch_json` += `review` (читает уже сохранённое `notes.review`); (2) новый `rpc_self_submit_mpk_review` (симметричен существующему `rpc_submit_review`, пишет `notes.mpk_review`, гейт — владение пулом delivered-куска, без клиент-org_id) + `PoolMonitorModal.tsx`/`MpkApp.tsx`/`pools-load.ts` вызывают его на `StarPicker.onChange`; (3) `rpc_get_pool_matches` += `myRating`. **Самокоррекция в этой же сессии:** первая версия миграции завела НОВУЮ таблицу `batch_reviews` + дублировала `CREATE OR REPLACE rpc_submit_review` (не прочитав существующий код — риск L1, молча откатило бы рабочую реализацию) — обнаружено и убрано до коммита. **Осознанно не реализовано:** double-blind reveal, отдельные dimension-строки, конвергенция с canon `deal_reviews` — тот же уровень простоты, что у уже существовавшего `rpc_submit_review` (не в скоупе точечного фикса «сломанная кнопка»). **✅ DEPLOYED + VERIFIED 2026-07-07** — `pg_proc.prosrc` подтверждает `fn_tsp_batch_json` содержит поле `review`, `rpc_self_submit_mpk_review` существует. Кейсы TSPF-LIFE-16/TSPM-CLOSE-05 → `active` (с уточнённым, упрощённым ожиданием). |
> | ONB-SUCCESS-ORPHAN-01 | onboarding | 🟠 significant | архитектурное решение (не авто-фикс, HS-2/HS-5) | `Success.tsx` (KPI-плитки/«Первые шаги»/баннер заявки) — orphan, 0 импортов; `Registration.tsx` после Agreement идёт прямиком в `/cabinet`/`/mpk`, экран никогда не рендерится | grep+graphify: 0 imports. ONB-SUC-01 | **✅ FIXED 2026-07-06, КОНСОЛИДИРОВАНО в `claude/confident-napier-599473`** — выбрано «подключить» (не удалить), per HS-2/P7 (не удалять built-функциональность): добавлен шаг `success` в `STEP_ORDER`/`Registration.tsx`, `handleRegister()` теперь `goTo('success')` вместо прямого `navigate()`; кнопка на самом экране Success ведёт в `/cabinet`/`/mpk`. Попутный фикс: `Success.tsx` CABINET_CONTENT.mpk.route был `/cabinet` — исправлен на `/mpk` (несовпадение с конвенцией `pickShellPath`, не была видна пока экран был orphan). tsc чист. **Открыт вопрос** (не решён, требует Arshidin): баннер «Заявка в ТУРАН · на рассмотрении» на этом экране показывается всегда, хотя заявка на членство автоматически НЕ подаётся при регистрации (REG-SUB-03) — копирайт vs факт, см. qa/scenarios/03-onboarding.md ONB-SUC-01. Готово к обычному релизу (фронтенд-only, деплой через штатный CI/CD, не требует DB-доступа). |
> | REG-PHONE-NORM-01 | registration | 🟠 significant | bug | `PhoneInput.tsx` не нормализует ввод, начинающийся с «8» → на бэкенд уходит невалидный `+78XXXXXXXXX` вместо `+77XXXXXXXXX` | src/pages/registration/components/PhoneInput.tsx:12-19,32-43; Contact.tsx:51,96. REG-OTP-04 | Добавить замену ведущей 8→7 в маске/нормализации перед сборкой E.164 |
> | OTP-CONFIG-HARDCODE-01 | registration | 🟡 note (P8) | code lags | OTP TTL (300с) и MAX_ATTEMPTS (3) — хардкод-константы edge-функции, не конфиг-таблица в БД (в отличие от `tsp_config`) | supabase/functions/bird-otp/index.ts:11-12 | Вынести в конфиг-таблицу при появлении необходимости менять без деплоя (не блокирует MVP) |
> | REG-PRIMARY-ORG-01 | registration | 🟠 significant | code↔canon (MS1-D-IDM-2) | `rpc_register_organization` ставит `is_primary=true` каждой новой связке без уникальности → пользователь с 2+ организациями получает 2+ primary одновременно, влияет на pickShellPath | REG-SUB-07 | Добавить unique-constraint/сброс предыдущего primary при вставке новой связки |
> | SEC-ART-TSP-VOCAB-01 | TSP-ui/антитраст | 🟠 significant | code↔canon (D-TSP-16) | слово «пул» видно фермеру: `BatchScreen.tsx:202` («ждёт заполнения пула»), `WithdrawSheet.tsx:43,55` («пул заполнен») | SEC-ART-03 | Переформулировать лейблы на фермерский словарь (без Pool/match/target/filled) |
> | SEC-ART-DISCLAIMER-CREATEPOOL-01 | TSP-ui/антитраст | 🟡 note | требует уточнения | `CreatePoolModal.tsx` (МПК, поле цены заявки) без антитраст-дисклеймера ст.171 — открытый вопрос: относится ли требование MS4-§8 к цене, которую сам МПК вводит (не reference_price ассоциации) | SEC-ART-01 | Сверить с текстом канона MS4-§8 перед решением: фикс UI или правка формулировки кейса |
> | TSP-CANCELPOOL-ORPHAN-01 | TSP-flow | 🟠 significant | bug | `rpc_cancel_pool` возвращает батч в `published`, но не сбрасывает `matched_heads`/`batch_allocations`/`pool_lines.current_heads`/`pool_requests.status` → возвращённая партия невидима матчерам навсегда | TSPM-POOL-06 | Полный откат side-effects при cancel (симметрично close_due) |
> | TSP-PRICEFLOOR-01 | TSP-pricing | 🟠 significant | code↔canon (D-TSP-4/MS4-BT-11) | `rpc_lower_price` клэмпает только до 1 ₸/кг; таблица `minimum_prices` пустая и не проверяется — защитный пол цены не работает | E2E-TSP-03 | Заполнить `minimum_prices` + проверять клэмп в rpc_lower_price |
> | TSP-DEADLOCK-ACCEPT-01 | TSP-concurrency | 🟠 significant | code↔CLAUDE.md (no advisory locks — SKIP LOCKED) | конкурентный `rpc_self_accept_offer` двух МПК на один батч отдаёт проигравшему `40P01 deadlock detected` вместо бизнес-ошибки; разный lock-order offers→batches vs batches→pool_lines→pools | TSPM-OFF-05, E2E-TSP-07. Целостность данных не страдает | Единый lock-order + SKIP LOCKED вместо конкурирующих UPDATE-путей |
> | MPK-FLOORPRICE-HARDCODE-01 | TSP-mpk-ui | 🟡 note (P8) | code lags | `MPK_CATS` (floor-цены категорий) захардкожены в `src/pages/cabinet/shell/mpk/types.ts`, не читаются из БД-справочника | 06-tspm ui | Читать из справочника категорий/минцен вместо константы |
> | MEM-ADMIN-GUARD-MISMATCH-01 | membership-ui | 🟠 significant | bug | `/admin/*` защищён `RequireExpert`, не `RequireAdmin`; редирект без toast «Доступ запрещён»; неиспользуемый `RequireAdmin.tsx` и отдельный неподключённый `useAdminGuard.ts` — 3 несогласованных гарда на одну область | MEM-ADM-06 | Унифицировать на один guard-компонент с ожидаемым UX (toast + redirect) |
> | BATCH-GRADE-SKU-MISMATCH-01 | TSP-data | 🟡 note | наблюдение | `fn_tsp_resolve_sku` даёт SKU c grade_id=VS, но `batches.grade_standard_id` записан как S — расхождение внутри одного создания батча; не влияло на матчинг в прогоне | E2E-TSP-08 | Сверить логику присвоения grade при создании батча |

## 🔴 Breaking (12)

| id | domain | conflict | what | evidence | action |
|----|--------|----------|------|----------|--------|
| AI-GATEWAY-01 | AI Gateway | code↔canon | nodes.py calls rpc_update_confirmation_payload — exists in no SQL; two-run amend raises at runtime | ai_gateway/nodes.py:262 | Add rpc_update_confirmation_payload(p_organization_id,p_conversation_id,p_payload jsonb) to d07 + registry, OR repoint nodes.py amend |
| TSP-SCHEMA-01 | TSP-schema | code lags | rpc_create_batch / rpc_publish_batch missing from code though Dok3 marks them Implemented | d02_tsp.sql (no defs); Dok3:71-72 | Implement per M4 §2.2 (auto-match→broadcast→published, BT-01/02/03); Dok3 status corrected in Task 7 regardless |
| TSP-SCHEMA-02 | TSP-schema | code↔canon | BT-05 broadcast leaves batch 'published' but rpc_accept_offer requires 'offering' — Offers un-acceptable | d02_tsp.sql:2491-2493,2706-2709,3035 | Set offering on retry-match Offer upsert, OR implement BT-05 as direct published→matched per M4 §3.3 |
| TSP-FLOW-01 | TSP-flow | code↔canon | Published batch can never reach 'offering' → FCFS accept unreachable on happy path | d02_tsp.sql:2491-2493,2706-2709,3035 | rpc_retry_match_pool/rpc_publish_pool set published→offering (offering_at + market.batch.offering + offering_started) on Offer create |
| TSP-FLOW-02 | TSP-flow | code lags | rpc_publish_batch is pre-M6: single 'published' path, no soft-warn/classifier-lock/scheduled_publish_at/branch | d07_ai_gateway.sql:1272-1300 | Rewrite to M6 contract (soft-warn vs minimum_price, lock classifier_version, scheduled_publish_at, branch scheduled/matched/offering/published) |
| VET-02 | Veterinary | code↔canon | rpc_get_vet_case_detail trusts client p_organization_id, no fn_my_org_ids() guard — cross-org leak (Art.171) | d04_vet.sql:1815-1845 | **✅ FIXED** on branch `fix/vet-f11-isolation` (commit `4a961c9`), guard added, cross_check 0/0/0. **PENDING DEPLOY** to Supabase. |
| FEED-05 | feed | code↔canon | feed_consumption_norms seed INSERT precedes its CREATE TABLE — fresh apply of d03 fails | d03_feed.sql:839 seed, 1579 CREATE | Move CREATE TABLE (+fcn_unique_norm) above the seed, or relocate seed after 1620 |
| IDENTITY-07 | identity | code↔canon | RoleSelect emits org-type 'services'/'feed_producer' → SQL CHECK + INVALID_ORG_TYPE; registration broken for those roles | constants.ts:125; d01_kernel.sql:238,3320 | Map services→supplier, feed_producer→supplier (or extend OrganizationType set per IDENTITY-06), verify vs deployed CHECK. **✅ FIXED 2026-06-23 (commit `44ddb26`)** — `orgTypeMap` services/feed_producer→supplier in Registration.tsx (UI-level; `rpc_register_organization` unchanged). |
| MARKET-UI-04 | market-ui | code↔canon | Admin pool via rpc_activate_pool_request leaves pools.organization_id NULL → M4 ownership invariant break | d02_tsp.sql:1995-1999; PoolQueue.tsx | Migrate PoolQueue to rpc_create_pool, OR backfill organization_id in rpc_activate_pool_request |
| MEMBERSHIP-01 | membership | code↔canon | Deployed memberships uses deleted 4-level stack, not canon 6-state FSM; no state/grace/tier columns | d01_kernel.sql:289-296 | Additively add state + grace_reason/grace_until/paid_until/tier/revoke_reason/state_changed_at per Microstep2 §4 (A1) |
| MEMBERSHIP-02 | membership | code↔canon | Code requires pre-existing membership row at submit + FK membership_id NOT NULL; canon: row created at T2 approve | d01_kernel.sql:3480-3483,339 | Make membership_applications org-anchored (drop NOT NULL FK); approve(T2) creates row in grace_period |
| MEMBERSHIP-03 | membership | code lags | grace_period/expired/revoked + transitions T4-T10 + MembershipStateTransition log unimplemented | canon Microstep2 §3,§7 | Backlog billing-driven (T4/T7/T9) + cron-driven (T5/T6/T8) + admin revoke (T10) + audit table |

## 🟠 Significant (38)

| id | domain | conflict | what | action |
|----|--------|----------|------|--------|
| AI-GATEWAY-02 | AI Gateway | code↔canon | Python tool names diverge from Dok5 names (market/expert) | Apply A3: keep SQL/RPC names, add map layer; Dok5 = LLM-tool-name canon (Task 9) |
| AI-GATEWAY-03 | AI Gateway | code lags | consultant role empty tool set; search_knowledge/escalate/get_membership_status/get_subsidy_programs missing | Implement consultant tools + search_knowledge for all roles, OR mark Planned in Dok5 |
| AI-GATEWAY-04 | AI Gateway | code↔canon | zootech tools differ from canon get_farm_context/update_herd_group/create_herd_group | Wire canon write tools into zootech set, OR ratify deployed set in Dok5 |
| AI-GATEWAY-05 | AI Gateway | code lags | compliance.py missing CF-04 (cross-org BLOCK) + CF-03 (epidemic alert) | Implement CF-04 (P-AI-2 last line) + CF-03, OR downgrade in Dok5 §8 with rationale |
| AI-GATEWAY-06 | AI Gateway | code↔canon | price disclaimer keys on canon names never emitted → fires only for get_price_grid | Add real emitted names to price_tools set (gated by A3 / AI-GATEWAY-02) |
| CONSULTING-03 | Consulting | code lags | inverted pasture-season interval validation not enforced | Add CHECK(pasture_start_month<=pasture_end_month) + Pydantic model_validator |
| TSP-SCHEMA-03 | TSP-schema | code lags | 5 legacy pre-M4 pool_request/match RPCs still active, no deprecation flag | Mark deprecated (registry+comment), stop callers, remove after data migration (A6) |
| TSP-SCHEMA-04 | TSP-schema | code↔canon | rpc_cancel_batch blocks offering/awaiting_price_decision/matched + checks deprecated pool_matches | Extend to BT-10/12/15, withdraw Offers, write cancelled_after_match |
| TSP-FLOW-03 | TSP-flow | code lags | rpc_create_batch writes target_month, omits ready_from/ready_to/farmer_price_per_kg | Add/persist ready_from/ready_to(+farmer_price) per D-M6-6 so §2.3 overlap match works |
| TSP-FLOW-04 | TSP-flow | code lags | farmer cancel BT-10/BT-15 unimplemented; rpc_cancel_batch pre-M6 | Implement M6 cancel modes w/ pool_line/offers + events |
| TSP-FLOW-05 | TSP-flow | code lags | market.batch.confirmed (identity reveal, D-M6-5/12) declared in Dok4 but never emitted | Emit in rpc_accept_offer + rpc_pool_accept_partial w/ identity payload (A7) |
| TSP-FLOW-06 | TSP-flow | code↔canon | ~~offer.created~~ **DONE 2026-07-08**; ~~offer.withdrawn~~ **DONE 2026-07-08** (sibling_accepted/pool_cancelled/pool_returned emitted). Only `reason=batch_cancelled` remains — blocked on M6 rpc_cancel_batch (TSP-FLOW-04) | Resolved except batch_cancelled reason → folds into TSP-FLOW-04 |
| TSP-ADMIN-UI-01 | TSP-flow | code↔canon | pre-M6 write-UI `/admin/pools` (`PoolQueue.tsx`+`PoolDetail.tsx`) работает на устаревших pre-M6 RPC (`rpc_create_pool_request`, `rpc_activate_pool_request`, `rpc_match_batch_to_pool`, `rpc_advance_pool_status`, `rpc_rollback_batch_match`) и старой D40-семантике (раскрытие контактов на `executing` вместо `confirmed`, D-M6-5/12). Дублирует управление, теперь живущее в `/admin/marketplace` (M6-канон, admin-RPC `rpc_admin_*`, ARS-194/199) | **DEPRECATED 2026-07-09 (ARS-200):** выведён из основной навигации (Sidebar + AdminDashboard), несёт баннер «устаревший экран». Код и роут сохранены (HS-2). Полное удаление файлов + ретайр pre-M6 RPC — отдельным согласованием с CEO (связано с TSP-SCHEMA-03). |
| NOTIF-DISPATCH-01 | notifications | code lags | Slice-4 диспетчер `platform_event → INSERT notifications`. **PUSH-канал закрыт полностью**: ✅ `offer_created_mpk` (D-DISPATCH-01) + ✅ `batch_dispatched_mpk` (2026-07-09, контрагент-MPK через accepted-offer). Остаток — только **non-push** (WA+in_app) шаблоны, каждый со своим блокером: (a) `batch_matched_farmer/_mpk` — `market.batch.matched` НЕ эмитится в M6 `rpc_accept_offer` (только legacy admin pool_matches путь) + шаблонам нужны label'ы `{category}/{region}` (join к справочникам); (b) `batch_published_mpk` — `market.batch.published` не эмитится (есть только `market.pool.published`); (c) `batch_cancelled_mpk` — pre-M6 `rpc_cancel_batch` не имеет MPK (блокирует cancel при наличии matches), M6-cancel = TSP-FLOW-04 не построен; (d) `pool_contacts_revealed` — завязан на `market.batch.confirmed`, не эмитится (TSP-FLOW-05) | non-push остаток: для (a) эмитить market.batch.matched в rpc_accept_offer + fan-out обеим аудиториям (анонимно, до confirmed) с резолвом label'ов; (b) эмитить market.batch.published; (c) свернуть в TSP-FLOW-04; (d) свернуть в TSP-FLOW-05. Последний хоп notification→FCM/APNs = ARS-156 |
| VET-03 | Veterinary | code↔canon | code emits 'vet.signal.detected', canon 'vet.epidemic_signal.detected' | Rename emitted literals (d08:166,376; d04:2551) to canon |
| VET-04 | Veterinary | code↔canon | vet.vet_case.opened / vet.vaccination.plan_created / vet.vaccination.completed ≠ Dok4 canon | Rename to vet.case.opened / vet.vaccination_plan.created / vet.vaccination_record.created |
| VET-07 | Veterinary | code lags | health-restriction trigger emits no vet.health_restriction.created (Dok4 V-06 dead) | Add platform_events INSERT in fn_create_health_restriction_from_rec |
| EDUCATION-02 | education | code↔canon | code emits 'education.enrollment.completed'; Dok4 canon edu.* (A4) | Emit edu.course.completed + edu.certificate.issued (+enrolled/lesson) |
| EDUCATION-03 | education | code lags | certificate issuance emits no distinct edu.certificate.issued | Split emissions on cert INSERT when Slice 7 built |
| FEED-04 | feed | code↔canon | F17 is 4-tab RationPage; canon F17/F18 RationViewer.tsx/FeedBudget.tsx orphaned | Decide: doc the tabbed UI (FEED, Task 12) OR re-route; remove dead code |
| GOVERNANCE-01 | governance | code lags | entire Feature Governance layer (M3) zero implementation | CEO-gated: implement M3 tables+RPC fail-closed, OR confirm deferred (see open question) |
| GOVERNANCE-02 | governance | code lags | rpc_check_feature_access does not exist | Implement w/ M3, fail-closed (D-FG-2), register canonical name |
| IDENTITY-01 | identity | code lags | AssociationMembership not renamed; table still `memberships` | Rename intent recorded; gated on Microstep2 FSM finalization (A1) |
| IDENTITY-02 | identity | code lags | PlatformSubscription table absent (canon MVP) | Implement table + tier='free' auto-create hook |
| IDENTITY-03 | identity | code lags | SubscriptionEvent append-only log absent | Add subscription_events table |
| IDENTITY-04 | identity | code lags | FeatureGate registry absent | Implement feature_gates + effective_access() (antitrust-relevant) |
| IDENTITY-05 | identity | code↔canon | canon mandates 3 registration RPCs; code keeps atomic rpc_register_organization | Split per D-IDM-6 (additive migration); Dok3 RPC-01 noted in Task 7 |
| IDENTITY-06 | identity | code↔canon | OrganizationType seed diverges (no service/education/government; no requires_approval) | Create organization_types ref table w/ requires_approval; align codes |
| IDENTITY-09 | identity | code↔canon | expert = expert_profiles table vs canon D-IDM-8 (User.expertise_areas[]) | A8: keep expert_profiles (HS-2); add expertise_areas if needed; canon ratified to reality (Task 14) |
| IDENTITY-11 | identity | code↔canon | onboarding intent (D-IDM-5) no storage; users.preferences missing | Add users.preferences jsonb; move intent off blocking role_select |
| MARKET-UI-01 | market-ui | code lags | A-CAT screens A-CAT-01..04 never built | Build 4 screens+layout+sidebar+routes, OR mark UI-pending (doc note in Task 11) |
| MARKET-UI-02 | market-ui | code lags | farmer batch UI old 5-state FSM; deployed has 11 M4/M6 states | Extend UI (offering/awaiting_price_decision+lower_batch_price/confirmed/dispatched/delivered) |
| MEMBERSHIP-04 | membership | code↔canon | rpc_submit_membership_application signature diverges (membership_type/notes vs documents[]) | Gated on A1: additive replacement RPC accepting documents[] |
| MEMBERSHIP-06 | membership | code↔canon | frontend membership_status = 4th vocabulary | Consolidate to canon AssociationMembership state (A1) |
| TSP-ADAPTER-01 | TSP-flow | code↔canon | 2 слоя TSP-RPC сосуществуют: canonical M4/M6 (uuid, admin-pipeline) + self-serve adapter (text-sig rebind, `rpc_self_*`/`fn_tsp_*`). Adapter в `supabase/migrations/` (Option B, ADR-CABINET-SHELL-01), вне cross_check.sh scope | Phase-2: реконсиляция/конвергенция слоёв; решить судьбу legacy admin-pipeline vs self-serve self-match. **✅ DECIDED 2026-06-23: D-TSP-CANON-01** — adapter = канон торгового слоя; отменяет A6 + DEF-TSP-M4-OWNERSHIP. CONVERGENCE pending: порт MS4/MS6-матчинга в adapter по **D-TSP-MATCH-01** (ask+бид, deal=бид, overlap, broadcast/Offer/FCFS), затем ретайр uuid-дублей. |
| TSP-ADAPTER-02 | TSP-schema | deploy-order | rebind дропает d07 `rpc_create_batch(uuid)`/`rpc_get_org_batches(uuid,text)` → text-sig/no-arg. На проде применён ПОСЛЕ d07 (text-sig wins, AI-19/AI-20 uuid-sig retired). Полный `deploy_sql.py` (d02<d07) БЕЗ re-apply adapter → возврат uuid-sig → PGRST203 overload | Deploy-порядок: d-файлы → затем `supabase/migrations/` adapter. Phase-2: при конвергенции убрать d07 uuid-sig defs. **✅ DECIDED 2026-06-23 (D-TSP-CANON-01):** канонический порядок деплоя = d-файлы → adapter; d07 uuid-sig defs убрать при конвергенции. |
| CABINET-SHELL-01 | market-ui | code lags | MPK-юзеры не авто-роутятся на `/mpk`: `pickShellPath()` (account.ts) есть, но main `Login` хардкодит `/cabinet` (protected main-auth). `/mpk` reachable только прямым URL | Phase-2: role-based post-login redirect (не ломая main-auth) ИЛИ `CabinetApp` self-redirect для mpk-орг. **✅ FIXED 2026-06-23 (commit `ca34bd6`)** — `pickShellPath` redirect в Login.tsx (deep-link `from` сохранён, main-auth не тронут). **✅ FIXED 2026-06-25** — post-registration redirect в Registration.tsx тоже учитывает роль (`role === 'mpk' ? '/mpk' : '/cabinet'`); раньше хардкодил `/cabinet` → МПК после реги попадал в фермерский шелл. |
| REG-EXPERT-01 | identity | code lags | expert-роль регистрации из `feature/my-changes` отложена: `'expert'` нет в d01 `org_type` CHECK + `rpc_register_organization`; UI `ExpertDetails`/`ExpertDocs`/`WelcomeStep` не взяты | Phase-2: extend org_type CHECK + `rpc_register_organization` под expert (+expert fields), затем порт UI; связано с IDENTITY-07. **✅ PARTIAL 2026-06-23 (commit `44ddb26`, Option A — reuse `consultant`):** UI (5-я роль + ExpertDetails/Docs ported) + expert→consultant через `orgTypeMap`, БЕЗ изменения org_type CHECK. PENDING Option B: запись `expert_profiles` при регистрации (решения по таксономии: vet→veterinarian map, single spec, is_staff, experience/price/about). |
| DEPLOY-PIPE-01 | deploy | deploy-order | `deploy_sql.py` SQL_FILES не включает `d10_public_site.sql` (docstring до 2026-07-03 тоже отставал: «d01→…→d08» при факте d09+d11) — свежий полный деплой молча пропустит public-site схему (registration_applications, news, storage buckets). Найдено 2026-07-03 при сверке apply order для CLAUDE.md | добавить d10 в SQL_FILES после d09 ЛИБО задокументировать отдельный путь деплоя d10; учесть TSP-ADAPTER-02 (adapter в `supabase/migrations/` применяется ПОСЛЕ d-файлов) |

## 🟡 Minor (13)

| id | domain | what | action |
|----|--------|------|--------|
| CONSULTING-06 | Consulting | Dok7 Фаза4 `activated` + rpc_activate_consulting_project unimplemented | Mark NOT-YET (deferred) in Dok7 (Task 13) — no code action |
| CONSULTING-07 | Consulting | SimpleRationEditor omits nutrients_met/deficiencies/solver_status/calc_* (ADR-FEED-06) | Persist fields OR annotate Dok7 §9.3 (Task 13) |
| TSP-SCHEMA-07 | TSP-schema | pool_matches/pool_manifests lack DEPRECATED comments | Add deprecation comments; resolve Q-TSP-MANIFEST-FATE |
| TSP-FLOW-09 | TSP-flow | auto-close 400kg target_heads heuristic is lossy placeholder | Document placeholder OR make target_heads nullable |
| VET-10 | Veterinary | vet_cases.severity DB default 'moderate' vs D-F10-1 'nullable, AI determines' | Drop default, set rpc default null |
| IDENTITY-10 | identity | EconomicActivityType eligibility ref (D-IDM-1) not implemented | Add economic_activity_types ref + organizations.economic_activity_type_ids[] |
| MARKET-UI-07 | market-ui | CAPEX routes moved to /admin/directories/capex/*; Dok6-Slice-CAPEX stale | Doc fix in Task 11 |
| OPERATIONS-04 | operations | RPC-37 deployed but canon/Dok3 say NOT IMPLEMENTED | Doc status flip (Tasks 7 & 12) |
| OPERATIONS-05 | operations | RPC-44 rpc_add_knowledge_chunk deployed but marked DEFERRED | Doc status flip (Tasks 7 & 12) |
| IDENTITY-13 | identity | DEF-009 fn_* dual defs (d01 naive + d07 JWT); last-applied wins | Add comments at d01 defs noting d07 override (low risk) |
| TSP-SCHEMA-08-code | TSP-schema | (canon-internal, no code change) Batch links pool_line not Pool | Cross-ref note only — Task 14 |
| CABINET-SHELL-02 | market-ui | Новый мобильный shell = primary `/cabinet` (+`/mpk`, вне AppLayout); legacy веб-кабинет → `/cabinet-legacy` (Sidebar/Header/22 страницы перепривязаны). Консалтинг `/admin` не затронут | Решить судьбу `/cabinet-legacy` после фидбэка (fallback vs убрать) — ADR-CABINET-SHELL-01 |
| QA-GATE-01 | qa | `cross_check.sh` SQL_FILES не включает `d11_norms.sql` — d11 вне QA-гейта; заодно: `farm_norms_ref` PK = `serial`, а Code Rules требуют uuid (найдено 2026-07-03) | добавить d11 в SQL_FILES cross_check.sh (проверить, что гейт не падает на serial PK); решить судьбу serial PK — справочник P8, low risk |

> **Note on VET-02:** ✅ FIXED on branch `fix/vet-f11-isolation` (commit `4a961c9`) — ownership guard added, single definition confirmed, cross_check 0/0/0. **NOT YET DEPLOYED** — apply via `python3 deploy_sql.py <DB_PASSWORD>` to Supabase `mwtbozflyldcadypherr` before prod is safe.

## 🟢 Native app (design-tracked, 4)

### DEBT-NATIVE-ROUTER-01 — dual react-router (v5-остров для Ionic-оболочек) ИЛИ отложенный edge-swipe
**Context:** ADR-NATIVE-ROUTER-01 AMEND-1 (2026-07-03). v6-совместимого Ionic-роутера не существует; движок стека страниц/переходов Ionic React (`StackManager`) шипится ТОЛЬКО в `@ionic/react-router`, который статически импортирует v5-only API (спайк: standalone outlet на v6 = белый экран). Решение: вариант A = изолированное поддерево react-router **v5** для `/cabinet/*` + `/mpk/*` через Vite resolve.alias (остальное приложение v6), за гейт-спайком сосуществования; фолбэк — вариант C = плоский Ionic на v6 без edge-swipe.
**Debt если шипим A:**
- Две версии react-router сосуществуют (v5 для Ionic-оболочек, v6 для остального), читая один `window.history`. Пересматривать при любом мажорном бампе любой из версий. Retire-when: Ionic выпустит официальный react-router v6 адаптер (трекать ionic-framework#24177) → снять v5-alias.
- Vite-alias обязан оставаться scoped только на Ionic-пакет; регрессия, «протёкшая» v5 в дерево приложения (или наоборот) = тихий слом — покрыть smoke-тестом: v6-роут приложения И v5-роут оболочки оба рендерятся и навигируются. ✅ Покрыто (2026-07-04): `src/tests/router-smoke.browser.test.tsx`, vitest browser-проект `routers` (`npm run test:routers`); тест проверен симуляцией регрессии — island-редирект отключён → тест /cabinet падает («no export named 'withRouter'»), v6-тесты живут.
**Debt если шипим C (фолбэк):**
- Нет edge-swipe-назад и независимых back-стеков вкладок; push/pop = ручной CSS. ОСОЗНАННО пожертвованный acceptance-пункт (трек fast-follow, D-ROADMAP-01). Retire — миграцией на A или на официальный v6-роутер Ionic.
**Blocking pre-work:** ✅ ГЕЙТ ПРОЙДЕН 2026-07-03 (см. DECISIONS_LOG ADR-NATIVE-ROUTER-01 AMEND-1) — dual-router доказан в этом Vite 6 без patch-package (importer-keyed `resolveId`-плагин `agos:ionic-v5-island` + nested optimizeDeps.include CJS-депсов). **Ветка A активна**; ветка C не понадобилась. Остаточный долг = блок «если шипим A» выше; smoke-тест v6+v5 роутов написан 2026-07-04 (`src/tests/router-smoke.browser.test.tsx`, проект `routers`). Dev-нюанс: после рестарта Vite-сервера нужен полный reload страницы (стейл-граф → тихий no-op навигации).
- **Install-mode долг (найден Vercel-билдом PR #27):** peer-требование `react-router@^5` у `@ionic/react-router` осознанно не удовлетворяется → чистый `npm install` падает с ERESOLVE. Фикс: репо-уровневый `.npmrc` `legacy-peer-deps=true` (детерминированно для Vercel/CI/локали). Цена: npm перестаёт валидировать peer-конфликты ВСЕХ пакетов — будущие реальные peer-конфликты не всплывут на install; ловить их на PR-ревью бампов. Убрать `.npmrc` вместе с v5-островом. **Процесс-правило зафиксировано (S2.1, ARS-157):** `CLAUDE.md` → Code Rules → «Dependency Bumps» (D-DEP-BUMP-01) — ручной чек `npm ls` на каждом бампе; retire вместе с `.npmrc`/островом.
- **Карты направлений (S6, ARS-152):** маппинг Route↔URL + глубина анимации теперь живут в ДВУХ файлах — `shell/nav.ts` (фермер) и `shell/mpk/nav.ts` (МПК). При добавлении роута в любую оболочку — обновить её карту, иначе направление анимации молча дефолтится. Retire — вместе с островом.
- **Per-tab стеки — URL-группировка (N-2, Increment 2, 2026-07-15):** фермерский `shell/nav.ts` группирует URL по табу (`/cabinet/{home,farm,market,messages}/…`, sibling-префиксы) — требование Ionic v8 для независимого стека на вкладку. При добавлении роута: положить его под URL-префикс СВОЕГО таба И прописать `tabOf`/TAB_MAP (`store.ts`), иначе экран не привяжется к вкладке (сломается подсветка/стек). `urlToRoute` держит backward-compat старой плоской схемы + декларативные `<Redirect from→to>` в outlet нормализуют старые deep-link/push-URL — **не удалять, пока живы старые push-пейлоады** (C10). Pathless-fallback в outlet ОБЯЗАН быть стабильным (`render={renderHome}`): условный рендер/`<Redirect>` в нём морфит persistent вью-стек Ionic и двоит страницы (диагностировано). **МПК-остров (`mpk/*`) на per-tab стеки НЕ переведён** — зеркало N-2 = отдельный follow-up. Retire — с островом.
**Severity:** medium (A: ограничено, конфиг-уровень, гейт пройден; smoke-тест ловит регрессию острова — остаётся сам факт двух роутеров до официального v6-адаптера Ionic).
**Owner surface:** `vite.config.ts`, `package.json`, `src/pages/cabinet/shell/CabinetApp.tsx` + `shell/nav.ts` + `shell/store.ts` (TAB_MAP), `src/pages/cabinet/shell/mpk/MpkApp.tsx` + `mpk/nav.ts`, `src/tests/router-smoke.browser.test.tsx` + `src/tests/router-tabstack.browser.test.tsx` (per-tab стеки N-2) + `src/tests/messages-back.browser.test.tsx` (issue #4). Слайсы: ARS-148 (S2 ✅), ARS-152 (S6 ✅ 2026-07-04 — оба острова активны), Increment 2 N-2 (фермер ✅ 2026-07-15).

### DEBT-NATIVE-ASSETS-01 — иконка/сплэш = апскейл, не векторный мастер (ARS-150 / S4)
**Context:** `assets/logo.png` (источник `npm run cap:assets`) сейчас — lossy-апскейл `public/favicon-512.png` до 1024 (в песочнице нет векторного мастера). Нативные иконки/сплэши сгенерированы из него.
**Retire-when:** финальная бренд-иконка из дизайн-прохода UI-кита **ARS-109** → заменить `assets/logo.png` (+ опц. `logo-dark.png`/кастомный сплэш), перегенерировать `npm run cap:assets` + `npx cap sync`.
**Severity:** low (визуальный долг, не функциональный). **Owner surface:** `assets/`, `android/app/src/main/res/**`, `ios/App/App/Assets.xcassets/**`.

### DEBT-NATIVE-STORE-01 — deep-link плейсхолдеры + iOS pod/сборка на build-машине (ARS-150 / S4)
**Context:** (1) `public/.well-known/apple-app-site-association` `TEAMID` и `assetlinks.json` `REPLACE_WITH_SIGNING_CERT_SHA256` — плейсхолдеры до определения store-аккаунта (открытый вопрос: TURAN vs Zengi, Apple D-U-N-S). (2) iOS `pod install` + сборка + прогон на устройствах не выполнены в песочнице (нет CocoaPods/полного Xcode) — шаг acceptance на настроенной Mac-build-машине. (3) iOS Associated Domains capability и `google-services.json`/APNs (push, S5) заводятся вручную при первой настройке подписи.
**Retire-when:** store-аккаунт определён → подставить Team ID + signing SHA256; прогон TestFlight/Internal testing зелёный (ARS-156 E2E push). См. `Docs/AGOS-NativeApp-S4-BuildAndAcceptance.md`.
**Severity:** medium (блокирует финальный deep-link + публикацию, НЕ блокирует сборку). **Owner surface:** `public/.well-known/*`, `ios/`, `android/app/`. Связ.: ARS-156 (S5.4), C-серия push.

### DEBT-NATIVE-VERIFY-01 — нативно-специфичные правки Этапа 2 не проверены на устройстве (аудит нативности 2026-07-13)
**Context:** Пакет нативной полировки `/cabinet` (Этап 2) содержит правки, которые в web-превью НЕ наблюдаемы (`env(safe-area-*)`=0, `WebHost.onDeepLink`=no-op, статус-бар — только Capacitor). Code-verified (tsc+build зелёные), но требуют device smoke-test:
- **C10** — тёплый deep-link: тап по push при открытом приложении переключает экран ВНУТРИ острова (`host.onDeepLink` мультиподписка → `CabinetApp.go(urlToRoute)`). Проверка: открыть /cabinet, push с `data.path='/cabinet/batch/:id'`, тап → экран партии открывается без холодного рестарта; не-остров путь (напр. /membership) по-прежнему через PushDeepLinkBridge.
- **C2** — `padding-top: env(safe-area-inset-top)` на `.ion-page`+MPK-модал: на iPhone с чёлкой шапки/OfflineBar не под статус-баром.
- **L1/L2** — `env(safe-area-inset-bottom)` чат-док + тост: не в зоне home-indicator.
- **L5** — цвет статус-бара `#f6f3ed` = фон кабинета: нет Android-шва статус-бар↔контент.
**Retire-when:** прогон на iOS+Android устройствах/симуляторах (Mac-build-машина, см. DEBT-NATIVE-STORE-01) — все пункты зелёные. **Severity:** low (правки аддитивны/безопасны; деградация = косметика на устройстве, не краш). **Owner surface:** `src/platform/host/{AgOSHost,WebHost,CapacitorHost}.ts`, `src/pages/cabinet/shell/{CabinetApp.tsx, ionic.css, cabinet.css, messages-chatscope.css}`, `capacitor.config.ts`. Связ.: аудит нативности 2026-07-13, DECISIONS_LOG 2026-07-14.

## 🐄 Farm module (ARS-169 reconciliation, 2)

> Найдены аудитом покрытия ARS-169 (2026-07-08), сверены с задеплоенным SQL. Полная карта: `Docs/AGOS-Farm-Module-CoverageMap-ARS169.md`. Оба — часть ЦТК-lifecycle (self-service петля F-D12); блокируют ARS-172.

### FARM-01 — `rpc_get_production_plan` ссылается на несуществующие колонки (draft-читатель сломан)
**Context:** `d07_ai_gateway.sql:280-283` — RPC селектит `fpp.plan_name` / `fpp.plan_start_date` / `fpp.plan_end_date`, но таблица `farm_production_plans` (`d05_ops_edu.sql:367-370`) имеет `name` / `cycle_start_date` / `cycle_end_date`. Runtime `column does not exist`. Это **единственный** RPC, читающий план любого статуса (в т.ч. `draft`); `rpc_get_active_plan` (d05:3637) отдаёт только `active`. Следствие: просмотр draft-плана перед активацией (F-D12) недоступен.
**Retire-when:** переименовать 3 колонки в SELECT на канонические (`name`/`cycle_start_date`/`cycle_end_date`); аддитивно, сигнатура RPC не меняется (P7). Проверить `cross_check.sh` + фактический вызов на staging.
**Severity:** medium (сломанный RPC, но текущий UI его не вызывает — `ProductionPlan.tsx` использует `rpc_get_active_plan`; всплывёт при постройке просмотра draft в ARS-172). **Owner surface:** `d07_ai_gateway.sql:260-290`.

### FARM-02 — нет RPC перехода draft→active (петля активации ЦТК неполна)
**Context:** `farm_production_plans.status` = draft/active/completed/cancelled (d05:372). Draft создаётся `rpc_start_production_plan` (d05:3159); активация в `active` происходит **только при создании** через `p_auto_activate:=true`. Отдельного `rpc_activate_production_plan` (draft→active после создания) НЕТ — существующие `rpc_activate_*` принадлежат другим доменам (d02 pool, d04 vaccination). Self-service петля F-D12 (фермер → draft → просмотр → активация) собрана только на 1/3.
**Retire-when:** добавить аддитивный `rpc_activate_production_plan(p_organization_id, p_plan_id, …)` с проверкой `idx_farm_plan_one_active` (один active на ферму) + emit события. Реализовать в ARS-172.
**Severity:** medium (блокирует self-service активацию из фермерского кабинета; сейчас активировать может только auto-путь при создании или эксперт вручную в консоли). **Owner surface:** `d05_ops_edu.sql` (RPC-слой ЦТК), `rpc_name_registry`.

## 💬 Messaging (d12, ARS-223/224 — backend merge 2026-07-16)

> Найдено код-ревью при backend-only merge ветки `kernuree/messaging-support-channel`. RLS-рекурсия (#1) и гонка get-or-create (#2) исправлены В ТОМ ЖЕ merge; ниже — отложенный perf-пункт. Фронт-находки (#3 null-body preview, #4 сравнение времени unread) относятся к невнесённому фронту ARS-225 и уедут вместе с ним.

### MSG-FANOUT-IDX-01 — idempotency-guard fan-out сканирует notifications без индекса
**Context:** `d12_messaging.sql` `fn_fanout_comm_notifications` перед вставкой делает `exists(select 1 from notifications where template_id='new_message' and (params->>'message_id')::uuid = p_message_id)`. Индекса на `params->>'message_id'` нет → по мере роста `notifications` это O(N) фильтр-скан, выполняемый синхронно внутри каждого `rpc_send_message`.
**Retire-when:** добавить partial expression index `on notifications (((params->>'message_id'))) where template_id='new_message'`, либо перевести идемпотентность на `platform_event_id`. Аддитивно.
**Severity:** low (на Phase-1 объёмах незаметно; всплывёт при масштабе). **Owner surface:** `d12_messaging.sql` (fn_fanout_comm_notifications), `d01_kernel.sql` (notifications).

# Прогон qa/scenarios/03-onboarding.md — backend-волна (sql/rpc/e2e)

Окружение: staging REST-режим (.env → SUPABASE_URL + SERVICE_ROLE_KEY доступны, не использовались), коммит b7b8cf4
Итог: PASS 0 / FAIL 0 / PARTIAL 0 / SKIP 0 (из 6 кейсов файла; 0 в скоупе backend-волны)

## Скоуп-фильтр

Задание backend-волны: исполнять только кейсы, чей `layer:` содержит `sql`, `rpc` или `e2e`.
Разбор метаданных всех кейсов файла:

| ID | layer | status | В скоупе backend? |
|---|---|---|---|
| ONB-SUC-01 | ui | active | нет → UI-волна |
| ONB-SUC-02 | ui | mock | нет → UI-волна |
| ONB-ROUTE-01 | ui | active | нет → UI-волна |
| ONB-ROUTE-02 | ui | active | нет → UI-волна |
| ONB-ROUTE-03 | ui | active | нет → UI-волна |
| ONB-GATE-01 | ui | active | нет → UI-волна |

**Вывод:** файл целиком UI-слойный (что ожидаемо: онбординг = экран Success,
маршрутизация оболочек pickShellPath, гейт Рынка — всё клиентское поведение;
Microstep 5 не проектировался, канон = код + Dok6-слайсы). Backend-волне
исполнять нечего. Кейсы НЕ помечаются SKIP, чтобы не задваивать учёт —
их прогонит UI-волна против preview-сервера.

## Мутации БД / зачистка

БД не трогалась (ни одного вызова). QA-префиксных сущностей не создавалось —
зачистка не требуется.

## Находки (кандидаты в дефекты / IMPL_DEBT)

- Нет (исполнение не проводилось). Флаг внутри ONB-SUC-01 (копирайт баннера
  «заявка на рассмотрении» vs факт REG-SUB-03 «заявка автоматически не подаётся»)
  остаётся на проверку UI-волной.

## Предложения по кейсам

- Нет. Разметка `layer:` корректна для всех 6 кейсов.

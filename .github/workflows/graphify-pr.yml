name: graphify blast radius

# On every PR, comment which graph nodes the changed code is connected to and
# flag touched "god nodes" (high-degree hubs).
#
# graphify-out/ is gitignored (retro ARS-152: a committed graph.json conflicted
# on every pair of parallel PRs), so there is nothing to read from the repo.
# Instead this builds a fresh, ephemeral AST-only graph (code structure, no LLM,
# no API key) directly in the runner on every invocation — nothing is committed.
#
# Note: PRs opened from forks get a read-only token, so the comment step is
# skipped there. Branch-to-branch PRs inside the repo work normally.

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: graphify-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  blast-radius:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Compute changed files
        run: |
          git diff --name-only \
            "${{ github.event.pull_request.base.sha }}...${{ github.event.pull_request.head.sha }}" \
            > changed.txt
          echo "Changed files:"; cat changed.txt

      - name: Build ephemeral AST graph (not committed)
        run: uvx --from graphifyy graphify update . || echo "graphify build failed — impact comment will be skipped"

      - name: Build impact comment
        id: impact
        run: |
          if [ ! -f graphify-out/graph.json ]; then
            echo "graph build failed — skipping"; echo "skip=1" >> "$GITHUB_OUTPUT"; exit 0
          fi
          python3 .github/scripts/graphify_pr_impact.py changed.txt graphify-out/graph.json > impact.md
          cat impact.md

      - name: Upsert PR comment
        if: steps.impact.outputs.skip != '1'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('impact.md', 'utf8');
            const marker = '<!-- graphify-blast-radius -->';
            const { data: comments } = await github.rest.issues.listComments({
              ...context.repo, issue_number: context.issue.number, per_page: 100,
            });
            const existing = comments.find(c => c.body && c.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({ ...context.repo, comment_id: existing.id, body });
            } else {
              await github.rest.issues.createComment({ ...context.repo, issue_number: context.issue.number, body });
            }

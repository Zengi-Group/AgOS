---
status: agreed           # гейт замысла проведён 2026-09-11: ревью качества дало 12 дыр по 5 осям, 10 закрыты правкой спека, 2 приняты явно (FR-014 — надписи не меняем; M-010 — причина отказа базы одна на два случая); решения владельца 11.09 записаны в Clarifications. Код (/build) — после подписи g2_approved
g2_approved: 2026-09-11 · Dias Zhagaparov
baseline_commit:         # пусто до /build
brain_spec: "[[projects/agos/specs/tsp-farmer-sell-flow]]"
task_home: ARS-687 (эпик ARS-94 · Рынок TSP); сосед-предшественник ARS-684 (монитор, shipped)
---

# Маркет-борд — предложение только в настоящую заявку — выдуманной сделки больше нет

<frozen-after-approval>

## Intent

Оператор мясокомбината открывает партию на маркет-борде и отправляет по ней предложение.
Селектор «Привязать к закупке» по умолчанию стоит на «Без привязки» — и в этом случае экран
**придумывает** хозяйство («КХ «Берекет»»), объявляет «Сделка состоялась», показывает
раскрытые контакты и принимает оценку. В базе не появляется ничего: ни привязки партии, ни
оффера, ни отзыва. Комбинат уверен, что купил; фермер не узнаёт о «сделке» никогда.

Это третья из трёх демо-кнопок закупочного флоу (две закрыл ARS-684) и единственная, которая
срабатывает на **реальных** партиях без ограждения. Слайс убирает выдуманный путь: предложение
уходит только в настоящую заявку комбината, а когда отправить некуда — оператор видит, почему,
и что делать. Канон это и предполагает: `Offer` — обращение **партии к комбинату**
(Microstep 4 §40), путь комбината к партии один — привязка к своей заявке.

## Boundaries & Constraints

**Always**
- **FR-001** Для **реальной** партии (строка БД) отправка доступна только когда выбрана
  **реальная** заявка комбината. Пара «реальное + демо» в любую сторону отправку не разрешает.
  Единственное изъятие — полностью демонстрационный путь (`FR-007`).
- **FR-002** Пока отправка недоступна, оператор видит **причину** рядом с кнопкой, дословно:
  заявка не выбрана — «Выберите свою заявку: предложение уходит в конкретную закупку»;
  выбрана демонстрационная заявка при реальной партии — «Эта заявка демонстрационная — выберите
  настоящую»; заявок нет — см. `FR-004`.
- **FR-003** Если у комбината ровно одна заявка, доступная для привязки, она выбрана заранее —
  руками выбирать не нужно. Подстановка выполняется **после** того, как список заявок прочитан из
  базы (`FR-009`), и не отменяет выбор, уже сделанный оператором руками.
- **FR-004** Если прочитанный список заявок пуст, оператор видит отдельное состояние с текстом
  «Сначала создайте заявку на закупку — предложение уходит в неё» и **кнопкой**, ведущей на
  создание заявки; окно партии при этом закрывается.
- **FR-005** Успешная отправка привязывает партию к выбранной заявке существующей
  `rpc_self_match_batch_to_pool`. Задеплоенное тело этой функции создаёт **кусок** партии
  (`fn_tsp_alloc_chunk`), поэтому в мониторе заявки строка появляется маршрутом «кусок»
  (`source = 'allocation'`, ARS-684). Слайс это поведение не меняет — см. `FR-012`.
- **FR-006** Ни на одном исходе интерфейс не показывает хозяйство, которого нет в базе, и не
  объявляет сделку, которой в базе нет.
- **FR-007** Полностью демонстрационный путь сохраняется: **демо-партия** (идентификатор не UUID)
  при любом состоянии селектора ведёт себя как сегодня, включая «Без привязки». Это изъятие из
  `FR-001`, а не исключение из него: реальных данных этот путь не касается.
- **FR-008** Проверка минимальной цены остаётся ровно такой, как сегодня: подсказка под полем
  ввода, **не** блокирующая отправку. Слайс её поведение не меняет.
- **FR-009** Пока список заявок не прочитан, оператор видит, что он грузится, и отправка
  недоступна. Если прочитать не удалось, показано это, а не «заявок нет», и демонстрационные
  заявки в селектор реальному пользователю не подставляются.

**Never**
- **FR-010** Слайс **не заводит** направление «предложение комбината фермеру»: в модели его нет
  (`offers` несёт `mpk_org_id` и статус `pending — awaiting MPK response`, то есть офферы идут от
  фермера к комбинату; задеплоены только `rpc_self_accept_offer`/`rpc_self_reject_offer`).
  Это отдельная работа с новой схемой, RPC, извещением фермеру и решением по ст. 171.
- **FR-011** Слайс не трогает монитор заявки (дом — ARS-684, shipped), входящие офферы,
  уведомления фермеру, планировщик, живые цены, выход из недобравшегося пула (`TSP-FLOW-10`).
- **FR-012** Слайс **не трогает механику дробления**. Установлено проверкой прода: задеплоенная
  `rpc_self_match_batch_to_pool` зовёт `fn_tsp_alloc_chunk`, то есть привязка создаёт кусок —
  откат 26.07 (`TSP-SLICE9-ROLLBACK-01`) переопределил три другие self-serve функции, а эту не
  тронул. Владелец 11.09 решил **принять это как есть** и записать явно: единственный рабочий
  путь привязки сегодня — через кусок, и монитор после ARS-684 его показывает. Приведение этой
  функции к non-split модели — дом ARS-314, не этот слайс.
- **FR-013** Слайс не меняет схему БД, не заводит новых RPC и не меняет сигнатуры существующих (P7).
- **FR-014** Слайс не меняет надписи существующих элементов («Отправить предложение», тост
  «Оффер отправлен — партия привязана к закупке»): расхождение слова «предложение» с немедленной
  односторонней привязкой — отдельное решение по копирайту, а не работа этого слайса.

## I/O & Edge-Case Matrix

| id | сценарий | вход / состояние | ожидаемое | обработка ошибки |
|----|----------|------------------|-----------|------------------|
| M-001 | happy · привязка | реальная партия, выбрана реальная заявка | отправка проходит; партия привязана и видна в мониторе этой заявки строкой маршрута «кусок» | RPC отказал → показана строка отказа из базы, партия не привязана |
| M-002 | заявка не выбрана | реальная партия, селектор на «Без привязки» | кнопка неактивна, рядом: «Выберите свою заявку: предложение уходит в конкретную закупку» | — |
| M-003 | одна заявка | список прочитан, в нём ровно одна заявка | она выбрана заранее, отправка доступна | — |
| M-004 | заявок нет | список прочитан и пуст | «Сначала создайте заявку на закупку — предложение уходит в неё» + кнопка перехода к созданию | — |
| M-005 | несколько заявок | список прочитан, в нём две и более | ни одна не выбрана сама, до выбора действует `M-002` | — |
| M-006 | демо · партия | демо-партия (идентификатор не UUID), селектор в любом состоянии, включая «Без привязки» | демо-путь как сегодня, включая окно «Сделка состоялась» | — |
| M-007 | смешанная пара | реальная партия + демо-заявка | кнопка неактивна, рядом: «Эта заявка демонстрационная — выберите настоящую» | — |
| M-008 | цена ниже минимума | реальная партия, заявка выбрана, цена < минимума | подсказка под полем — как сегодня; отправку не блокирует, база отказывает своей строкой | отказ показан как в `M-010` |
| M-009 | повторное нажатие | оператор нажал отправку дважды | второе нажатие не создаёт вторую привязку | — |
| M-010 | отказ привязки | нет места в заявке / цена ниже ask фермера | показана строка отказа из базы дословно, экран не объявляет сделку | база отдаёт одну строку на обе причины (`ALLOC_FAILED: … нет места / цена < ask`) — различение причин вне скоупа |
| M-011 | «Сделка состоялась» | реальная партия на любом пути слайса | окно с раскрытыми контактами и оценкой не достигается | — |
| M-012 | список грузится | реальная партия, ответ по заявкам ещё не пришёл | видно, что заявки грузятся; отправка недоступна; демо-заявки в селекторе не показаны | — |
| M-013 | список не прочитан | чтение заявок не удалось | сказано, что список не загрузился (не «заявок нет»); отправка недоступна; демо-заявки не подставлены | повторная попытка доступна |

</frozen-after-approval>

## Design contract (сигнатуры, НЕ тела)

- **Data model.** Изменений нет: ни таблиц, ни колонок, ни FSM (P7, схема FINAL). Читается
  существующий список заявок комбината; «доступная для привязки» = та, что уже приходит в
  `BatchDetailModal` пропсом `pools` (сегодня `MpkApp.tsx:468` фильтрует по статусу «набирается»).
- **API / RPC.** Новых нет. Привязка — задеплоенная `rpc_self_match_batch_to_pool(p_pool_id,
  p_batch_id, p_matched_heads, p_price_per_kg)`, вызывается как сегодня (`MpkApp.offerBatch`).
  Сигнатуры существующих не меняются.
- **Events.** Слайс событий не добавляет. Известный разрыв (старше слайса, дом — ARS-98):
  self-serve путь пишет `batch_events`, `platform_events` не эмитит.
- **UI contract.** `BatchDetailModal` (`mpk/modals/`), проводка `MpkApp.tsx:464-475`.
  Состояния кнопки отправки: доступна · недоступна с причиной (`M-002`, `M-007`, `M-008`) ·
  отправка идёт (`M-009`). Отдельное состояние «нет заявок» (`M-004`). Ветка `onOffer` и модалка
  `DealClosedModal` перестают быть достижимы для реальных данных (`M-011`); удаление самого
  компонента в скоуп не входит — он остаётся домом демо-пути (`FR-007`). Персона — оператор
  мясокомбината. Зона `/mpk` = Ionic-оболочка закупок: дизайн-система не меняется, новых экранов
  нет, топбар не затрагивается.

## Slices → Tasks

| задача | тир | закрывает id | приёмка (глазами оператора комбината) |
|--------|-----|--------------|----------------------------------------|
| UI · привязка уходит только в настоящую заявку; выдуманный путь недостижим для реальных данных | mechanical | FR-001,005,006,007,013,014 · M-001,006,011 | Выбрал свою заявку, отправил — партия действительно в ней, вижу её в мониторе. Хозяйства, которого нет в базе, нигде не появляется |
| UI · причина недоступности отправки | mechanical | FR-002,008 · M-002,007,008 | Когда отправить нельзя — вижу текстом, почему именно |
| UI · состояния списка заявок: грузится · пусто · не прочитан · одна · несколько | mechanical | FR-003,004,009 · M-003,004,005,012,013 | Заявка одна — не выбираю руками; ни одной — знаю, что создать; список не загрузился — вижу это, а не «заявок нет» |
| UI · защита от двойной отправки и показ отказа базы | mechanical | FR-005 · M-009,010 | Повторное нажатие не создаёт вторую привязку; отказ базы виден текстом |
| Docs · `DECISIONS_LOG` + решения владельца 11.09 (дробление принято как есть) | mechanical | FR-010,011,012 · — | — |

## Verification (G3 inputs)

`cross_check.sh` · `tsc -b` + build · `npm run test:routers` — браузерный тест
`BatchDetailModal` на реальном Chromium, **дом машинного покрытия всех 13 строк матрицы**
(образец — `src/tests/mpk-pool-monitor.browser.test.tsx`, ARS-684; id называются с префиксом
`ARS-687 M-NNN` из-за коллизии пространства id, `IMPL_DEBT` `QA-CASE-ID-NAMESPACE-01`) ·
converge intent↔reality по этому спеку.

**Про QA-реестр — сверено, а не предположено:** в `qa/scenarios/06-tsp-mpk.md` **нет** сценария на
привязку партии с маркет-борда к своей заявке. Группа `TSPM-OFF` — это «входящие предложения
(Offer)», то есть broadcast фермер→комбинат, который `FR-010`/`FR-011` прямо выносят из скоупа;
ссылаться на неё было бы обещанием несуществующего покрытия. Новый `@case` в этом слайсе не
заводится (дал бы сироту `check_coverage.sh`); заведение сценария группы `TSPM-*` на матч с
маркет-борда — отдельная задача QA-реестра.

SQL не меняется: тела RPC не трогаем (`FR-012`, `FR-013`), поэтому деплой — только фронт
(Vercel с `main`), `prod_diff.py` не требуется. **Отдельно проверяется руками после выкладки**
(живой заход оператора, как в ARS-684 не состоялся): реальная партия + реальная заявка →
строка появилась в мониторе именно этой заявки.

## Open Questions

> Пусто — открытых intent-gap нет.

## Clarifications  (append-only)

- [2026-09-11] Q: что должно происходить при невыбранной заявке — запретить отправку, оградить
  кнопку демо-данными или завести направление «предложение комбината фермеру»?
  → A (владелец): **обязать выбрать заявку**; если подходящая одна — подставить её саму
  · отвергнуто: оградить по образцу ARS-684 (оператор теряет действие без объяснения);
  завести направление комбинат→фермер (в модели его нет вовсе — нужна схема, RPC, извещение
  фермеру и отдельное решение по ст. 171; отдельная большая работа).
- [2026-09-11] Q: чинить это внутри ARS-684? → A: нет, отдельная задача ARS-687 со своим G2/G3
  · отвергнуто: дописать в ARS-684 — его frozen-блок (`FR-013`) прямо исключает этот экран, а
  слайс закрыт и выложен; расширение замороженного блока задним числом.
- [2026-09-11, ревью качества спека] Q: задеплоенная `rpc_self_match_batch_to_pool` зовёт
  `fn_tsp_alloc_chunk`, то есть привязка создаёт **кусок** — механику дробления, отключённую
  решением CEO 26.07 (откат переопределил три другие self-serve функции, эту не тронул). Принять,
  приостановить слайс или привязывать партию целиком? → A (владелец, 11.09): **принять как есть и
  записать явно** (`FR-005`, `FR-012`) · отвергнуто: приостановить ARS-687 до разбора с дроблением
  (починка фальшивой сделки важнее и от дробления не зависит); привязывать партию целиком —
  правка задеплоенной RPC, скоуп вырос бы с UI до SQL и задел дом ARS-314.

## Assumptions  (append-only, ВНЕ frozen)

- [2026-09-11] «Доступная для привязки заявка» = то, что уже приходит в модалку пропсом `pools`
  (сегодня фильтр по статусу «набирается», `MpkApp.tsx:468`). Слайс правило отбора не меняет.
  Опровергнет: решение расширить привязку на заявки других статусов.
- [2026-09-11] Причина недоступности показывается рядом с кнопкой тем же приёмом, что уже
  применён для минимальной цены (`mpk-error-hint`, `BatchDetailModal.tsx:103`) — новых
  компонентов и правил дизайна не вводится. Опровергнет: требование отдельного вида подсказки.
- [2026-09-11] `DealClosedModal` остаётся в коде как дом демо-пути (`FR-007`), а не удаляется:
  удаление сломало бы демо-сценарий, который владелец сохраняет. Опровергнет: решение отказаться
  от демо-пути вовсе.
- [2026-09-11, после ревью качества] Минимальная цена **не** становится условием отправки.
  Проверено по коду: сегодня `aboveMin` читается только в оформлении поля
  (`BatchDetailModal.tsx:95`) и подсказке (`:101`), а `send()` гейтит лишь «цена > 0» (`:44`) —
  цена ниже минимума уходит в базу, и база отказывает своей строкой. Делать её блокировкой —
  новое поведение, которого слайс не просил; сохраняем как есть (`FR-008`). Опровергнет: решение
  владельца блокировать отправку ниже ask фермера на клиенте.
- [2026-09-11, после ревью качества] Демо-путь опознаётся по **партии**, а не по паре
  «партия + заявка»: сегодня демо проходит именно через «Без привязки» (`BatchDetailModal.tsx:47`
  не выполняется → `:61-73`), поэтому требование пары сломало бы демо-сценарий. Опровергнет:
  появление демо-партий с UUID либо отказ от демо-данных в закупках.
- [2026-09-11, после ревью качества] «Демонстрационная заявка» в тексте для оператора — это то,
  что в коде опознаётся как не-UUID. Слово «seed» оператору не показывается. Опровергнет:
  появление у демо-заявок настоящих идентификаторов.

## Implementation Notes  (append-only)

## Spec Change Log  (append-only)

## Review Triage Log  (append-only)

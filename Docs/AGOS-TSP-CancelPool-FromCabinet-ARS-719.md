---
status: agreed           # гейт замысла проведён 2026-09-17; подпись g2_approved ставит человек
g2_approved:             # ставит ТОЛЬКО человек: scripts/approve-g2.sh <спек>. Пусто = не одобрено
baseline_commit:         # пусто до /build
brain_spec: "[[projects/agos/specs/tsp-farmer-sell-flow]]"
task_home: ARS-719 (эпик ARS-94 · Рынок TSP); разрез — ARS-727 (C-1), ARS-728 (C-2), ARS-729 (C-3); соседи — ARS-314 (функции отмены, shipped), ARS-695 (точка выбора + хелпер освобождения, shipped), ARS-718 `FR-017` (десктоп отмену не строит), ARS-98 (ретайр uuid-пайплайна + остальные семь anon-открытых функций)
---

# Отмена заявки из кабинета МПК — кнопка доведена до работы, которая уже написана

<frozen-after-approval>

## Intent

Комбинат отменяет заявку из кабинета — и заявка только меняет статус. Партии фермеров
остаются проданными заявке, которой больше нет: у фермера партия «Продана», отгружать
нечего, вернуть её в продажу может только оператор ассоциации из админки. Сам комбинат
такого хода не имеет вовсе.

Работа, которая освобождает партии обоих маршрутов, написана и проверена дважды —
`fn_tsp_pool_release_matches` (ARS-695, ею живут возврат при недоборе и автоподметание) и
`rpc_cancel_pool` (ARS-314). Не хватает **проводки кнопки**: единственная кнопка отмены
ведёт в `rpc_self_advance_pool_status`, а та по конструкции умеет только менять статус и до
`cancelled` дотянуться не может — этого значения нет в её белом списке.

Слайс заводит `rpc_self_cancel_pool` — прямого брата `rpc_self_pool_return_batches`, с тем
же гейтом и тем же освобождением, но из `filling` и в `cancelled`. И закрывает то, что
вскрыл замер попутно: канон-функция `rpc_cancel_pool` исполнима ролью `anon` и сверяет
организацию, **которую назвал сам вызывающий**, а не ту, к которой он относится.

## Boundaries & Constraints

**Always**
- **FR-001** Отмена освобождает партии **обоих** маршрутов одним ходом, атомарно: куски
  (`batch_allocations`) и привязанные целиком (`batches.pool_line_id`).
- **FR-002** Отмена доступна только владельцу заявки. Гейт — `pool_requests.organization_id
  = any(fn_my_org_ids())`: ровно он стоит у прямого брата `rpc_self_pool_return_batches` и
  ещё у шести из восьми соседей `rpc_self_*` (исключение — `rpc_self_match_batch_to_pool`,
  она гейтится через `pools.organization_id`). `organization_id` от клиента не принимается
  **вовсе** — такого параметра нет в сигнатуре.
- **FR-003** Отменённая заявка получает канон-статус `cancelled`, а не легаси `closed`.
- **FR-004** Счётчики обнуляются вместе с освобождением: `pools.matched_heads`,
  `pool_lines.current_heads`, `pool_lines.current_volume_kg`.
- **FR-005** Порядок проверок статуса фиксирован и именно такой: **сначала** «уже
  `cancelled` → вернуть 0 без ошибки» (идемпотентность), **потом** «не `filling` →
  `INVALID_STATUS`». Обратный порядок сделал бы повторную отмену ошибкой, а она не ошибка.
- **FR-006** У фермера на возвращённой партии снова есть ход: без живых кусков →
  `published`, с остатком → `partially_matched`; терминальный статус куска сохраняется.
- **FR-007** `rpc_cancel_pool` перестаёт быть исполнимой ролями `public` и `anon` и получает
  в теле тот же guard, которым закрыли пятерых batch-братьев (`SEC-RPC-ORGTRUST-01`):
  `p_organization_id = any(fn_my_org_ids()) or fn_is_admin() or auth.role() = 'service_role'`,
  иначе `FORBIDDEN`. Оттуда же урок про гранты: `revoke from anon` без `from public` — no-op,
  PUBLIC наследуется обеими ролями.
- **FR-008** Guard не ломает существующих вызывающих. Единственный прямой вызов
  `rpc_cancel_pool` в репо — `tests/ars_314_cancel_pool_chunks_test.sql:142`; после слайса он
  обязан остаться зелёным (через `service_role`-ветку guard, как у batch-братьев). Живых
  вызовов из `src/` у функции нет — проверено; админ-консоль зовёт `rpc_admin_cancel_pool`.
- **FR-009** Кнопка остаётся ровно там, где стоит, — в состоянии `filling` монитора заявки,
  и слайс не рисует новых **экранов**: ветка «Заявка отменена, партии возвращены
  поставщикам» уже построена (`PoolMonitorModal.tsx:661`, ARS-695). Диалог подтверждения
  `FR-016` экраном в этом смысле не считается.
- **FR-010** Пока вызов летит, кнопка показывает «Отменяем…» и не принимает второго нажатия
  — как соседняя «Закрыть заявку» (`PoolMonitorModal.tsx`, состояние `closing`). Двойной
  клик не должен опираться на идемпотентность базы: она страхует, но не оправдывает экран,
  который молчит.
- **FR-016** Отмена идёт через подтверждение: модалка называет последствие («партии
  вернутся поставщикам») и несёт **необязательное** поле причины. Причина уходит в
  `p_reason` и оседает в журнале событий заявки; пустая причина — законный ввод, отмену не
  блокирует. Форма — та же, что у `CancelPoolBtn` в `/admin/marketplace`
  (`MarketplaceAdmin.tsx:259`): один и тот же ход не должен выглядеть в двух консолях
  по-разному.

**Never**
> Строки этого блока снаружи не наблюдаемы — они запрещают, а не производят. Проверяются
> отсутствием изменений в диффе, а не строкой матрицы; исключение — `FR-014`, у него
> проверка названа в §Verification.

- **FR-011** Не трогает сигнатуры существующих RPC (P7): `rpc_cancel_pool`,
  `rpc_self_advance_pool_status`, `fn_tsp_pool_release_matches` остаются как есть.
- **FR-012** Не строит отмену на десктопе — это граница ARS-718 (`FR-017`).
- **FR-013** Не переводит адаптер на канонические статусы (ARS-314/ARS-98) и **не ретайрит
  остальные семь anon-открытых функций uuid-пайплайна** — их дом ARS-98.
- **FR-014** Кабинет не переводит боевую заявку в `closed` через
  `rpc_self_advance_pool_status`: после слайса туда не ведёт ни один живой ход.
- **FR-015** Не чинит счётчик тонн (ARS-692) и не трогает `pool_requests.status`.

## I/O & Edge-Case Matrix

| id | сценарий | вход / состояние | ожидаемое | обработка ошибки |
|----|----------|------------------|-----------|------------------|
| M-001 | happy path | `filling`, 3 партии: 2 куска + 1 целиком | все три в продаже, счётчики 0, заявка `cancelled` | — |
| M-002 | пусто | `filling`, ни одной партии | заявка `cancelled`, освобождать нечего | — |
| M-003 | чужая заявка | вызов из другой организации | `FORBIDDEN`, ни одной записи не изменено | тост «нет доступа» |
| M-004 | анонимный вызов | роль `anon` напрямую в PostgREST | отказ на уровне гранта | — |
| M-005 | повторная отмена | заявка уже `cancelled` | 0 освобождённых, не ошибка | — |
| M-006 | неверный статус | заявка `closed_filled` / `executing` | `INVALID_STATUS`; на экране кнопки нет | тост с причиной |
| M-007 | частично доставлена | партия из 2 кусков, 1 `delivered` | доставленный не воскресает, партия → `partially_matched` | — |
| M-008 | отказ RPC | сеть/БД вернули ошибку | локальный патч откачен, заявка на экране осталась `filling` | тост «Не удалось отменить: …» |
| M-009 | офлайн | запрос не ушёл | та же ветка, что M-008 | тот же тост |
| M-010 | гонка | два сотрудника одной орг. жмут одновременно | второй получает идемпотентный ноль | без ошибки |
| M-011 | фермер смотрит партию | отмена во время открытого экрана | после перечита партия «В продаже»; вернулись ходы `published`-партии — снять с продажи и снизить цену | — |
| M-012 | передумал | диалог подтверждения закрыт без нажатия | ни одной записи не изменено | — |
| M-013 | двойной клик | оператор жмёт «Отменить» дважды подряд | второе нажатие не уходит: кнопка в состоянии «Отменяем…» и заблокирована | — |

</frozen-after-approval>

## Design contract (сигнатуры, НЕ тела)

- **Data model.** Ни одной новой таблицы или колонки. FSM `pools`: `filling → cancelled` —
  значение уже в боевом `pools_status_check` (замер 17.09). Авторитет — владелец заявки
  (МПК). История: `batch_events` + `platform_events`, current-only для `pools`.
- **API / RPC.** Новая, аддитивно:
  `rpc_self_cancel_pool(p_pool_id uuid, p_reason text default null) returns int`
  — `SECURITY DEFINER`, `set search_path = public, pg_temp`; гейт `FR-002`; предусловие
  `pools.status = 'filling'` (иначе `INVALID_STATUS`); освобождение делегируется
  **существующей** `fn_tsp_pool_release_matches(p_pool_id)` — той же, которой живут
  `rpc_self_pool_return_batches` и `rpc_self_close_due_pools` (P4: один дом освобождения);
  возвращает число освобождённых партий. Гранты: `revoke ... from public, anon` +
  `grant execute to authenticated` — норма всех восьми соседей. Регистрация в
  `rpc_name_registry` (D-NEW-A).
  Плюс к `rpc_cancel_pool` — `FR-007` (guard в теле + revoke), сигнатура не меняется.
- **Events.** `market.pool.cancelled` эмитит сама `rpc_self_cancel_pool`: замер показал, что
  `fn_tsp_pool_release_matches` пул-уровневого события не пишет. Отзыв висящих офферов и
  `market.offer.withdrawn` хелпер делает сам — дублировать не нужно.
- **UI contract.** `PoolMonitorModal.tsx:393` — кнопка открывает модалку подтверждения
  (`FR-016`) и по подтверждению зовёт новый `onCancelPool(poolId, reason)` вместо
  `applyStatus({ status: 'closed' })`. `MpkApp.tsx` — новый `cancelPool` →
  `supabase.rpc('rpc_self_cancel_pool', { p_pool_id, p_reason })` + `refetchPools()`.
  Состояния: в процессе → `FR-010`; успех → экран закрывается, список перечитан; отказ →
  `M-008`. Экран отменённой заявки уже существует, новый не рисуем (`FR-009`).

## Slices → Tasks

| задача | тир | закрывает id | приёмка глазами закупщика и фермера |
|--------|-----|--------------|--------------------------------------|
| C-1 (ARS-727) · `rpc_self_cancel_pool` + гранты + registry | semantic | FR-001..006, FR-011, M-001..003, M-005..007, M-010 | Комбинат отменил заявку — партии фермеров в продаже, у фермера снова есть ход |
| C-2 (ARS-728) · Запереть `rpc_cancel_pool` (guard + revoke public/anon) | semantic | FR-007, FR-008, M-004 | Отменить чужую заявку снаружи нельзя, а проверка ARS-314 осталась зелёной |
| C-3 (ARS-729) · Проводка кнопки в кабинете + модалка подтверждения | mechanical | FR-009, FR-010, FR-014, FR-016, M-008, M-009, M-011..013 | Кнопка «Отменить заявку» предупреждает о последствии и делает то, что обещает; при сбое экран не врёт |

## Verification (G3 inputs)

- **SQL-тест** `tests/` по образцу `ars_314_cancel_pool_chunks_test.sql`, на боевой базе с
  откатом: `M-001`, `M-002`, `M-003`, `M-005`, `M-006`, `M-007`, `M-010`.
- **`M-004`** — `has_function_privilege('anon', …, 'EXECUTE')` по обеим функциям до и после:
  ожидание `false` у `rpc_cancel_pool` и у новой `rpc_self_cancel_pool`.
- **`FR-008`** — прогон существующего `tests/ars_314_cancel_pool_chunks_test.sql`: зелёный.
- **`FR-014`** — `grep -rn "status: 'closed'" src/pages/cabinet/shell/mpk/` не находит ни
  одного пути, ведущего в `rpc_self_advance_pool_status` у боевой заявки. Так выглядит его
  провал: найденная строка = требование нарушено.
- **UI-края `M-008`, `M-009`, `M-011`, `M-012`, `M-013`** — preview монитора заявки:
  отказ RPC (сеть отключена), офлайн, экран фермера после перечита, закрытие диалога без
  подтверждения, двойной клик. Каждый край проверяется явно, по id.
- **Общее:** `cross_check.sh` (вкл. CHECK 11) · `tsc -b` + build фронта · QA-сценарии
  `qa/scenarios/05-tsp-farmer.md` (TSPF-LIFE) через `/qa-run` · `prod_diff.py` после выкладки
  (дрейф 0) · converge intent↔reality по `FR-`/`M-`.

## Open Questions

> Спек не покидает `draft`, пока жив вопрос.

- Нет живых. Единственный вопрос гейта закрыт 2026-09-17 и переехал в `Clarifications`
  решением `FR-016`.

## Clarifications  (append-only)

- [2026-09-17] Q: спрашивать ли подтверждение перед отменой и брать ли причину? → A:
  **диалог с подтверждением и необязательным полем причины**, форма как у `CancelPoolBtn` в
  `/admin/marketplace` (`FR-016`) · отвергнуто: только подтверждение без причины — дешевле на
  одно поле, но не оставляет следа «почему», а фермер спросит; мгновенная отмена без вопроса
  — до слайса была почти безобидна (кнопка ничего не освобождала), после него один промах
  мыши возвращает партии всех фермеров заявки без права отыграть назад.

- [2026-09-17] Q: кабинет зовёт отмену новой self-обёрткой или канон-функцией
  `rpc_cancel_pool` напрямую? → A: **новой обёрткой** `rpc_self_cancel_pool` с гейтом
  `fn_my_org_ids()` (`FR-002`) · отвергнуто: звать `rpc_cancel_pool` напрямую — замер
  показал, что у неё нет гейта вызывающего, она сверяет организацию, которую назвал сам
  клиент, и открыта роли `anon`; проводка кабинета к ней вписала бы класс
  `SEC-RPC-ORGTRUST-01` в живой экран. Плюс канон торгового слоя по `D-TSP-CANON-01` —
  self-serve адаптер, а не uuid-пайплайн.
- [2026-09-17] Q: дыра в грантах `rpc_cancel_pool` — чинится здесь или отдельным
  SEC-тикетом? → A: **здесь** (`FR-007`), потому что слайс эту функцию и так вскрывает, а
  живых вызовов в `src/` у неё нет · отвергнуто: отдельный тикет — он бы стал вторым домом
  того же факта (P4) при живом ARS-98. Остальные семь anon-открытых функций кластера сюда
  НЕ входят (`FR-013`), их дом — ARS-98, факты туда дозаписаны 17.09.
- [2026-09-17] Q: освобождение делегировать канон-хелперу `fn_tsp_release_pool_allocations`
  (ARS-314) или self-serve `fn_tsp_pool_release_matches` (ARS-695)? → A: **ARS-695**, она
  уже несёт возврат при недоборе и автоподметание, умеет оба маршрута, чинит `matched_heads`
  и `current_heads` и заперта на `service_role` · отвергнуто: канон-хелпер — он живёт на той
  стороне, которую `D-TSP-CANON-01` отправил в отставку.

## Assumptions  (append-only, ВНЕ frozen)

- [2026-09-17] След события у фермера останется тем, что пишет `fn_tsp_pool_release_matches`
  — отдельного «партия вернулась, потому что заявку отменили» в `batch_events` не будет
  (у хелпера нет параметра причины, а расширять его сигнатуру — не аддитивно). Почему так:
  фермерский кабинет `batch_events` не читает **нигде** (проверено по `src/pages/cabinet/`),
  то есть разницы пользователь сегодня не увидит. Опровергнет: экран истории партии у
  фермера или уведомление, которое цитирует причину возврата.
- [2026-09-17] `'closed'` остаётся в `REAL_STATUSES` (`PoolMonitorModal.tsx:107`) — после
  `FR-014` туда не ведёт ни один живой ход, но удаление значения — правка сверх необходимой
  (HS-5). Второе место с `applyStatus({ status: 'closed' })` (строка 477) проверено: ветка
  `expired` для боевой заявки недостижима, `'expired'` в `REAL_STATUSES` нет. Опровергнет:
  новая кнопка в будущем слайсе, ставящая `'closed'` боевой заявке.

## Implementation Notes  (append-only)

## Spec Change Log  (append-only)

## Review Triage Log  (append-only)

---
status: agreed           # гейт замысла проведён 2026-09-10 (G2-карточка: 5 находок ревью закрыты правкой, 1 Open Question отвечен владельцем → Clarifications); код (/build) — после подписи g2_approved
g2_approved: 2026-09-10 · Dias Zhagaparov
baseline_commit: 29da494c36bcdbc77ce389e355452518cd31729c   # /build ARS-684, зафиксирован до первой правки 2026-09-10
brain_spec: "[[projects/agos/specs/tsp-farmer-sell-flow]]"
task_home: ARS-684 (эпик ARS-94 · Рынок TSP); корень-сосед ARS-314, конвергенция слоёв ARS-98
---

# Монитор пула — двухмаршрутная read-model — комбинат снова видит своих поставщиков

<frozen-after-approval>

## Intent

С 26 июля оператор мясокомбината открывает набранный пул и видит «заполнен, контакты
раскрыты» — над **пустым списком поставщиков**: запись вернулась в модель «партия целиком
привязана к строке пула», а читающая функция монитора осталась в модели кусков. Данные о
сделке в базе есть — их некому показать. Отсюда всё остальное: комбинат не узнаёт, у кого
купил (хотя фермер своего покупателя видит), не может подтвердить приёмку и оставить отзыв,
а без приёмки недостижима и репутация обеих сторон.

Слайс возвращает монитору зрение — он читает **обе** формы связи — и делает приёмку и отзыв
достижимыми на обоих маршрутах. Дробление партий при этом **не включается**: решение CEO от
26.07 в силе, монитор лишь читает записанное. Разбор корня и доказательства — ARS-684.

## Boundaries & Constraints

**Always**
- **FR-001** Монитор показывает поставщиков для обоих маршрутов набора: есть распределения
  (`batch_allocations`) — по ним; нет — по привязке партии к строке пула
  (`batches.pool_line_id → pool_lines`). Образец приёма — `fn_ars_360_mpk_counterparty_ids`.
- **FR-002** Каждая строка несёт признак своего маршрута, чтобы приёмка адресовала кусок или
  партию, не угадывая.
- **FR-003** После раскрытия контактов пула комбинат видит название хозяйства и телефон в каждой
  строке — на обоих маршрутах (симметрия `D-M6-12`; сегодня фермер покупателя видит, комбинат
  фермера — нет).
- **FR-004** До раскрытия строки видны, хозяйство анонимно. Гейт `pools.mpk_contact_revealed_at`
  не ослабляется (`D-M6-5/12`, ст. 171).
- **FR-005** Приёмка достижима на обоих маршрутах; после неё строка показывает состояние из базы,
  а не локально угаданное.
- **FR-006** Отзыв комбината достижим после приёмки и подаётся канонически
  (`rpc_submit_deal_review`, уже двухмаршрутный). Тем самым достижима и взаимная репутация
  (`rpc_get_mpk_reputation`) — отдельной работы не требует.
- **FR-007** Управляющие элементы монитора у реального пула меняют только то, что реально
  меняется в базе. Дорисовывающие поставщиков, прогресс или сроки — доступны исключительно у
  демо-пула (seed, идентификатор не UUID), по образцу уже ограждённой кнопки «Демо: фермер
  отгрузил».
- **FR-008** Сигнатура `rpc_get_pool_matches(uuid)` не меняется; поля ответа только добавляются;
  существующая проекция `myRating` (ARS-360 / ADR-353, retire-дата 2026-10-31) сохраняется как есть
  (P7). *Снаружи не наблюдаемо — проверяется контракт-снапшотом в §Verification.*
- **FR-009** Комбинат видит строки только тех пулов, что принадлежат его организации; данные
  чужого пула не отдаются ни на одном маршруте.
- **FR-015** В строке поставщика показывается собственная прошлая оценка этого хозяйства
  (`myRating`, уже в ответе), а при её отсутствии — прочерк. Одинаковая константа рейтинга у всех
  хозяйств из монитора уходит.

**Never**
- **FR-010** Слайс **не включает дробление партий обратно** — решение CEO 26.07
  (`TSP-SLICE9-ROLLBACK-01`) в силе. Read-model только читает обе уже существующие формы записи.
- **FR-011** Слайс не трогает 6 канон-функций `d02_tsp.sql` (дом — ARS-314) и наследует его guard:
  `deploy.py --files d02_tsp.sql` и `--all` не запускаются. Деплой слайса — одна миграция поверх.
- **FR-012** Слайс не заводит новых таблиц, колонок и RPC (речь о схеме БД; поля *ответа*
  read-model добавляются — см. FR-008): обе функции приёмки
  (`rpc_self_confirm_delivery_alloc`, `rpc_self_confirm_delivery`) уже задеплоены.
- **FR-013** Слайс не чинит «фальшивую сделку» в `BatchDetailModal.tsx:61-73` (другой экран),
  не реализует выход из недобравшегося пула (`TSP-FLOW-10`), уведомления, планировщик и живые
  цены — каждое остаётся отдельной задачей.
- **FR-014** Слайс не регистрирует семейство `rpc_self_*` в `rpc_name_registry` и Dok 3 — это
  явный скоуп ARS-98 (Slice D).

## I/O & Edge-Case Matrix

| id | сценарий | вход / состояние | ожидаемое | обработка ошибки |
|----|----------|------------------|-----------|------------------|
| M-001 | happy · автоматч | пул `closed_filled`, контакты раскрыты, партии связаны через `pool_line_id`, распределений нет | список: строка на партию, название хозяйства и телефон видны | — |
| M-002 | happy · ручной матч | пул набран `rpc_self_match_batch_to_pool`, распределения есть | список как сегодня, приёмка по кускам не сломана | — |
| M-003 | смешанный пул | часть партий через распределения, часть через `pool_line_id` | видны обе группы, каждая строка со своим маршрутом, дублей нет | — |
| M-004 | до раскрытия | пул `filling`, `mpk_contact_revealed_at` пуст | строки видны, вместо хозяйства «аноним», телефона нет | — |
| M-005 | пусто | пул без единой набранной партии | пустой список + честная подпись «поставщиков пока нет» | — |
| M-006 | приёмка партии | строка маршрута «партия», статус «в пути» | приёмка проходит, строка → «Принята»; когда принята последняя партия пула, пул → `completed` | RPC отказал → строка не меняется, показана причина |
| M-007 | приёмка куска | строка маршрута «кусок», статус «в пути» | как сегодня, поведение не меняется | как сегодня |
| M-008 | приёмка не в том статусе | партия ещё не отгружена фермером | действия приёмки на строке нет | `INVALID_STATUS` → сообщение оператору, состояние не меняется |
| M-009 | чужой пул | оператор другой организации | строки не отдаются | `FORBIDDEN` → сообщение, список пуст |
| M-010 | отзыв | партия принята (`delivered`) | отзыв подаётся, оценка сохраняется и видна при переоткрытии | повторная подача → `REVIEW_ALREADY_SUBMITTED`, сообщение |
| M-011 | отзыв рано | партия ещё не принята | действия отзыва на строке нет | — |
| M-012 | демо-пул | seed-пул, идентификатор не UUID | демо-элементы доступны | — |
| M-013 | ошибка чтения / офлайн | RPC не ответил (первый вызов или очередной опрос) | ранее показанный список остаётся на экране и не подменяется пустым; показано сообщение «не удалось обновить список» | поллинг продолжается, следующий успешный ответ снимает сообщение |
| M-014 | снятая партия | партия снята с продажи / кусок `cancelled` | строки в списке нет, счётчик её не считает | — |
| M-015 | реальный пул | пул с UUID | демо-элементов нет вовсе; прогресс и сроки берутся из базы | — |
| M-016 | первая загрузка | модалка открыта, ответа ещё нет | видно, что список грузится; демо-поставщики seed-пула в реальный пул не подставляются ни на миг | ответ не пришёл → `M-013` |
| M-017 | повторная приёмка | оператор нажал приёмку дважды / партия уже принята | второе нажатие ничего не меняет, строка остаётся «Принята» | `INVALID_STATUS` не показывается оператору как ошибка — состояние уже целевое |
| M-018 | рейтинг хозяйства | комбинат уже оценивал это хозяйство / ещё нет | своя прошлая оценка / прочерк; одинаковой звезды у всех строк нет | — |

</frozen-after-approval>

## Design contract (сигнатуры, НЕ тела)

- **Data model.** Изменений нет: ни таблиц, ни колонок, ни FSM (P7, схема FINAL). Читаются
  `batch_allocations` и цепочка `batches.pool_line_id → pool_lines → pools`. Авторитет при
  конфликте (P2): есть распределения — источник они, `pool_line_id` = легаси-хвост (первый кусок);
  тот же порядок, что в `fn_ars_360_mpk_counterparty_ids`.
- **API / RPC.** `rpc_get_pool_matches(p_pool_id uuid) → jsonb` — сигнатура прежняя, строка ответа
  дополняется признаком маршрута (`source`: кусок либо партия), `matchId` остаётся уникальным
  ключом строки, существующие поля (вкл. `myRating`) сохраняются. Приёмка — задеплоенные
  `rpc_self_confirm_delivery_alloc(uuid)` (кусок) и `rpc_self_confirm_delivery(uuid)` (партия;
  гейт «пул моей организации» через `pool_line→pool`, `dispatched→delivered`, rollup пула в
  `completed`). Отзыв — `rpc_submit_deal_review` без правок. Новых RPC нет.
- **Events.** Слайс событий не добавляет. Известный разрыв (старше слайса, дом — ARS-98 / Dok 4):
  self-serve путь пишет только `batch_events`, `platform_events` не эмитит.
- **UI contract.** `PoolMonitorModal` (`mpk/modals/`), загрузка `mpk/data/pools-load.ts`, проводка
  `mpk/MpkApp.tsx`. Строка выбирает функцию приёмки по признаку маршрута. Состояния: пусто
  (`M-005`), ошибка/офлайн (`M-013`), загрузка (`M-016`), до раскрытия (`M-004`). Персона —
  оператор мясокомбината. Зона `/mpk` = Ionic-оболочка закупок: дизайн-система не меняется, новых
  экранов нет, топбар не затрагивается.

## Slices → Tasks

| задача | тир | закрывает id | приёмка (глазами оператора комбината) |
|--------|-----|--------------|----------------------------------------|
| DB · двухмаршрутный fallback в `rpc_get_pool_matches` (одна миграция поверх) | mechanical | FR-001,002,003,004,008,009 · M-001..005, M-014 | Открываю набранный пул — вижу, у кого купил, с названием и телефоном |
| UI · приёмка и отзыв по маршруту строки | mechanical | FR-005,006 · M-006..011, M-013, M-017 | Подтверждаю приёмку и оставляю отзыв прямо в мониторе |
| UI · убрать из монитора выдуманное: демо-элементы и константу рейтинга | mechanical | FR-007, FR-015 · M-012, M-015, M-016, M-018 | В реальном пуле нет ни кнопок, дорисовывающих поставщиков и сроки, ни одинаковой звезды у всех хозяйств |
| Docs · контракт read-model + `DECISIONS_LOG` | mechanical | FR-008 · — | — (D-RPC-CONTRACT-SYNC-01: форма ответа меняется в том же PR, что и доки) |

## Verification (G3 inputs)

`cross_check.sh` · `tsc -b` + build · `npm run test:routers` (браузерные тесты на реальном
Chromium — дом машинного покрытия UI-строк матрицы) · `tests/ars_684_pool_monitor_dual_route_test.sql`
(дом машинного покрытия SQL-строк матрицы и формы ответа) · сценарии `qa/scenarios/` группы TSPM
(монитор/закрытие пула) · после деплоя — `python3 scripts/prod_diff.py` (ожидание: 0 дрейфа) ·
converge intent↔reality по этому спеку.

**FR-008 проверяется SQL-тестом, а не CHECK 11** (правка 2026-09-10, см. `Spec Change Log`):
снапшот `contracts/rpc_return_keys.txt:96` собирается только по `SQL_FILES` из `cross_check.sh:20`,
куда не входят ни ARS-360, ни миграция этого слайса — то есть для `rpc_get_pool_matches` CHECK 11
сверяет июньскую форму (10 ключей) и молча пропускает как уже случившийся дрейф, так и новое поле
`source`. Охват снапшота — чужой скоуп (владелец `cross_check.sh` — qa-agent), заведён отдельной
задачей. Форму ответа (18 ключей, сохранность `myRating` на обоих маршрутах) фиксирует
`tests/ars_684_pool_monitor_dual_route_test.sql`.

## Matrix Test Audit  (сверка ПО ID, не по смыслу)

Прогоны этого захода: SQL — `psql` против живой базы, функция слайса применена в той же
транзакции, которую тест откатывает (`ROLLBACK`, прод не изменён); UI — `npm run test:routers`,
реальный Chromium. Строка «прогнан и прошёл» ниже — из вывода ЭТИХ прогонов, не из отчётов
исполнителей.

Вывод SQL-прогона (дословно): `NOTICE: ARS-684 / Pool Monitor dual-route: контракт
rpc_get_pool_matches пройден. Закрыты M-001 M-002 M-003 M-004 M-005 M-009 M-014, FR-008.` ·
`ROLLBACK`
Вывод UI-прогона: `Test Files 8 passed (8) · Tests 114 passed (114)` (базовая линия до слайса —
7 файлов / 101 тест; +1 файл / +13 тестов).

| id | тест, называющий этот id | прогнан и прошёл |
|----|--------------------------|------------------|
| M-001 | `tests/ars_684_…_test.sql:257-276` | да · SQL |
| M-002 | `…:299-315` (`matchId` = id распределения, сверен на ≠ batchId) | да · SQL |
| M-003 | `…:336-370` (две группы, каждая партия ровно один раз) | да · SQL |
| M-004 | `…:376-403` (аноним на ОБОИХ маршрутах) | да · SQL |
| M-005 | `…:408-411` (`[]`, не null) · `mpk-pool-monitor.browser.test.tsx:147` | да · SQL + UI |
| M-006 | `mpk-pool-monitor.browser.test.tsx:156` | да · UI |
| M-007 | `…:174` (адрес — `id` строки, НЕ `batchId`) | да · UI |
| M-008 | `…:191` | да · UI |
| M-009 | `tests/ars_684_…_test.sql:416-425` (`FORBIDDEN`) | да · SQL |
| M-010 | `mpk-pool-monitor.browser.test.tsx:201` | да · UI |
| M-011 | `…:218` | да · UI |
| M-012 | `…:228` | да · UI |
| M-013 | `…:236` (сбой после успеха) + `…:253` (сбой первым ответом) | да · UI, две строки |
| M-014 | `tests/ars_684_…_test.sql:427-434`, оба маршрута | да · SQL |
| M-015 | `mpk-pool-monitor.browser.test.tsx:262` | да · UI |
| M-016 | `…:271` | да · UI |
| M-017 | `…:286` | да · UI |
| M-018 | `…:305` | да · UI |
| FR-008 | `tests/ars_684_…_test.sql:278-293` и `:317-329` (набор 18 ключей через `EXCEPT`, `myRating` разными оценками по маршрутам) | да · SQL |

**Все 18 строк матрицы имеют прогнанный тест, назвавший свой id.** До этого захода их было 0:
`test:routers` давал 101/101 и до, и после правок кода — то есть ни один существующий тест
изменённое поведение не наблюдал.

Коллизия пространства id обойдена намеренно: `mpk-profile-org-section.browser.test.tsx:49`
держит `@case M-007 M-008 M-018 FR-008` для ЧУЖОГО слайса, поэтому id здесь называются с
префиксом `ARS-684 M-NNN`, а из `@case`-тегов взяты только существующие сценарии
`TSPM-CLOSE-04`/`TSPM-CLOSE-05` (`qa/scenarios/06-tsp-mpk.md`) — сирот `check_coverage.sh` не
получит. Общее решение по пространству id — `IMPL_DEBT` `QA-CASE-ID-NAMESPACE-01`.

Сверх матрицы закрыт **`FR-003` в части телефона** — `mpk-pool-monitor.browser.test.tsx:222`
(телефон в строке + `href="tel:…"` + строка без телефона не ломается). Тест проверен на
несущесть: с убранным рендером телефона он краснеет, то есть наблюдает поведение, а не проходит
вслепую. Тем же приёмом обратной подстановки исполнитель проверил `M-007` (адрес строки),
`M-016` (гейт загрузки) и `M-017` (подавление тоста) — все три упали на неверном ожидании.

**Что машинным покрытием НЕ закрыто** (называю, потому что «не смотрели» ≠ «чисто»):
- **Маршрутизация «имя RPC + имя параметра»** в `MpkApp.confirmDelivery`: браузерный тест
  наблюдает, с чем модалка зовёт `onConfirmDelivery`, но не то, в какую RPC это уходит. Опечатка
  в `p_batch_id` осталась бы незамеченной — `supabase.rpc(name, obj)` не типизирован.
- **Живой заход оператора МПК** не выполнялся: сессии МПК в сессии сборки нет, учётные данные
  ассистент не вводит. Визуальная приёмка глазами — за владельцем.
- **`tests/*.sql` не гоняются автоматически** (`IMPL_DEBT` `QA-SQL-TESTS-NOT-RUN-01`): прогон выше
  сделан вручную и в CI не повторится.

## Open Questions

> Пусто. Вопрос якоря 7 о посылке `FR-015` закрыт владельцем 10.09 («оставить прочерк») —
> вопрос, ответ и проверка сквозного пути отзыва живут в `Clarifications`, разбор находки —
> в `Review Triage Log` (#19). Здесь копии нет намеренно: один факт — один дом (P4).

## Clarifications  (append-only)

- [2026-09-10, якорь 7 · converge] Q: в ветке «Сделка завершена» показывается «сумма ≈ N млн ₸»,
  посчитанная как `filledHeads × 0.45 × avgPrice / 100000 / 10`. Коэффициент `0.45` не приходит ни
  из одной строки ответа, и арифметика печатает величину примерно в 1000 раз меньше действительной
  (100 гол × 450 кг × 1650 ₸ = 74 млн ₸ → «0.1 млн ₸»). Ни один id слайса эту строку не требует
  (`unrequested`). Убрать, починить или оставить? → A (владелец, 10.09): **считать по настоящим
  данным, элемент сохранить** · отвергнуто: удалить строку (оператор потерял бы итог сделки одним
  числом) и оставить как есть (экран продолжал бы утверждать неверную сумму рядом с настоящими
  данными — то же, что убрали константой `4.5`). Итог считается тем же `supplierSum`, которым
  считается «≈ N ₸» в самой строке поставщика: гол × средний вес × цена строки.

- [2026-09-10, якорь 7] Q: `FR-015` называет `myRating` «прошлой оценкой хозяйства», а фактически это
  оценка данной партии — в ветке набора звезда структурно всегда прочерк. Оставить прочерк, убрать
  звезду из ветки набора или заводить агрегат репутации? → A (владелец, 10.09): **оставить прочерк**
  · отвергнуто повторно: агрегат репутации (нужна новая RPC и отдельное решение по ст. 171) и
  удаление элемента. Посылка `FR-015` остаётся неточной — исправлено понимание, не требование:
  звезда показывает оценку ЭТОЙ партии либо прочерк. Проверено при ответе (не обещано): подача
  отзыва доходит до базы и возвращается на экран — `rpc_self_submit_mpk_review` →
  `fn_ars_360_mpk_counterparty_ids` (на маршруте «партия» распределений нет → `pool_line →
  pools.organization_id`) → `rpc_submit_deal_review` пишет `deal_reviews.overall_score` с
  `reviewer_role='mpk'`; чтение `myRating` идёт по `pool_requests.organization_id`, и это тот же id,
  потому что `rpc_self_activate_pool_request:53` заполняет обе колонки из одного `pool_request`.
  Фильтра по `visible_at` в чтении нет — своя оценка видна сразу, чужая не отдаётся.
  **Ограничение, названное владельцу:** `unique (batch_id, reviewer_org_id)` + явная проверка дубля
  делают первый клик окончательным (`REVIEW_ALREADY_SUBMITTED`, после раскрытия —
  `REVIEW_IMMUTABLE_AFTER_REVEAL`); право передумать — отдельное решение, сейчас его нет.

- [2026-09-10] Q: что показывать в звезде рейтинга вместо константы `4.5`
  (`pools-load.ts:106`), которая после починки станет видна оператору у каждого хозяйства?
  → A: собственную прошлую оценку (`myRating`), при её отсутствии — прочерк (`FR-015`, `M-018`)
  · отвергнуто: убрать звезду совсем (потом возвращать элемент); агрегат репутации фермера
  (источника нет, новая RPC и отдельное решение по ст. 171); оставить `4.5` (видимая выдумка
  рядом с настоящими данными — ровно то, что слайс убирает демо-кнопками).

- [2026-09-10] Q: чинить read-model внутри ARS-314 или отдельным слайсом? → A: отдельный слайс
  ARS-684 со своим G2/G3; ARS-314 остаётся про канон `d02` · отвергнуто: подзадача и расширение
  скоупа ARS-314 — разные объекты, разный риск деплоя, чужие acceptance.
- [2026-09-10] Q: входит ли ограждение демо-кнопок монитора? → A: да, обе кнопки монитора
  · отвергнуто: только read-model (кнопка подмешивает фикцию ровно в чинимый список); включение
  «фальшивой сделки» из `BatchDetailModal` (другой экран, слайс заметно шире).
- [2026-09-10] Q: вернуть read-model на `pool_line` вместо fallback? → A: нет, двухмаршрутность
  · отвергнуто: возврат — регрессия, `rpc_self_match_batch_to_pool` откатом не задет и живёт в UI
  (`MpkApp.tsx:295`).

## Assumptions  (append-only, ВНЕ frozen)

- [2026-09-10] Статус строки маршрута «партия» выводится из `batches.status` (`confirmed`→ждёт
  отгрузки, `dispatched`→в пути, `delivered`→принята), по образцу маппинга статусов куска — чтобы
  UI не различал маршруты в отрисовке. Опровергнет: состояние партии, которому в этой шкале нет
  честного места.
- [2026-09-10] Партии в `matched` (пул не закрыт) показываются «активными», как сегодня активные
  куски. Опровергнет: решение скрывать строки до закрытия пула.
- [2026-09-10] Снятые партии исключаются, как исключаются куски `cancelled`. Опровергнет:
  требование показывать снятое историей.
- [2026-09-10] Дом контракта формы ответа `rpc_get_pool_matches` — этот спек: в Dok 3 функции нет
  вовсе, как и всего семейства `rpc_self_*` (регистрация — скоуп ARS-98, FR-014). Опровергнет:
  решение регистрировать read-функции в Dok 3 раньше Slice D.

## Implementation Notes  (append-only)

## Spec Change Log  (append-only)

- [2026-09-10] §Verification: средство проверки FR-008 заменено с CHECK 11 на SQL-тест слайса.
  Причина найдена ДО кода (якорь 6, дирижёр; независимо подтверждена db-агентом): снапшот
  CHECK 11 собирается по `SQL_FILES` (`cross_check.sh:20`), а действующее определение
  `rpc_get_pool_matches` живёт с 2026-07-31 в `20260731074557_ars_360_review_convergence.sql` —
  вне этого списка. Обещание «проверяется контракт-снапшотом» было пустым уже на момент подписи
  G2: снапшот описывает форму июня (без `grade`, `breed`, `*At`, `myRating`), и CHECK 11 на этой
  функции зелен независимо от того, что с ней происходит. Включить файл в `SQL_FILES` здесь нельзя
  без правки чужого инструмента: `rpc_get_pool_matches` оказалась бы определена в двух файлах
  списка и CHECK 1 дал бы CRITICAL, потребовав записи в `DUP_WHITELIST`. `propagated:` frozen-блок
  НЕ менялся — FR-008 как требование в силе, изменён только инструмент его проверки (§Verification
  вне frozen). Охват снапшота маршрутизирован владельцу `cross_check.sh` отдельной задачей.

## Review Triage Log  (append-only)

### Итерация 1 — якорь 7, слепая пара + находки дирижёра (2026-09-10)

Вердикт ставил дирижёр; оценки ревьюеров-субагентов отброшены (у них нет контекста градуировать).
Проверка каждой находки — по месту, с прохождением до вызывающих и гейтов.

**Маршрут `patch` (починено на месте, поверхности не добавлено)**

| # | находка | вердикт · доказательство | что сделано |
|---|---------|--------------------------|-------------|
| 1 | Партия со СНЯТЫМ куском возвращается строкой-партией с полным `heads` — нарушен `M-014` | **high · CONFIRMED.** `fn_tsp_alloc_chunk` пишет `batches.pool_line_id` по первому куску (`20260702160000:203`), rollup при отмене всех кусков выходит раньше и НЕ очищает его («нет активных кусков — оставляем как есть», `20260702190000:78`). Узкий guard `a2.pool_id = p_pool_id and a2.status <> 'cancelled'` такую партию пропускал | guard приведён к образцу: `not exists (… where a2.batch_id = b.id)` — по ВСЕМ распределениям партии, любой пул, любой статус |
| 2 | Read-model и резолвер отзыва выбирают РАЗНЫЕ маршруты для одной партии | **high · CONFIRMED.** `fn_ars_360_mpk_counterparty_ids` считает `count(*)` по всем распределениям, включая `cancelled` и чужие пулы (`d02_tsp.sql:5641`), и лишь потом падает на `pool_line`. Утверждение Design contract «тот же порядок» было **опровергнуто фактом**: у меня был per-pool и без `cancelled`. Следствие: строка показана как партия, а отзыв отказывает `NO_DELIVERED_MPK_COUNTERPARTY` | тем же патчем #1 — порядок авторитета теперь буквально совпадает с образцом |
| 3 | Отзыв на строке-КУСКЕ упрётся в `INVALID_STATUS` | **high · CONFIRMED.** `rpc_submit_deal_review` гейтит `if v_batch.status <> 'delivered'` — по статусу **партии** (`20260731074557:470`). Строка-кусок `delivered` не означает `delivered` у партии. Регрессия введена моей же инструкцией агенту («строка delivered»), тогда как матрица говорит «партия принята» (`M-010`/`M-011`) | звёздная форма в ветке `filled`/`executing` — только при `source === 'batch'`; для кусков отзыв остаётся в ветке `executed`, поведение куска не меняется (`M-007`) |
| 4 | `M-005`: подпись «поставщиков пока нет» только в ветке `filling`, а `closed_filled` маппится в `filled` (`pools-load.ts:44`) — то есть **ровно тот пул, с которого начался слайс**, остался без подписи | **high · CONFIRMED** чтением обеих ветвей | подпись вынесена в общий `matchesEmptyNote` и добавлена в ветку `filled`/`executing` |
| 5 | `M-013` смешан с `M-005`: при сбое чтения показывалось «Поставщиков пока нет» — утверждение о базе, которого мы не знаем | **high · CONFIRMED.** Тот же класс, что `M-005` в ARS-646 («нет данных» ≠ «отказано») | подпись показывается только когда список РЕАЛЬНО прочитан и пуст (`liveSuppliers !== null && length === 0`) |
| 6 | Гонка ответов: перечит после приёмки обгоняется ответом поллинга, выданным ДО неё → строка откидывается в «В пути» | **medium · CONFIRMED** по коду (окно опроса 8 c) | нумерация выдачи запросов; ответ старше применённого отбрасывается |
| 7 | При отказе подачи отзыва на экране оставалась оценка, которой нет в базе | **medium · CONFIRMED** | локальный патч откатывается в `catch` |
| 8 | `SupplierRow.rating` стал мёртвым полем: ни одного читателя, 6 мест записи; демо-пул потерял свои звёзды ★4.8/4.2/4.5 | **medium · CONFIRMED** (`grep` по `src/` — читателей нет) | отрисовка `s.myRating ?? s.rating`: константа 4.5 у реальных строк ушла (FR-015), демо сохранило свои РАЗНЫЕ значения, мёртвого поля нет |
| 9 | Фикстура `M-014` в SQL-тесте зелена на форме, которую писатель кусков не создаёт (`pool_line_id = null` отсекался внутренним join'ом ДО guard'а) | **high · CONFIRMED** (см. #1) | фикстура переведена на реальную форму записи — теперь guard проверяется по-настоящему |

**Маршрут `defer` → леджер (не скоуп слайса)**

| # | находка | вердикт · доказательство |
|---|---------|--------------------------|
| 10 | Пул закрывается в `completed` раньше времени: `rpc_self_confirm_delivery` считает остаток только по партиям через `pool_line` (`20260622120000:1809-1818`), `rpc_self_confirm_delivery_alloc` — только по распределениям (`20260702190000:225-233`). В смешанном пуле (`M-003`) приёмка последней строки любого маршрута закрывает пул, пока строки другого не приняты | **high · CONFIRMED** чтением обеих функций. Найдено дирижёром до кода, подтверждено обоими ревьюерами независимо. Обе функции приёмки — **вне скоупа** (FR-012: «уже задеплоены»), правка меняла бы FSM пула |
| 11 | Гейт владения read-model идёт через `pool_requests.organization_id`, а канонический создатель `rpc_create_pool` пишет `pool_request_id = null` (`d02_tsp.sql:2793-2810`) — для пула в канонической форме владелец получит `FORBIDDEN` | **medium · CONFIRMED, латентно.** Живой создатель (`rpc_self_activate_pool_request`) `pool_request_id` ставит, поэтому сейчас не проявляется. Дом расхождения `pools.organization_id` ↔ `pool_requests.organization_id` — **ARS-668**. Гейт сохранён как есть намеренно (P7): его смена открыла бы доступ там, где сейчас отказ, — это не механическая правка |
| 12 | Коллизия пространства id: `mpk-profile-org-section.browser.test.tsx:49` держит `@case M-007 M-008 M-018 FR-008` — те же идентификаторы у ЧУЖОГО слайса; аудит «по id» дал бы ложное «покрыто» | **high · CONFIRMED.** В этом слайсе обойдено: id называются с префиксом `ARS-684 M-NNN`, `@case` — только существующие `TSPM-CLOSE-04/05`. Общее решение по пространству id — дом QA |
| 13 | `tests/*.sql` не исполняются ничем автоматически: CI `sql-checks` гоняет только `cross_check.sh` + `qa/check_coverage.sh` (`ci.yml:33-40`), а `check_coverage.sh` порога падения не имеет и видит только `@case`-теги | **high · CONFIRMED** чтением воркфлоу и скрипта |
| 14 | Охват снапшота CHECK 11 (`SQL_FILES`) не включает ни ARS-360, ни миграцию слайса | **high · CONFIRMED эмпирически**: после правки `cross_check.sh` снова печатает «OK: 172 rpc return-contracts match snapshot», не увидев ни новую функцию, ни поле `source`. Цена «просто добавить все миграции» измерена: 40 файлов, **24 функции** переопределены в них более одного раза (лидеры 8/7/7/7) → `CHECK 1` дал бы CRITICAL по каждой. Уже заведено отдельной задачей; в слайсе закрыто заменой средства проверки (`Spec Change Log`) |

**Маршрут `false` (проверено — не происходит)**

| # | находка | почему нет |
|---|---------|-----------|
| 15 | «Повторный клик по звезде даст `REVIEW_ALREADY_SUBMITTED`» | Матрица этого и ожидает: `M-010`, столбец обработки ошибки — «повторная подача → `REVIEW_ALREADY_SUBMITTED`, сообщение». Код показывает сообщение — соответствует |
| 16 | «`price` может выйти нулём при `deal_price_per_kg` и `farmer_price_per_kg` = null» | `farmer_price_per_kg` заполняется при публикации партии (`d02_tsp.sql:1180-1182`), а до публикации партия в пул не попадает: маршрут «партия» требует `pool_line_id`, который ставится при матче |
| 17 | «`heads` завышены у партии в `partially_matched`» | Снято патчем #1: партия с любым распределением на маршрут «партия» больше не попадает вовсе |
| 18 | «`order by ... desc` без `nulls last` поднимет строки без даты наверх» | Семантика унаследована от исходника ARS-360 без изменений; на порядок строк матрица требований не предъявляет |

**Незакрытое — вопрос владельцу (см. `Open Questions`)**

| # | находка | вердикт |
|---|---------|---------|
| 19 | `FR-015` называет `myRating` «собственной прошлой оценкой этого **хозяйства**», а фактически это оценка **данной партии**: lateral гейтит `dr.batch_id = b.id`. Следствие — в ветке набора (`filling`) звезда структурно всегда прочерк, элемент бессмыслен | **high · CONFIRMED.** Ложная посылка внутри **frozen**-блока, то есть класс `intent_gap`. Код исполняет `FR-015` буквально (оценка либо прочерк) и константу 4.5 убирает, поэтому откат кода ничего бы не улучшил — но выбор «оставить прочерк / убрать звезду из ветки набора / завести агрегат репутации» принадлежит владельцу. **Закрыто 10.09: владелец выбрал прочерк** (`Clarifications`) |

### Итерация 2 — converge (замысел ↔ реальность), 2026-09-10

Сверка инспектировала код **как он стоит**, не дифф. Инвентарь: 33 id (15 `FR` + 18 `M`),
реализовано 28, находок 6. `contradicts` — ни одного, то есть блокера G3 сверка не дала.

| # | ось · id | находка · доказательство | маршрут |
|---|----------|--------------------------|---------|
| 20 | `partial` · **FR-003**, M-001 | **Телефон хозяйства не выводился на экран.** RPC его отдаёт в обеих ветках, но строка поставщика печатала только название и район: `PoolMonitorModal.tsx:436-443` и `:549-551`. Единственным потребителем `s.farmPhone` был **печатный документ** сделки (`:80 → deal-doc.ts:101`). Асимметрия, которую FR-003 называет как причину своего существования, на телефоне сохранялась буквально: у фермера телефон покупателя печатается в строке (`BatchScreen.tsx:287`). Это половина смысла слайса — «узнать, у кого купил, и позвонить» | **patch.** Телефон добавлен в строку в обеих ветках, ссылкой `tel:`. Отдельного гейта не нужно: до раскрытия контактов база присылает `null` (M-004) |
| 21 | `partial` · **FR-007** | `applyStatus` (`:290-297`) применял статус локально и при отказе RPC его НЕ откатывал — реальный пул до следующего опроса рисовал состояние, которого в базе нет. Ровно тот класс, от которого `filledHeads` в этой же ветке огорожен явно | **patch.** При отказе `onAdvance` статус возвращается к прежнему |
| 22 | `partial` · **M-011** | В ветке `executed`/`closed` (`:553`) звёздная форма рендерилась для КАЖДОЙ строки — без проверки приёмки и без метки состояния, так что «в пути» визуально не отличалась от принятой. Ветка достижима с непринятыми строками из-за `POOL-COMPLETE-PER-ROUTE-01` | **patch.** Звёзды только при `delivered`; состояние строки теперь подписано (раньше эта ветка его не показывала вовсе). Гейт по приёмке, НЕ по маршруту: при штатном закрытии партия куска тоже `delivered`, поведение куска не меняется (M-007) |
| 23 | `partial` · **M-013** | Сообщение «Не удалось обновить список» рендерилось в двух ветках из трёх; ветка `executed`/`closed` не показывала его вовсе. Половина требования («список не подменяется пустым») держалась везде | **patch.** Сообщение добавлено в третью ветку |
| 24 | `partial` · **M-016** | То же для признака загрузки: завершённый пул до прихода ответа показывал сделку без поставщиков и без «Загрузка…» | **patch.** Признак добавлен в третью ветку |
| 26 | `partial` · **M-016** | Ветка `filled`/`executing` рендерила список поставщиков **безусловно**, а не за гейтом загрузки, как ветка `filling`: до первого ответа сюда попал бы `pool.suppliers` одновременно с надписью «Загрузка…». Найдено исполнителем браузерного теста и **проверено экспериментом**, а не чтением: подложенная строка просачивалась в DOM. В проде путь замаскирован (`toPool` всегда ставит реальному пулу `suppliers: []`, `pools-load.ts:78`) — но комментарии компонента обещали эту гарантию для ОБЕИХ ветвей, а структурно она была в одной | **patch.** Список загейтен так же, как в `filling` |
| 25 | `unrequested` | **Выдуманная сумма сделки у реального пула:** `sumMln = pool.filledHeads * 0.45 * avgPrice / 100000 / 10` (`:544`), рендер «сумма ≈ N млн ₸». Коэффициент `0.45` не приходит ни из одной строки ответа (у каждой строки есть настоящие `avgWeight` и `price` — они и считаются в `supplierSum`), а арифметика даёт величину примерно **в 1000 раз меньше** действительной: 100 гол × 450 кг × 1650 ₸ = 74 млн ₸, формула печатает «0.1 млн ₸». Не огорожена `!realPool`. Ни один id инвентаря её не требует | **вопрос владельцу → закрыт 10.09.** Молча не удалялось: обоснование требует id, id — во frozen-блоке, блок заморожен. Владелец выбрал «считать по настоящим данным, элемент сохранить» (`Clarifications`): итог = сумма `supplierSum` по строкам |

- [2026-09-10] converge-находка в **чужом** слайсе (не правится здесь): инвентарь
  `AGOS-Dok6-Slice10-MPK-Profile.md:248` числит «приёмку по кускам» действующей функцией
  `PoolMonitorModal.tsx`. С 26.07 она недостижима — агрегат пуст. Маршрутизировано в Review
  Triage Log слайса 10, не в этот скоуп.

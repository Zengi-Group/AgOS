---
status: draft            # G2 не пройден: подпись g2_approved ставит человек своим терминалом
g2_approved:
baseline_commit:         # пусто до /build
brain_spec: [[projects/agos/specs/tsp-farmer-sell-flow]]
task_home: ARS-731 (Linear, team ARS, проект «Рынок TSP»)
---

# TSP — закрытие заявки подтверждает сделку в обеих формах записи

<frozen-after-approval>

## Intent

Канон `MS4-BT-13` (`D-TSP-11`, `D-M6-5`, QA `TSPF-LIFE-10`): когда заявка закрывается
набранной, сделки переходят `matched → confirmed`, покупатель раскрывается, и у фермера
появляется ход «Отметьте отгрузку».

Сегодня это выполняется наполовину. Сделка между фермером и комбинатом записывается
**двумя формами**: авто-матч при публикации пишет её ссылкой в самой партии
(`batches.pool_line_id`), ручной матч комбината с маркет-борда — строкой в
`batch_allocations` (одна строка на **всю** партию; дробления нет — решение владельца
2026-07-26). **Все четыре** пути, закрывающие заявку по набору цели, подтверждают только
**свою** форму записи и оставляют чужую в `matched`.

Сделка в `matched` — тупик обеих сторон: у фермера нет кнопки отгрузки, у комбината строка
говорит «Ожидает отгрузки», то есть комбинат ждёт фермера, а фермеру нечего нажать.
Недостижимыми становятся сразу три последних шага цепи — отгрузка, приёмка, отзыв.
На проде это уже случилось (партия 30 голов в `matched` при заявке 80/80 `completed`,
`IMPL_DEBT` → `MPK-AWAITING-DISPATCH-FALSE-01`).

Слайс доводит правило до конца на всех путях закрытия, чинит уже застрявшие сделки и
убирает ложную подпись с экрана комбината. Новых возможностей не добавляет.

**Термин.** «Сделка» здесь — **запись** о привязке партии к заявке (ссылка либо строка),
а не партия целиком. Партия может остаться `partially_matched`, если её историческая часть
ещё продаётся: подтверждается сделка, статус партии пересчитывается из её записей.

## Boundaries & Constraints

**Always**
- **FR-001** **Все четыре** тела, закрывающие заявку по набору цели, подтверждают сделку
  **в обеих формах записи**, вызывая существующую задеплоенную `fn_tsp_pool_confirm_matches`
  (ARS-695). Тела названы поимённо, чтобы счёт нельзя было занизить:
  `rpc_self_activate_pool_request`, `rpc_self_auto_match_batch`, `rpc_self_accept_offer`
  (все три — `20260726140000_tsp_slice9_defer_revert_selfserve.sql`, L160 / L311 / L485) и
  `fn_tsp_alloc_chunk` (`20260702190000_tsp_chunk_dispatch.sql`, L356). Односторонние
  `update` внутри них **заменяются** вызовом, а не дополняются им: два дома одного правила —
  то, из-за чего дефект и возник (`P4`). **Провал выглядит так:** починены не все четыре —
  тогда дефект переживает слайс, а путь принятия оффера или активации заявки продолжает
  оставлять чужую форму в `matched`.
- **FR-002** Вызов делается **внутри той же ветки**, что и перевод заявки в `closed_filled`,
  то есть только при фактическом переходе (`if found` после `update … where status =
  'filling'`). Повторный вызов и гонка двух путей безопасны: `fn_tsp_pool_confirm_matches`
  идемпотентна по построению (`where status = 'matched'`), второй вызов подтверждает ноль
  записей.
- **FR-003** Сигнатуры функций не меняются, новых функций слайс не заводит (`P7`). Правка —
  только тела.
- **FR-004** Разовый ремонт данных: сделки в `matched`, чья заявка закрыта **как набранная**,
  переводятся в `confirmed` в обеих формах записи. Набранными считаются статусы
  `closed_filled` · `closed_partial` · `completed` · `executing` · `executed` · легаси
  `filled` · `dispatched` · `delivered`. Отметка времени — `now()` момента ремонта, в
  `batches.confirmed_at` и `batch_allocations.confirmed_at` соответственно; исторический
  `matched_at` не переписывается. **Почему `now()`, а не задним числом:** подтверждение
  действительно произошло сейчас, и подделывать дату аудита ремонтом нельзя.
- **FR-005** Ремонт идемпотентен: повторный прогон не меняет ни одной строки.
- **FR-006** Монитор заявки у комбината различает **все четыре** значения `status`, которые
  отдаёт `rpc_get_pool_matches` (`ARS-684`): `active` → «Сделка ещё не подтверждена» ·
  `confirmed` → «Ожидает отгрузки» · `dispatched` → «В пути» · `delivered` → «Принято».
  Значение, не входящее в четвёрку, показывается как «Состояние неизвестно» и **не выдаётся**
  за одно из известных.
- **FR-007** У строки «Сделка ещё не подтверждена» **нет хода приёмки**: кнопка появляется
  только у строки «В пути», как того требуют обе RPC приёмки (`must be dispatched`). Экран,
  честный в подписи и лживый в кнопке, — не исправление.
- **FR-008** Новое различие состояний заводится **в одном месте** — в мапперe ответа
  (`data/pools-load.ts`, `toSupplier`) и типе `SupplierRow`; обе поверхности (мобильный
  `PoolMonitorModal`, десктопный `RequestMonitor`) берут подпись оттуда. Третья формулировка
  того же состояния не заводится (`P4`).
- **FR-009** Экран партии у фермера в состоянии `matched` называет, чего он ждёт («заявка
  ещё набирается — отгрузка откроется, когда покупатель её закроет»). Существующее
  предупреждение о штрафе за снятие **сохраняется** (`HS-2`): объяснение добавляется к нему,
  а не вместо него. Хода отгрузки в этом состоянии по-прежнему нет — отгружать нечего, пока
  сделка не подтверждена (`MS4-BT-16` требует `confirmed`).
- **FR-010** Правило исхода остаётся в SQL: экран показывает то, что вернула база, и своей
  ветки подтверждения не заводит.

**Never**
- **FR-011** Слайс не трогает `rpc_self_match_batch_to_pool` — седьмую живую функцию
  отложенного Слайса 9 (решение владельца 2026-09-18, долг `TSP-SLICE9-LIVE-MATCH-01`).
  Уточнение, чтобы не путать: FR-001 правит `fn_tsp_alloc_chunk` — **хелпер**, который эта
  функция вызывает; сама она, её сигнатура и её решение «писать строкой в
  `batch_allocations`» остаются как есть.
- **FR-012** Слайс не чинит планировщик (`ARS-694`), уведомления (`ARS-685`), отмену заявки
  (`ARS-719`) и дефекты маркет-борда (`ARS-689`/`688`/`691`).
- **FR-013** Деплой — **только точечной выкладкой** (`scripts/deploy/`), как ARS-695 и
  ARS-314; гнать `d02_tsp.sql` целиком запрещено guard'ом `TSP-SLICE9-ROLLBACK-01`. Новые
  тела живут в **новой миграции** `supabase/migrations/`, поверх существующих — ни одна
  применённая миграция не переписывается на месте. В `d02_tsp.sql` этих четырёх функций нет
  вовсе, поэтому правило «все изменения в канонические файлы» здесь неприменимо, и это
  сказано явно, а не обойдено молчанием.
- **FR-014** Ремонт не трогает сделки заявок, закрытых **без набора** (`cancelled` ·
  `closed_unfilled` · `expired_empty` · `closed`), и заявок, ещё живых (`filling` ·
  `awaiting_mpk_decision` · `draft`): их партии должны возвращаться на рынок или продолжать
  набор, и это чужой дом (`ARS-314`). Подтвердить такую сделку значило бы создать сделку,
  которой не было. Перечень сверен с `pools_status_check` (`d02_tsp.sql`) — все 15
  разрешённых значений распределены между `FR-004` и этим требованием.
- **FR-015** Слайс не меняет `rpc_get_pool_matches` и вообще ни одной read-RPC: данные для
  правды уже приходят.

**Следствие, названное вслух (не «Never», а цена решения)**
- **FR-016** Вызов `fn_tsp_pool_confirm_matches` **добавляет эмиссию событий** на четырёх
  путях закрытия: её тело пишет `batch_events` и зовёт `fn_tsp_emit_batch_confirmed`. Это
  принимается сознательно — событие «сделка подтверждена» и должно возникать при
  подтверждении; доставка до человека остаётся домом `ARS-685` (вне скоупа, канал выбирает
  владелец). Побочно: метаданные события жёстко несут `partial_accept: true`, что при
  закрытии полным набором неправда — правится здесь же, потому что иначе слайс своими
  руками начнёт писать ложь в журнал.

## I/O & Edge-Case Matrix

| id | сценарий | вход / состояние | ожидаемое | обработка ошибки |
|----|----------|------------------|-----------|------------------|
| M-001 | смешанная заявка, закрывает авто-матч | сделки обеих форм; цель добирает `rpc_self_auto_match_batch` | заявка `closed_filled`; **обе** сделки `confirmed`; у фермера без остатка — ход «Партия отгружена» | — |
| M-002 | смешанная заявка, закрывает аллокатор | тот же состав, цель добирает `fn_tsp_alloc_chunk` | то же | — |
| M-003 | смешанная заявка, закрывает принятие оффера | тот же состав, цель добирает `rpc_self_accept_offer` | то же — путь, который при счёте «два тела» остался бы сломанным | — |
| M-004 | смешанная заявка, закрывает активация заявки | цель добирается свипом при `rpc_self_activate_pool_request` | то же | — |
| M-005 | партия с историческим остатком | сделка подтверждена, но у партии есть непроданная часть | сделка `confirmed`, партия остаётся `partially_matched`; ход фермера — «Отгрузить готовое (N гол.)» | — |
| M-006 | ремонт застрявшей сделки | сделка `matched`, `confirmed_at = NULL`, заявка `completed` | после ремонта `confirmed`; у фермера появилась кнопка отгрузки, у комбината — «Ожидает отгрузки» | повторный прогон не меняет ни строки |
| M-007 | ремонт не трогает чужое | сделка `matched` при заявке `cancelled` / `closed_unfilled` / `filling` | осталась `matched` | — |
| M-008 | подпись неподтверждённой строки | заявка набирается, строка `active` | «Сделка ещё не подтверждена», **кнопки приёмки нет** | — |
| M-009 | подписи остальных состояний | строки `confirmed` / `dispatched` / `delivered` | «Ожидает отгрузки» / «В пути» / «Принято»; кнопка приёмки только у «В пути» | неизвестное значение → «Состояние неизвестно», кнопки нет |
| M-010 | экран фермера при `matched` | заявка ещё набирается | видно объяснение, чего ждать, и сохранено предупреждение о штрафе; кнопки отгрузки нет | — |
| M-011 | сквозной путь | заявка закрылась набранной → фермер отгружает → комбинат подтверждает приёмку | партия проходит `confirmed → dispatched → delivered`, открывается отзыв | отказ RPC показывается строкой с причиной от базы рядом с действием; состояние перечитывается из базы, локально не угадывается |

</frozen-after-approval>

## Design contract (the «how» — сигнатуры, НЕ тела)

- **Data model.** Новых сущностей, таблиц и колонок нет.
- **API / RPC.** Ни одной новой и ни одной изменённой сигнатуры. Правятся **тела** четырёх
  функций (`FR-001`); вызывается существующая `fn_tsp_pool_confirm_matches(uuid) → int`
  (`SECURITY DEFINER`, `revoke … from authenticated` — внутренний хелпер).
- **Артефакт.** Новая миграция в `supabase/migrations/` + идемпотентный скрипт ремонта в
  `scripts/deploy/`, применяемые в окне владельца одним заходом (`FR-013`).
- **Events.** Слайс **начинает** эмитить `batch.confirmed` на четырёх путях закрытия как
  следствие `FR-001` (см. `FR-016`). Доставка — вне скоупа (`ARS-685`).
- **UI contract.** Дом различия состояний — `data/pools-load.ts` (`toSupplier`) + тип
  `SupplierRow`; потребители — `modals/PoolMonitorModal.tsx` и
  `requests/RequestMonitor.tsx` (`FR-008`). Экран партии фермера — `screens/BatchScreen.tsx`,
  состояние `matched` (`FR-009`). Персоны приёмки — фермер и закупщик комбината.

## Slices → Tasks

| задача | тир | закрывает id | приёмка (глазами участника) |
|--------|-----|--------------|------------------------------|
| **C-1** SQL: все четыре пути закрытия зовут `fn_tsp_pool_confirm_matches` + правка метаданных события | semantic | FR-001, FR-002, FR-003, FR-010, FR-016, M-001…M-005 | Заявка набралась — сделка подтверждена у всех, каким бы путём заявка ни закрылась |
| **C-2** Ремонт застрявших сделок | semantic | FR-004, FR-005, FR-014, M-006, M-007 | Сделки, висевшие мёртвыми, снова отгружаемы; отменённые и ещё живые заявки не тронуты |
| **C-3** Честные состояния строки у комбината | mechanical | FR-006, FR-007, FR-008, FR-015, M-008, M-009 | Вижу, ждёт сделка подтверждения или уже отгрузки, и не жму кнопку, которая откажет |
| **C-4** Объяснение у фермера при `matched` | mechanical | FR-009, M-010 | Понимаю, почему кнопки нет и когда она появится, и по-прежнему предупреждён о штрафе |

## Verification (G3 inputs)

- **SQL-тест** на смешанную заявку (обе формы записи) с закрытием **каждым из четырёх**
  путей — `M-001`…`M-004`; плюс партия с остатком — `M-005`. Прогон на боевой базе в
  откатываемой транзакции, как `tests/ars_695_*`.
- **Ремонт**: счётчик до/после, проверка `FR-014` (заявки без набора и живые не тронуты),
  повторный прогон без изменений (`M-006`/`M-007`). Приёмка прогона — не «одна известная
  партия», а: все сделки, попавшие под `FR-004`, перешли в `confirmed`; ни одна строка вне
  этого множества не изменилась.
- **UI-строки `M-008`…`M-010`** закрываются браузерными тестами, называющими свой `M-id`
  (`src/tests/**/*.browser.test.tsx`, проект `routers`) — без них провал C-3/C-4 неотличим
  от успеха до ручного взгляда на экран.
- `cross_check.sh` — CHECK 11 обязан остаться без изменений (доказательство `FR-015`);
  `tsc -b`; сборка; `npx vitest run --project routers`.
- **Доказательство `FR-003`** (сигнатуры не менялись) — дифф: в нём нет ни одной строки
  `create or replace function` с изменённым списком аргументов.
- После выкладки — `python3 scripts/prod_diff.py`, дрейф 0.
- Сценарии `qa/scenarios/`: фермер — `TSPF-LIFE-10`, `TSPF-LIFE-13`, `TSPF-LIFE-14`,
  `TSPF-LIFE-15`; комбинат — `TSPM-CLOSE-01`, `TSPM-OFF-02`, `TSPM-POOL-05`.
- `converge intent↔reality` по этому слайсу.

## Open Questions

> Спек не покидает `draft`, пока жив вопрос.

Живых вопросов нет — четыре решения владельца приняты 2026-09-18 и перенесены в
`Clarifications`.

## Clarifications  (append-only)

- [2026-09-18] Q: чиним ли уже застрявшие данные? → A: **да, разовый ремонт** (`FR-004`),
  только для заявок, закрытых как набранные · отвергнуто: только фикс на будущее (фермер с
  застрявшей сделкой так и не увидел бы хода); сначала измерить (измерение всё равно
  делается тем же запросом при выкладке).
- [2026-09-18] Q: что писать про неподтверждённую строку у комбината? → A: **«Сделка ещё не
  подтверждена»** (`FR-006`) · отвергнуто: «Ждёт закрытия заявки» — длиннее и говорит про
  процесс, а не про факт базы.
- [2026-09-18] Q: объём при четырёх днях до пилота? → A: **SQL + ремонт + экраны** ·
  отвергнуто: только SQL (подпись продолжала бы врать); плюс фикс «Подходит ✓» (два разных
  замысла в одном слайсе).
- [2026-09-18] Q: что делать с `rpc_self_match_batch_to_pool` — седьмой живой функцией
  отложенного Слайса 9? → A: **не трогать, записать в долг** (`FR-011`) · отвергнуто:
  откатить её здесь же (правка живого пути привязки за неделю до пилота); завести отдельный
  тикет (дом уже есть — `TSP-SLICE9-ROLLBACK-01` + `TSP-SLICE9-LIVE-MATCH-01`).

## Assumptions  (append-only, ВНЕ frozen)

- [2026-09-18] Ремонт считается **разовым**: после `FR-001` новые застревания невозможны,
  потому что все четыре пути закрытия перечислены поимённо и проверены тестом каждый.
  Опровергло бы — обнаружение пятого пути, переводящего заявку в закрытую-набранной.
- [2026-09-18] Подпись «Ожидает отгрузки» остаётся верной для подтверждённой строки —
  правится только неподтверждённая. Опровергло бы: решение владельца переписать обе.
- [2026-09-18] Экран фермера при `matched` остаётся **без хода**: отгружать нечего, пока
  сделка не подтверждена, и это канон (`MS4-BT-16`). Опровергло бы: решение разрешить
  отгрузку до подтверждения — это другая модель сделки.
- [2026-09-18] Партия с непроданным остатком — **историческая** ситуация: дробление
  отключено 26.07, новые такие партии не появляются. Поэтому `M-005` описан как краевой
  случай, а не как основной путь. Опровергло бы: реактивация Слайса 9.

## Implementation Notes  (append-only)

## Spec Change Log  (append-only)

## Review Triage Log  (append-only)

Ревью **качества спека** (якорь 4, пункт ⑤ G2-карточки), отдельный ревьюер со свежим
контекстом, 2026-09-18. Вердикт: **18 дыр по 5 осям** (12 Gap · 3 Vague · 3 Conflict).
Спек не был заморожен, поэтому **все 18 закрыты правкой до гейта**; ни одна не принята
как есть.

| # | находка (ось) | вердикт | что сделано |
|---|---------------|---------|-------------|
| 1 | `[Gap]` Completeness · закрытие по набору цели стоит в **четырёх** телах, спек называл два | **подтверждена, ошибка автора спека** — проверено: `20260726140000` L160/L311/L485 (три функции) + `20260702190000` L356 | `FR-001` переписан: все четыре названы поимённо с адресами; добавлены строки матрицы `M-003`/`M-004` на два прежде неучтённых пути |
| 2 | `[Conflict]` Consistency · «слайс не эмитит событий» ↔ тело вызываемой функции пишет `batch_events` и зовёт `fn_tsp_emit_batch_confirmed` | **подтверждена** — тело прочитано (`20260914120000:118-172`) | заведён `FR-016`: эмиссия принимается сознательно, доставка остаётся в `ARS-685`. Попутно найдено то, чего ревьюер не назвал: метаданные жёстко несут `partial_accept: true` — при полном наборе это ложь, правится тем же `FR-016` |
| 3 | `[Gap]` Completeness · перечни статусов заявки покрывали 9 значений из 15 | подтверждена | `FR-004` + `FR-014` переписаны: все 15 значений `pools_status_check` распределены явно, легаси названы поимённо |
| 4 | `[Gap]` Completeness · не сказано про повтор и гонку путей закрытия | подтверждена | `FR-002`: вызов внутри ветки фактического перехода; идемпотентность названа с механизмом (`where status = 'matched'`) |
| 5 | `[Gap]` Completeness · read-model отдаёт **четыре** значения, спек называл два | подтверждена | `FR-006`: все четыре подписи + поведение при неизвестном значении |
| 6 | `[Gap]` Completeness · правится подпись, но не ход: у неподтверждённой строки осталась бы кнопка приёмки | подтверждена — и это тот же дефект, что закрывали в ARS-718 | `FR-007` + `M-008`/`M-009` |
| 7 | `[Gap]` Completeness · не назван дом нового различия состояний | подтверждена | `FR-008`: маппер + тип, обе поверхности берут оттуда |
| 8 | `[Gap]` Completeness · не назван артефакт, где живут новые тела | подтверждена — четырёх функций нет в `d02_tsp.sql` вовсе | `FR-013`: новая миграция поверх, правило канонических файлов неприменимо — сказано явно |
| 9 | `[Vague]` Clarity · «с отметкой времени» — какой и в какое поле | подтверждена | `FR-004`: `now()` момента ремонта, поля обеих форм названы, `matched_at` не переписывается, причина названа |
| 10 | `[Vague]` Clarity · `M-011` «отказ показывается причиной» | подтверждена | уточнено: строка с причиной **от базы** рядом с действием, состояние перечитывается |
| 11 | `[Vague]` Clarity · «сделка» в двух значениях (запись vs партия) | подтверждена | термин определён в `Intent`; `M-005` разводит подтверждение сделки и статус партии |
| 12 | `[Vague]` Clarity · `FR-011` запрещает трогать ручной матч, а `FR-001` правит функцию, которую тот зовёт | подтверждена — формулировка допускала два прочтения | `FR-011` уточнён: правится хелпер, не вызывающая функция |
| 13 | `[Conflict]` Consistency · «молчание заменяется объяснением» разрешало удалить предупреждение о штрафе | подтверждена — `HS-2` | `FR-009`: предупреждение **сохраняется**, объяснение добавляется к нему |
| 14 | `[Conflict]` Consistency · «все `confirmed`» неверно для партии с остатком (rollup оставляет `partially_matched`) | подтверждена — комментарий в теле функции это прямо описывает | `M-005` заведён отдельной строкой; допущение про историчность остатка записано |
| 15 | `[Gap]` Measurability · у трёх UI-строк не назван прибор | подтверждена | `Verification`: браузерные тесты, называющие свой `M-id` |
| 16 | `[Gap]` Measurability · приёмка ремонта привязана к одной известной партии | подтверждена | `Verification`: критерий на множество (все под `FR-004` перешли, ничего вне множества не изменилось) |
| 17 | `[Gap]` Coverage · в проверке нет ни одного сценария домена МПК | подтверждена | добавлены `TSPM-CLOSE-01`, `TSPM-OFF-02`, `TSPM-POOL-05` и `TSPF-LIFE-13` |
| 18 | `[Gap]` Coverage · пять требований без строки матрицы и без отметки «не наблюдаемо» | подтверждена | `FR-003` и `FR-015` получили **прибор** вместо строки матрицы (дифф сигнатур и CHECK 11 — названы в `Verification`); `FR-012`/`FR-013` — границы слайса, наблюдаются диффом; связь проставлена явно |

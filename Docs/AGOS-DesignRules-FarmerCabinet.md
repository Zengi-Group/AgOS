# AGOS — Правила дизайна фермерской зоны (канон)

**Status:** Adopted 2026-07-10 (DECISIONS_LOG → D-UI-FARMER-RULES-01).
**Scope:** вся фермерская зона — кабинет `/cabinet` (`.agos-cabinet-stage`, включая MPK-шелл, который наследует `cabinet.css`) + вход-фаннел (`/welcome`, `/login`, `/register`, `/membership`, светлая «paper», `src/lib/auth-ui/tokens.ts`).
**НЕ распространяется на:** админку/экспертку (`[data-shell]` — канон `AGOS-DesignSystem-v12.md`) и публичный лендинг (`:root`, легаси-палитра).

> Этот файл — единственное место, где живут визуальные правила фермерской зоны.
> Правки дизайна от CEO анализируются и фиксируются здесь как правила (§ Реестр правил из правок CEO) — чтобы одна и та же правка не повторялась дважды.

---

## Authority chain

| Слой | Авторитетный источник |
|---|---|
| Правила (этот документ) | `Docs/AGOS-DesignRules-FarmerCabinet.md` |
| Токены кабинета (deployed) | `src/pages/cabinet/shell/cabinet.css` — блок `.agos-cabinet-stage` |
| Токены вход-фаннела | `src/lib/auth-ui/tokens.ts` (светлая «paper», scoped) |
| Иконки (deployed) | `src/pages/cabinet/shell/components/icons/PhIcon.tsx` + `TuranStar.tsx` |
| Источник дизайна (прототип) | `Zengi-Group/agos-farmer` (Lovable) — структура, CSS, контент |
| Ход редизайна | `Docs/AGOS-Farmer-Redesign-Handoff.md` |
| Контракты экранов | Dok 6 slice-файлы (Slice5a Market, Slice7 Farm-Wizard, …) |

При расхождении deployed CSS и этого документа — это дефект: флагать и приводить оба к согласию (правило CLAUDE.md «Conflict resolution»).

---

## 1. Палитра

- Кабинет = **только светлая тема «daylight»** (решение CEO 2026-07-09): `--bg #f6f3ed`, `--bg-c #fbfaf6`, `--fg #3d2b1f`, акцент `--accent #E8920B`. Тёмная «night» + переключатель — отложены (значения есть в DS прототипа).
- Цвета — **только через токены** `.agos-cabinet-stage` (`--bg/--fg/--bd/--accent/--cta/…` + muted-подложки `--*-m`). Литеральный hex в компонентах — дефект.
- CTA-кнопка тёмная (`--cta #3d2b1f`), акцент-амбер — для активных состояний, Turan-star и амбер-актива таб-бара.
- Boot-фон: `--boot-bg #fdf6ee` (BootScreen, инлайн-плейсхолдер `#root`) — никаких белых провалов на пути в кабинет.

## 2. Типографика

- Шрифт: **Geist** (UI), **Geist Mono** (цифры/ID) — через `var(--font-sans)`/`var(--font-mono)`.
- **Mono только для данных** (R-9): `mk-mono` (Geist Mono) — исключительно цифры, цены, счётчики, ID, даты. Любая текстовая микрокопия на русском (подписи под CTA, подписи категорий/вариантов, eyebrow-строки, hints) — Geist sans. `mk-mono` на фразе = дефект. Смешанная строка («защитная: 1 400 ₸/кг») — фраза в sans, числовая часть при необходимости в mono-`span`.
- Гоча Ionic: внутри `ion-content` шрифт берётся из `--ion-font-family` (задан Geist в `ionic.css`), кнопки/инпуты требуют `font-family: inherit` — уже задано на `.agos-cabinet-stage`; не ломать.
- Никаких новых шрифтовых семейств без решения CEO (бюджет загрузки шрифтов — 7 семейств, ARS-217).

## 3. Иконки (правило Phosphor-only)

- **Только Phosphor Icons** — через единый `PhIcon` (пути 1:1 из прототипа) или `@phosphor-icons/react`. Логотип — отдельный `TuranStar`.
- **Никогда:** lucide в фермерской зоне, самодельные SVG-пути, эмодзи вместо иконок (📞⏰ → PhIcon `phone`/`clock` — уже исправлялось), стикеры в хедере.
- **Вес = вес блока:** сервис-плитки Главной = **bold**, in-content = regular, membership-флоу = duotone; тонкие (`userThin`) — аватар/аккаунт.
- Новая иконка = добавить путь в `PhIcon.tsx`, не импортировать вторую библиотеку.

## 4. Язык стиля (усвоено из правок CEO)

- **Плоскость, не украшательство:** карточки плоские, радиус 12 (`--r-xl`, как `obs-card`/`dec-row`), без теней-подложек у внутренних блоков («РЕКОМЕНДУЕМ» — плоский), иконки без плиток-подложек.
- **Один акцент на блок**, подпись только у активного элемента (паттерн HomeStartLadder).
- Пустые состояния — мягкий органичный «блоб» (`color-mix accent 10%`), не квадратная рамка; заголовок 19px/700 (UI-EMPTYSTATE-01).
- CTA-тексты короткие («Снизить цену», «Варианты» — не предложения).
- **Карточка данных = внутренняя сетка** (R-15): шапка «заголовок+контекст слева | семантический чип справа» → главное число с тренд-барами по базовой линии → тихий подвал-пояснение. Один цветовой акцент на карточку — чип с тонированной подложкой (`color-mix` ok/warn 12–14%). Термин, которого фермер может не знать («защитная цена»), сопровождается иконкой-пояснением (PhIcon regular, fg3). Элементы, «висящие в воздухе» без выравнивания к сетке, — признак генеративного дизайна, дефект.
- **Вторичное действие — под primary CTA в доке** (R-13), не между полем ввода и CTA: паттерн MkCta + тихая ссылка действует и в auth-фаннеле. **Под CTA — максимум ОДНА строка микрокопи** (R-27): две строки (действие-ссылка + подпись-caption) = дефект. В мастере фермы skip-ссылку сняли совсем (auto-save + ✕ + необязывающие ответы делают её лишней); ссылка/caption остаётся там, где действительно нужна и одна.
- **Карточки-опции выбора (BigRadio) — как чипы R-18:** видимая граница `var(--bd)` + подложка `bg-c` + зазор между карточками (R-27). Прозрачная граница у невыбранной карточки + `gap:0` = карточки сливаются в одну плиту = дефект.
- **Переключатель входа/регистрации — общий атом, не инлайн-верстка** (R-25): «Есть аккаунт? Войти» / «Нет аккаунта? Зарегистрироваться» — единственный примитив `AuthAltAction` (в `auth-ui/primitives.tsx`), одинаково стоит в `StickyDock` под CTA на всех входных экранах. Префикс — тихая (`fg3`) **не**кликабельная подпись; тап-таргет — только слово-действие (accent, 44pt). Без `›`-шеврона: это кросс-линк между входами, а не drill-down (шеврон закреплён за карточками-переходами вглубь, как в RoleSelect). Повтор одного и того же интерактива инлайн-стилями в трёх файлах = дефект (разъедется, правка повторится — см. R-13).
- **Один путь вперёд на экранах флоу** (R-14): шаг/награда мастера держит primary CTA + один выход; дополнительные навигационные ссылки в теле («Все цены TURAN ›») убираются — справочники доступны из постоянной навигации.
- Отвергнутые направления (не предлагать заново): full-screen takeover-экраны для онбординг-подсказок («новый экран»), карты с номерами шагов + прогресс-баром («перегруз»), голые hairline-списки («утилитарно, не стильно»).

## 5. Структура и навигация

- **Портируем структуру прототипа** (разметка + CSS + иконки), не ре-темизируем старый код. Переиспользуем атомы `.mk-*`/`.sh-*`/`PhIcon`; RPC/данные/навигация — инварианты.
- Хедеры, 3 уровня: `HomeHead` (ask-бар — только Главная) · `TabHead` (вкладки) · `SubHead` (`‹ back-label`, sticky, сплошной фон).
- Таб-бар: 4 таба (Главная/Ферма/Рынок/Сообщения), амбер-актив; **скрыт на детальных экранах** (`hideTabBar`, route-based) — фуллскрин-UX деталей сохраняем (решение CEO, ARS-220).
- Страницы непрозрачны (`ion-content --background: var(--bg)`) — bleed-through при слайде недопустим; `backdrop-filter: blur` в хедерах не использовать.
- Переходы нативные iOS (`mode:'ios'`, swipe-back); модальные флоу — slide-up `.agos-flow-page`.
- Дисклеймер ст.171 — всюду, где показаны референс-цены (`.mk-ref-d`).
- **Тактильный отклик — обязателен** (R-28): каждый интерактив (кнопка, строка-тап-таргет, чип, таб, радио, ban-dot) несёт видимое `:active`/pressed-состояние — `transform: scale(.985)`, затемнение или подложка (образец `.mk-cta:active`). Интерактив только с `:hover`, без `:active`, «мёртв» на касании = дефект: нативное приложение отвечает на КАЖДЫЙ тап. Hover-эффекты интерактивов оборачивать в `@media (hover:hover)`, иначе подсветка «залипает» после тапа на тач-устройстве. Тап-таргет ≥44pt (мелкие точки/чипы добирают хит-зону через `::before`).

## 6. Запрещено в фермерской зоне

- Тёмная тема / переключатель тем (до отдельного решения)
- lucide-react, самодельные SVG-иконки, эмодзи в UI-копирайте
- Литеральные hex вместо токенов
- Тени/подложки у внутренних блоков карточек, плитки под иконками (**исключение:** чат-пузыри несут лёгкий `--sh-sm`-подъём — R-21)
- Стикеры/иллюстрации в хедерах
- `backdrop-filter: blur`, прозрачные `ion-page`
- Удаление функционала ради «упрощения UX» (HS-2)
- Любой спиннер, кроме `TuranLoader` (lucide `Loader2`, `.mk-spin`/`.spin` border-кольца, `IonSpinner`, самодельные `animate-spin`) — см. §7

---

## 7. Лоадеры — единственные два в проекте (R-26)

- **Единственный лоадер = компонент `TuranLoader`** (`src/components/TuranLoader.tsx`): бренд-звезда TURAN (та же геометрия, что лого/`TuranStar`) в фирменном оранжевом логотипа **`#F7931E`**. Ровно **два** варианта, третьего нет.
- **`breathe`** — полноэкранная / маршрутная / страничная загрузка: route-guard'ы, lazy-чанки, «раздел загружается», boot (`BootScreen`), экраны-«обработка» мастеров (`FwPayoffPrices`, `FwResult`, `PubResult`, `WizStep3Category`). Размер крупный (44–64). Спокойное «дыхание» лучей → «приложение работает», а не «завис».
- **`BootScreen` (экран запуска) = ТОЛЬКО иконка-марка** (R-29): без полосы-индикатора и без текстовой подписи под маркой (`label` остаётся только `aria-label` для доступности). Полоса прогресса под лого = дефект.
- **`spin`** — инлайн / в кнопке / точечная операция «идёт прямо сейчас»: сохранение, отправка, расчёт. Размер мелкий (14–20), рядом с текстом кнопки (`className="mr-2"` при необходимости).
- Цвет — фиксированный оранжевый логотипа, **не токен**: звезда это марка (как `TuranStar`), а не UI-элемент; `#F7931E` — часть лого. Это единственное легитимное исключение из правила «только токены» (§1) — и только для этого компонента.
- Геометрия звезды — **1:1 из оригинального лого** (`public/turan-icon.svg`, viewBox `42.88 -0.13 130 130`), та же, что в inline-сплэше `index.html`. Анимация — из готового веб-сниппета дизайнера (`turan-loader-web.html`, 2026-07-14): `spin` = вращение марки на 90° + лёгкое синхронное «дыхание» лучей; `breathe` = «дыхание» лучей с фазовым сдвигом.
- **Лучи НЕ пересекаются при scale** (R-29): у `.tl-ray` обязателен `transform-box: fill-box` — тогда `transform-origin` каждого луча считается от ЕГО собственного bbox (внешний угол), а не от общего viewBox. Без `fill-box` лучи расходятся и пересекаются = дефект.
- Keyframes `tlSpin` / `tlBreatheSpin` / `tlBreathe` живут в `src/index.css`, классы namespaced `tl-*` — намеренно, чтобы не наследовать border-спиннеры кабинета (`.agos-cabinet-stage .spin`, `.mk-spin`). `prefers-reduced-motion` учтён там же.
- Новый лоадер где угодно в проекте = `TuranLoader`. Правило действует во **всём** проекте (не только фермерская зона): кабинет, вход-фаннел, админка, публичный сайт.

---

## Реестр правил из правок CEO

> Процесс: каждая дизайн-правка Arshidin'а (скриншот, текст, устно) → перед фиксом
> проанализировать «какое общее правило за этим стоит» → добавить строку сюда (и,
> если нужно, обновить §1–6) в том же PR, что и сам фикс. Правка без записи = правка,
> которая повторится.

| # | Дата | Правка CEO | Правило |
|---|---|---|---|
| R-1 | 2026-07-09 | Кабинет только в светлой теме | §1: только «daylight», тёмная отложена |
| R-2 | 2026-07-09 | «Торговать» — не самодельная иконка | §3: Phosphor-only, вес плитки = bold |
| R-3 | 2026-07-09 | Убрать стикер из хедера Главной | §6: без стикеров/иллюстраций в хедерах |
| R-4 | 2026-07-09 | CTA decision-карточек сократить | §4: короткие CTA-тексты |
| R-5 | 2026-07-09 | «РЕКОМЕНДУЕМ» — без подложки | §4: плоские внутренние блоки |
| R-6 | 2026-07-09 | Пустые состояния: рамка → блоб | §4 / UI-EMPTYSTATE-01 |
| R-7 | 2026-07-10 | Онбординг-лестница: отвергнуты takeover, «перегруз», hairline-список; финал — плоская карта радиус 12, PhIcon без подложек | §4: плоскость, один акцент, радиус 12 |
| R-8 | 2026-07-09 | Таб-бар скрывать на детальных экранах | §5: hideTabBar, фуллскрин деталей |
| R-9 | 2026-07-10 | «сохраняется после каждого ответа» — «откуда этот шрифт?» | §2: mono только цифры/цены/ID/даты; текстовая микрокопия — sans |
| R-10 | 2026-07-10 | Eyebrow «ВОПРОС 1 · СТАДО» — «не нужно, есть хедер и заголовок» | Не дублировать контекст текстовыми слоями: если хедер+заголовок уже отвечают «где я», eyebrow/степ-лейбл убрать; прогресс — точками в wiz-bar |
| R-11 | 2026-07-10 | Цифра голов не видна; при 300 головах +/− неудобен | Значение числового инпута видно целиком (min ~4 цифры); в степпере ручной ввод — первичный сценарий (тап по числу = select-all), +/− — для малых поправок |
| R-12 | 2026-07-10 | «точность не важна» — «к чему?» | Подзаголовок экрана отвечает на вопрос заголовка конкретно, без висящих оговорок; смягчения («хотя бы примерно») — в тексте валидации, не в сабе |
| R-13 | 2026-07-10 | «Нет аккаунта? Зарегистрироваться» между полем и CTA — «очень неправильно» | §4: вторичное действие — под primary CTA в док-зоне, не в середине контента |
| R-14 | 2026-07-10 | «Все цены TURAN › — удали» | §4: один путь вперёд на экранах флоу; навигационные ссылки в теле шага убрать, справочники — из постоянной навигации |
| R-15 | 2026-07-10 | Карточки цен «довольно генеративные», «акценты внутри» | §4: карточка данных = внутренняя сетка + один семантический чип-акцент + иконка-пояснение у термина |
| R-16 | 2026-07-10 | «Удали иконку коровы» над заголовком шага | Декоративных иконок-«идентичностей» над заголовками нет; иконка допустима только функциональная (поясняет термин/состояние, как shieldCheck у защитной цены) |
| R-17 | 2026-07-10 | «Как соотносятся сезоны с календарём?» — грид 12 месяцев не зависел от сезона | Каскадный саб-вопрос наследует ответ родителя: показываем опции окна выбранного ответа + явный выход «У нас иначе…» на полный набор (P5) |
| R-18 | 2026-07-10 | «Дизайн календаря очень плохой, сливаются карточки» | Standalone-чипы выбора — всегда с границей и подложкой (bg-c + bd, radius 12, высота ≥44); «голый текст» гридом без ячеек — дефект |
| R-19 | 2026-07-11 | «Дизайн интерфейса чатов очень плохой — посмотрим готовые библиотеки»; затем «относилось и к спискам/лентам — распространи туда» | Сложные интерактивные поверхности (чат: скролл-полотно, группировка пузырей, инпут, typing, список диалогов) не верстаются с нуля — берётся проверенный kit (принят Chatscope: ConversationList для списка тредов, MessageList+Message.CustomContent для лент, ChatContainer+MessageInput для AI-чата) и перекрашивается токенами поверх `cs-*`; с нуля остаётся только доменный контент (pinned-решения, действия, дисклеймеры). Иконки kit'а (FontAwesome) не используются — только PhIcon (R-2). Транспорт реального AI — Vercel AI SDK (`@ai-sdk/react`), отдельным слоем |
| R-20 | 2026-07-11 | «Сделай редизайн модуля Сообщения; ориентир — современные чаты (CometChat UI Kit)» | Визуальный уровень чат-поверхностей полируется под референс современных мессенджеров, но **только через CSS-тему kit'а** (движок Chatscope, PhIcon, R-9-mono — инварианты; ориентир вроде CometChat = визуальный эталон, а не замена kit'а — его Android-код в React не переносится). Нормы «современного чата» в фермерской зоне: строки списка **без линий-разделителей** — воздух + скруглённая (r-12) подложка при нажатии как тап-таргет-карточка; превью треда — **одна строка с многоточием**; разделители-даты — **центрированная пилюля** с подложкой (не голый капс-текст; согласуется с R-18); хвостик-угол на последнем в группе; соседние пузыри одного автора слипаются плотнее; presence-точка у «всегда на связи». *Размеры/плоскость пузырей уточнены в R-21.* |
| R-21 | 2026-07-11 | По результату R-20: «аватар — убери круглые подложки», «текст не считывается, слишком мелко, не делай мелкие текста», «аватарку крупнее — её не видно, в баблах тоже», «баблы убогие», «текст и шрифт как на этикетке магазина» | Чат — «максимально удобный», а не компактный. Конкретные нормы (усиливают/перекрывают робкие значения R-20): **(1) Аватары без бледных тонированных дисков** — крупный полноцветный глиф (список: глиф ≥28, контейнер 46; шапка ≥22, контейнер 44; в баблах ≥28). Диск-подложка 10–13 % opacity = дефект («не видно»). **(2) Текст читаемый, не мелкий:** тело сообщения ≥15px, имена/заголовки ≥17/700, превью ≥14 в `--fg2` (не `--fg3`), время/подписи ≥12–13. Мелкий серый текст = «этикетка магазина» = дефект. **(3) Пузыри — ИСКЛЮЧЕНИЕ из §6-плоскости:** радиус ~18, щедрый паддинг (12×16), лёгкий подъём `--sh-sm`; голый hairline-бабл без объёма читается «убого». **(4)** «Дублирование по левому краю» в чате — это кадр нативного iOS-перехода (parallax предыдущего экрана во время слайда), не статический дефект: верхняя `ion-page` кроет экран полностью после завершения перехода. |
| R-22 | 2026-07-11 | По референсам CometChat/Telegram: «отступы от аватарок — заголовки уезжают вправо», «компоновка левого блока (текст) и правого (дата+счётчик)», «продумай список с CTA чтобы было функционально» | Анатомия строки списка чатов = как в проверенных мессенджерах. **(1) Аватар — якорь строки:** глиф заполняет контейнер (список: глиф ~34 в контейнере 44, зазор до текста ~11), а не висит маленьким в большом боксе — иначе заголовок визуально «уезжает вправо». Выравнивание строки — по верху (`flex-start`), чтобы аватар держался у заголовка, а не проваливался к низу при наличии CTA. **(2) Два блока:** слева колонка «заголовок (17/700) + превью (одна строка, многоточие)», справа колонка «время сверху + счётчик непрочитанного под ним» (красный кружок канона, число mono). Время и счётчик выровнены по правому краю в одну вертикаль. **(3) Функциональный список:** строка целиком открывает тред; **один primary-CTA** на строке показывается ТОЛЬКО при реальном ожидающем действии (решение Рынка → «Снизить цену» `h.lower`; членство TURAN → его `cta` `h.member`) и вызывает ТОТ ЖЕ хендлер, что pinned-действие внутри треда («один объект — две поверхности»). Вторичные варианты (напр. «Варианты») живут внутри треда, не на строке списка. `cta` — производное поле `buildThreadList` (логика в data-слое, P4), не хардкод в экране. |
| R-23 | 2026-07-11 | По референсу Telegram (сам экран чата): «увеличь аватар в хедере», «убери аватар в бабле», «баблы как в Рынок», «удали правую иконку в хедере (шорткат к консультанту)», «CTA в бабле практически не заметно — добавь релевантные иконки, чтобы UI был понятнее» | Экран треда/чата = нормы Telegram-1:1. **(1) Аватар шапки — заметный** (глиф ~30 в контейнере 46). **(2) В баблах аватар НЕ показываем** (1:1-чат: собеседник один, аватар в каждом пузыре — визуальный шум); пузыри входящих выравниваются к левому краю без гаттера. **(3) Правый шорткат-иконка в шапке треда убрана** — Консультант достижим из списка/таба, дублировать вход в каждом треде не нужно. **(4) Действия в бабле — ВСЕГДА кнопки с релевантной PhIcon, никогда голый текст-ссылка** (ghost-текст «практически не заметен» = дефект): primary = solid тёмная (`--cta`), secondary = outline (`--bg-c` + `--bd-h`), обе высотой 40 с ведущей иконкой. Иконка едет в `ThreadMsgAction.icon` (data-слой): напр. «Снизить цену»→`tag`, «Отгружена»→`truck`, «Оценить»→`starOutline`, «Открыть стадо»→`cow`, «Открыть Ферму»→`sprout`, «Написать в TURAN»→`pencil`, членство→`checkCircle`. |
| R-24 | 2026-07-11 | «Улучши дизайн Empty State вкладки Рынок → офер на вступление» (гейт не-члена всё ещё был в старой пунктирной рамке) | Гейты/оферы-заглушки (не-член, «пусто в фильтре» и т.п.) используют тот же блоб-язык, что empty-state (R-6): органичный блоб `.mk-empty-art` + функциональный PhIcon, заголовок 18px/700, контент по центру, тёмный CTA. Пунктирная/сплошная прямоугольная рамка вокруг заглушки = дефект. Транзакционные плашки-действия (`ApprovedPlate` «оплатите взнос») остаются компактной плашкой — блоб только для заглушек-приглашений |
| R-25 | 2026-07-11 | «Есть аккаунт? Войти» реализован принципиально плохо | §4: переключатель входа/регистрации — общий атом `AuthAltAction`, не инлайн-верстка ×3. Одинаково в `StickyDock` под CTA на всех входных экранах (Login/Contact/RoleSelect); префикс — тихая некликабельная подпись, тап-таргет только слово-действие (accent, 44pt); без `›`-шеврона (кросс-линк, не drill-down). Дублирование интерактива инлайн-стилями = дефект. |
| R-26 | 2026-07-11 | Два фирменных лоадера (breathe/spin) на базе звезды TURAN → «это единственные 2 варианта во всём проекте» | §7: единственный лоадер проекта — `TuranLoader` (бренд-звезда, `#F7931E`), ровно два варианта: `breathe` (полноэкранная/страничная/boot) и `spin` (инлайн/кнопка/операция). Любой другой спиннер (lucide `Loader2`, `.mk-spin`/`.spin`, `IonSpinner`, самодельные `animate-spin`-кольца) = дефект. Действует во всём проекте, не только в фермерской зоне. |
| R-27 | 2026-07-12 | «Карточки сливаются — сделай компактнее»; «удали под главным CTA 2 подписи — так нигде и никогда не делай»; уточнение: «одну строку микрокопи под CTA можно, две — нельзя» | §4: **(1)** карточки-опции BigRadio = чипы R-18 (видимая граница `var(--bd)` + `bg-c` + зазор 8px, паддинг компактнее 12×14); прозрачная граница + `gap:0` (наследие ленты партий) → сливаются = дефект. Скоуп-класс `fw-opts`, глобальный `mk-stack8 gap:0` не трогаем. **(2)** под primary CTA максимум ОДНА строка микрокопи; 2 строки = дефект. В мастере фермы (F3/F4/F5) сняли обе — skip-ссылку «Пропустить вопрос» и note «сохраняется после каждого ответа» (эталон «skip под CTA» из R-13 ретирован; принцип R-13 «вторичное под, не между полем и CTA» остаётся для auth). |
| R-29 | 2026-07-15 | Лоадер при запуске: «убери лоадер-линию, оставь только иконку логотипа»; «поправь — лучи пересекаются друг с другом, используй оригинальный логотип» (+ готовый веб-сниппет анимации от дизайнера) | §7: **(1)** `BootScreen` = только иконка-марка, без полосы-индикатора и без текстовой подписи (полоса под лого = дефект). **(2)** Геометрия — 1:1 из оригинального `public/turan-icon.svg`; анимация — из веб-сниппета дизайнера (`turan-loader-web.html`). **(3)** `.tl-ray { transform-box: fill-box }` обязателен — иначе `transform-origin` луча считается от общего viewBox, лучи расходятся и пересекаются (это и была причина «пересечения лучей»). Цвет — фирменный `#F7931E`. |
| R-28 | 2026-07-13 | Полевая жалоба «клики без отклика» → аудит нативности `/cabinet` (2026-07-13): ключевые кнопки шторок/чата/списков имели только `:hover`, на касании «мертвы» | §5: тактильный отклик обязателен для КАЖДОГО интерактива. `:hover`-only без `:active`/pressed = дефект на таче (нативное приложение отвечает на каждый тап). Норма: `transform: scale(.985)` / затемнение / подложка (образец `.mk-cta:active`); hover-правила интерактивов — внутри `@media (hover:hover)` (иначе подсветка «залипает» после тапа); тап-таргет ≥44pt, мелкие точки/чипы добирают хит-зону `::before`. Пакет «Отклик на касание» Этапа 2 закрыл `.cta`/`.mp-cta`/`.thr-cta`/`.msg-act`/`.mk-menu-item`/`.mk-tab`/`.big-radio`/ban-dot и др. |

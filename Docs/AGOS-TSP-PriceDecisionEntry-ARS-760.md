---
status: agreed           # гейт замысла проведён 2026-09-21: три решения владельца закрыты, живых Open Questions нет, ревью качества — 18 дыр по 5 осям, все закрыты правкой до гейта; код (/build) — после подписи g2_approved
g2_approved: 2026-09-21 · Dias Zhagaparov
baseline_commit: 77974be13f8b3eee8ca5610d151bd1d7b2358c66   # зафиксирован /build 2026-09-21 до первой правки кода
brain_spec: [[projects/agos/specs/tsp-farmer-sell-flow]]
task_home: ARS-760 (Linear, team ARS, проект «Рынок TSP»)
---

# TSP — в точку решения по цене попадает только та партия, которой отказали

<frozen-after-approval>

## Intent

Точка решения по цене (`awaiting_price_decision`) задумана как ответ на **отказ рынка**:
канон `MS4-BT-09` — `offering → awaiting_price_decision`, условие «**all Offers expired**».
Смысл для фермера: «покупатели посмотрели и не взяли по этой цене — снижаем?».

Задеплоенное правило спрашивает не про отказ, а про возраст: берёт **любую** непроданную
партию на рынке, включая ту, которой никто ничего не предлагал. Расширение сделано
сознательно («партия без подходящего пула висела вечно»), но лекарство оказалось хуже
болезни: партию, которую **никто не смотрел**, система трактует как отвергнутую и раз за
разом опускает ей цену.

Замерено на проде: партия `c6ef90f8` прошла 1700 → 1600 → 1500 → 1400 при **нуле** офферов
за всё время — минус 300 ₸/кг, около 3 млн ₸ на 25 головах. Партия `f473d040` висит в точке
решения **с 2 июля**, офферов у неё тоже ноль. То есть известны **две** пострадавшие партии,
и обе — без единого предложения; ни одной пострадавшей «по отказу рынка» не найдено.
Оговорка измерения: счёт по `awaiting_price_decision_at` даёт занижение — снижение цены
обнуляет это поле, поэтому партия, уже вышедшая из точки решения, в него не попадает.

Слайс возвращает правилу его смысл. Он не строит расписание (`ARS-694`) и не заводит
уведомлений — партия без интереса рынка просто остаётся продаваться, как и сказал владелец
бизнеса 21.09: «батч висит долго».

## Boundaries & Constraints

**Always**
- **FR-001** Партия уходит в точку решения **только** из статуса `offering` и **только**
  если рынок отказал ей **по текущей цене**: среди офферов, выпущенных в **нынешнем выходе
  на рынок** (`offers.expires_at >= batches.published_at`), есть хотя бы один `expired` и
  **нет ни одного** `pending`. Возраст партии основанием не является ни сам по себе, ни как
  со-условие — члена возраста в предикате нет вовсе.
  **Почему «в нынешнем выходе», а не «вообще»:** `rpc_lower_price` возвращает партию в
  `published`, переписывает `published_at = now()` и офферы **не трогает** (проверено на
  проде). Предикат «есть `expired`, нет `pending`» без окна выполнился бы немедленно после
  снижения цены — и партия ушла бы обратно в точку решения, не показав рынку новую цену
  ни секунды. Храповик тогда не исчезает, а меняет топливо с возраста на протухшие офферы.
  **Почему мерой окна служит `expires_at`, а не `created_at`** (правка 21.09, `/build`, до
  первой строки кода): у `offers` есть `unique (batch_id, mpk_org_id)` — одна строка на пару
  «партия ↔ комбинат» навсегда, поэтому повторная рассылка не вставляет новую строку, а
  обновляет старую, и **`created_at` при этом не трогается** ни в одном живом писателе
  (`rpc_self_auto_match_batch` — оба апсерта, `rpc_retry_match_pool`, `rpc_lower_batch_price`).
  С окном по `created_at` оффер того же комбината в новом круге оставался бы «старше» нового
  `published_at`, то есть вне окна, — и `M-012` был бы недостижим, а канонический путь `BT-09`
  умер бы для всех повторных кругов. `expires_at` апсерт обновляет всегда, поэтому оно и
  отделяет нынешний круг от прошлого. `M-011` при этом сохраняется: у прежнего `expired`-оффера
  `expires_at` лежит в прошлом, то есть раньше нового `published_at`.
- **FR-002** Оффер гаснет **по своему собственному сроку**: `pending` с `expires_at < now()`
  → `expired`, независимо от статуса партии и от того, сколько у неё продано голов.
  **Охват строк — как сегодня:** гасятся офферы на партиях организации вызывающего
  (`fn_my_org_ids()`), не шире. Слайс не расширяет права записи: `SECURITY DEFINER`,
  пишущий строки чужих организаций, — отдельное решение, которого здесь никто не принимал.
  Следствие, названное вслух: до `ARS-694` оффер гаснет, только когда **продавец** откроет
  кабинет, — та же зависимость от вкладки, что и сегодня.
  Без этого правила `FR-001` недостижим: сегодня единственный, кто гасит офферы по времени,
  — тот самый свип, который `FR-001` сужает, и сужение без `FR-002` остановило бы
  канонический путь `BT-09` навсегда.
- **FR-003** Партия, которой **не делали предложений**, остаётся на рынке: статус не
  меняется, цена не меняется, «Требует решения» не появляется. Сколько бы она ни висела.
- **FR-004** Ни один автоматический путь не **снижает** цену партии: снижение выполняет
  только явное действие фермера (`rpc_lower_price`). Существующие писатели цены по чужому
  действию — `rpc_set_batch_terms` и админский `rpc_admin_set_batch_terms` — **сохраняются**
  (`HS-2`, `P7`); требование запрещает автоматику, а не людей. **Прибор:** в диффе слайса
  нет ни одной новой записи в `batches.farmer_price_per_kg`.
- **FR-005** Разовый ремонт: партии, попавшие в точку решения **не по отказу рынка** —
  то есть у которых в нынешнем выходе нет ни одного `expired`-оффера (ноль офферов вовсе,
  либо только `withdrawn`/`rejected`) — возвращаются на рынок в статус `published` с
  **текущей** ценой. Предикат ремонта — зеркало `FR-001`, а не у́же его: иначе партия,
  застрявшая со снятыми офферами, осталась бы без выхода навсегда (новое правило её туда
  больше не пустит, а ремонт бы не достал).
  Ремонт идёт **после** правки правила — иначе первый же открытый кабинет утащит их обратно.
- **FR-014** Возврат — полноценная смена состояния, а не правка одного поля:
  `awaiting_price_decision_at` обнуляется, `published_at` ставится в `now()` (партия выходит
  на рынок заново — и тем самым окно `FR-001` начинается с чистого листа), `offering_at`
  остаётся пустым. Пишется событие в `batch_events` с типом существующего словаря и
  `created_by = NULL` (ремонт идёт скриптом, пользователя нет — колонка это допускает).
  Массовая смена статусов на проде без следа в журнале недопустима.
- **FR-006** Ремонт **не восстанавливает** уже сниженные цены: какая цена стоит, с такой
  партия и возвращается. Решение владельца 21.09 по партии `c6ef90f8` — оставить 1400.
  Переписывать историю торга задним числом слайс не берётся.
- **FR-007** Ремонт идемпотентен: повторный прогон не меняет ни одной строки.
- **FR-008** Сигнатуры существующих RPC не меняются (`P7`), новых функций для клиента слайс
  не заводит. Правятся тела **двух** функций: `rpc_self_review_due_batches` (правило) и
  `rpc_lower_batch_price` (одна аддитивная строка `published_at = now()`).
  **Про вторую — прямо, потому что она добавлена после подписи** (ревью якоря 7, решение
  владельца 21.09): канонический `rpc_lower_batch_price` задеплоен на прод, ставит
  `offering` безусловно и `published_at` не обновляет — то есть снижение цены через него
  не начинает новый круг, протухшие офферы прошлого круга остаются в окне `FR-001`, и
  партия без подходящего МПК уезжает обратно в точку решения на первом же прогоне свипа.
  Без этой строки слайс лечил бы храповик на одном пути и оставлял на другом. Тело взято
  **с прода** (`pg_get_functiondef`), а не из `d02_tsp.sql`: версия в `d02` — Слайс-9-aware
  и не выкладывалась (`IMPL_DEBT` `DEBT-PROD-DRIFT-01`), выложить её отсюда значило бы
  протащить дробление партий под видом однострочной правки. Права доступа не меняются.
  **Про снапшот контрактов — прямо, потому что посылка «останется без изменений» ложна:**
  сегодня `contracts/rpc_return_keys.txt:157` содержит `moved,trigger`, то есть описывает
  **старое** тело (живое отдаёт `moved`/`afterMinutes`) — миграция `20260702200000` в
  `SQL_FILES` не входит, и `CHECK 11` её не видит (дом проблемы — `ARS-757`). Слайс
  регистрирует свою миграцию в `SQL_FILES`, и эта строка снапшота **изменится**. Значит
  снапшот перегенерируется и в том же PR появляется запись в `DECISIONS_LOG`
  (`D-RPC-CONTRACT-SYNC-01`) — «снапшот молчал, потому что не смотрел», а не «контракт
  сломали».
- **FR-015** Правило идемпотентно и безопасно при параллельных прогонах: свип зовётся из
  каждой открытой вкладки раз в 20 с, а `Design contract` разбивает его на два шага. Повтор
  не создаёт второго события `price_decision_due` по одной партии и не теряет обновления.

**Never**
- **FR-009** Слайс **не строит расписание** (`ARS-694`). Оба правила по-прежнему исполняются
  из открытого кабинета — то есть срабатывают, только когда фермер зайдёт. Это
  преэкзистентное ограничение, слайс его не чинит и не ухудшает.
- **FR-010** Слайс **не заводит уведомлений** о том, что партия долго без интереса. Решение
  владельца 21.09 — вариант A: ничего не делать, партия просто продаётся. Уведомление как
  мягкий сигнал остаётся возможным будущим, дом — `ARS-685`.
- **FR-011** Слайс не меняет **значения** порогов (`price_decision_after_minutes = 1440`,
  `offer_window_hours = 24`) — они остаются в `tsp_config` (`P8`). Но роль первого меняется
  и это сказано вслух: после `FR-001` он больше не решает, когда партия уходит в точку
  решения (решают офферы), и остаётся только тем, чем был до Слайса 8 — сроком, через
  который партии рассылаются офферы. Ключ `afterMinutes` в ответе сохраняется (`FR-008`),
  чтобы не ломать форму; мёртвым конфиг не становится, но его смысл сужается.
- **FR-012** Слайс не чинит подмену введённой цены (`ARS-755`), храповик шага и тексты
  экрана решения — там свой дом. Тексты при этом **становятся правдой сами**: после `FR-001`
  в точку решения попадает только партия, которой действительно отказали.
- **FR-013** Слайс не трогает частичную привязку (`ARS-754`) и пути закрытия заявки
  (`ARS-736`).
- **FR-016** Канон и сценарии правятся **в том же PR**, а не «потом» (`D-RPC-CONTRACT-SYNC-01`,
  урок ARS-279). Два места названы поимённо, потому что слайс делает их текст ложным:
  ① `MS4 §3.3` — таблицы переходов `awaiting_price_decision → published` в каноне **нет**
  (есть только `BT-11 → offering` и `BT-12 → cancelled`); возврат из `FR-005` вводит новый
  переход, и канон получает на него строку. ② `qa/scenarios/05-tsp-farmer.md` `TSPF-LIFE-19`
  утверждает «переход offering → decision управляется конфигом
  `price_decision_after_minutes`» — после `FR-001` им управляет истечение офферов.

## I/O & Edge-Case Matrix

| id | сценарий | вход / состояние | ожидаемое | обработка ошибки |
|----|----------|------------------|-----------|------------------|
| M-001 | партия без интереса остаётся на рынке | `published`, офферов ноль, возраст втрое больше `price_decision_after_minutes` | статус и цена не изменились, точки решения нет | — |
| M-002 | рынок отказал — точка решения появляется | `offering`, был оффер, срок его прошёл | оффер `expired`, партия → `awaiting_price_decision` | — |
| M-003 | оффер ещё жив — партию не трогают | `offering`, оффер `pending`, срок не прошёл | партия остаётся `offering`, оффер `pending` | — |
| M-004 | оффер гаснет независимо от партии | `pending` с прошедшим сроком на частично проданной партии | `expired` (сегодня не гаснет никогда) | — |
| M-005 | офферы сняты, а не отвергнуты | все офферы партии `withdrawn`, ни одного `expired` | точки решения нет — отказа по цене не было | — |
| M-006 | чужие статусы неприкосновенны | офферы `accepted` / `rejected` с прошедшим сроком | не изменены | — |
| M-007 | ремонт возвращает застрявших | партия в точке решения, офферов ноль | → `published`, цена прежняя, снова видна комбинатам | — |
| M-008 | ремонт не трогает законно застрявших | партия в точке решения, у неё есть `expired`-оффер | осталась в точке решения | — |
| M-009 | повтор ремонта | ремонт прогоняется дважды | второй прогон: 0 изменённых строк | — |
| M-010 | ремонт после правила, а не до | партия возвращена на рынок, кабинет открыт | обратно в точку решения **не** уходит | — |
| M-011 | снижение цены не отправляет в точку решения сразу | партия была в точке решения, фермер снизил цену, подходящего МПК нет | остаётся `published` с новой ценой; прежние `expired`-офферы в окно не попадают | — |
| M-012 | новый круг проходит канонически | после снижения цены партии сделали оффер, он истёк | **тогда** партия снова уходит в точку решения | — |
| M-013 | повтор и параллельный прогон | свип зовётся дважды подряд и из двух вкладок | второе событие `price_decision_due` по той же партии не создаётся | — |
| M-014 | контракт ответа под наблюдением | миграция зарегистрирована в `SQL_FILES` | `CHECK 11` видит живую форму ответа; изменение снапшота объяснено записью в `DECISIONS_LOG` | — |

</frozen-after-approval>

## Design contract (the «how» — сигнатуры, НЕ тела)

- **Data model.** Новых сущностей, таблиц, колонок нет. `offers.expires_at`, `offers.status`
  (CHECK уже допускает `expired`), `batches.status` — существующие.
- **API / RPC.** Сигнатуры не меняются. Правится **тело** `rpc_self_review_due_batches()`:
  два независимых шага вместо одного связанного — (а) погасить офферы по их сроку,
  (б) перевести в точку решения партии, у которых после шага (а) есть `expired` и нет
  `pending`. Форма ответа сохраняется; при необходимости счётчик погашенных офферов
  добавляется **аддитивным** ключом (`P7`), и тогда снапшот контрактов обновляется в том же
  PR с записью в `DECISIONS_LOG` (`D-RPC-CONTRACT-SYNC-01`).
- **Артефакт.** Новая миграция в `supabase/migrations/` поверх `20260702200000` + скрипт
  ремонта в `scripts/deploy/`. Выкладка — `scripts/deploy.py --files <файл>`. Миграция
  регистрируется в `SQL_FILES` (`cross_check.sh`) — иначе её контракты не проверяет никто.
- **Events.** Существующий `price_decision_due` пишется как сегодня. Канонический
  `market.offer.expired` (`Dok 4`, долг `TSP-FLOW-06`) слайс **не** эмитит — вне скоупа.
- **UI contract.** Экраны не меняются. Наблюдаемое отличие: партия без предложений больше не
  исчезает с рынка и не дешевеет сама.

## Slices → Tasks

| задача | тир | закрывает id | приёмка (глазами фермера) |
|--------|-----|--------------|----------------------------|
| **C-1** Оффер гаснет по своему сроку | semantic | FR-002, M-004, M-006 | Комбинат: моё предложение перестаёт числиться живым ровно тогда, когда истёк его срок — а не когда «созрела» чужая партия |
| **C-2** В точку решения — только после отказа рынка | semantic | FR-001, FR-003, FR-004, M-001…M-003, M-005 | Партию, которую никто не смотрел, никто и не уценивает |
| **C-3** Ремонт застрявших | semantic | FR-005, FR-006, FR-007, M-007…M-010 | Моя партия снова продаётся, а не висит в невидимом ожидании |

## Verification (G3 inputs)

- **SQL-тест** на боевой базе в откатываемой транзакции (как `tests/ars_731_*`):
  `M-001`…`M-010`, каждое утверждение называет свой `M-id`.
- **Фальсифицируемость:** тест обязан падать без миграции слайса.
  ~~Отдельно проверяется `M-011` — он и есть прибор на петлю: без окна `published_at`
  партия после снижения цены уходит в точку решения на первом же прогоне свипа.~~
  **Опровергнуто прогоном 21.09 (ревью якоря 7), исправлено здесь же:** `M-011` прибором
  на окно НЕ является. После `rpc_lower_price` партия получает статус `published`, а шаг
  (б) берёт только `offering` — то есть `M-011` проходит на гейте статуса независимо от
  окна. Проверено: с полностью удалённым членом окна зелены все 13 сценариев матрицы.
  Прибором на сам член окна служит отдельная фикстура `FR-001 (окно)`: партия снова в
  рассылке (`published_at = now()`), единственный оффер — протухший от прошлого круга
  (`expires_at` раньше `published_at`), `pending` нет; без окна она уезжает в точку
  решения, с окном остаётся в `offering`. Падение прибора при удалении окна показано.
  Прибор на выбор ПОЛЯ окна — `M-012` (подмена на `created_at` роняет ровно его).
  Прибор на расширение предиката ремонта — отдельная фикстура `FR-005 (зеркало)`:
  партия со снятыми офферами; сужение предиката до «ноль офферов» роняет ровно её.
- **`FR-004`** — прибор диффом: ни одной новой записи в `batches.farmer_price_per_kg`.
- **`FR-016`** — прибор диффом: строка перехода в `MS4 §3.3` и правка `TSPF-LIFE-19` лежат
  в этом же PR.
- **Ремонт:** счётчик до/после, повторный прогон без изменений; контрольный замер — партий в
  точке решения без единого оффера ноль.
- `cross_check.sh` (CHECK 11 без изменений либо обновлён вместе с `DECISIONS_LOG`); `tsc -b`;
  сборка; `npx vitest run --project routers`.
- Сценарии `qa/scenarios/`: `TSPF-LIFE-19` (таймер цены), `TSPM-OFF-02`.
- После выкладки — `python3 scripts/prod_diff.py`, дрейф 0.
- `converge intent↔reality` по этому слайсу.

## Open Questions

> Спек не покидает `draft`, пока жив вопрос.

Живых вопросов нет — три решения владельца приняты 2026-09-21 и перенесены в
`Clarifications`.

## Clarifications  (append-only)

- [2026-09-21] Q: сужать ли правило до канона, зная, что широкий предикат — осознанное
  расширение с записанной причиной («партия без подходящего пула висела вечно»)? → A:
  **сужать** · отвергнуто: оставить как есть (тогда планировщик `ARS-694` сделал бы
  снижение цены автоматическим и ежесуточным).
- [2026-09-21] Q: что делать с исходной болью — партия без подходящего пула висит бессрочно?
  → A: **вариант A, ничего** (`FR-010`): висит, это выбор фермера · отвергнуто: уведомление
  через N дней (без работающей доставки `ARS-685` — пустой звук на пилоте); точка решения
  без снятия с рынка (меняет модель состояний за неделю до пилота).
- [2026-09-21] Q: где живёт правило истечения офферов — здесь или в `ARS-694`? → A:
  **здесь** (`FR-002`). **Отменяет** запись `DECISIONS_LOG.md` от 18.09 (выкладка ARS-731):
  «свип истечения смотрит не на срок предложения, а на „созревание“ партии … дом — `ARS-694`,
  чинится там же» — дом переехал, и это сказано здесь, чтобы у факта не осталось двух домов
  (`P4`) · отвергнуто: оставить в спеке планировщика (обнаружена кольцевая
  зависимость: сужение `FR-001` без `FR-002` останавливает канонический путь `BT-09`
  навсегда, потому что единственный гасящий офферы по времени — сужаемый свип).

- [2026-09-21] Q: `FR-001` мерил окно по `offers.created_at`, но апсерт офферов это поле не
  обновляет (`unique (batch_id, mpk_org_id)` → повторная рассылка переиспользует строку), из-за
  чего `M-012` недостижим, а `M-011` и `M-012` вместе неисполнимы. Каким полем мерить круг? → A:
  **`expires_at`** (Диас, 21.09, до первой строки кода) · отвергнуто: оставить `created_at` и
  добавить `created_at = now()` в `on conflict do update` трёх живых функций — шире радиус (три
  чужие функции вместо одного предиката) и колонка перестала бы означать «когда предложение
  возникло» (`P12`).

## Assumptions  (append-only, ВНЕ frozen)

- [2026-09-21] **«Отказ рынка» = есть `expired`, нет `pending`.** Партия, у которой все
  офферы `withdrawn` (сняты при матче в другом месте), в точку решения не идёт: её цену никто
  не отвергал (`M-005`). Опровергло бы: решение считать снятие оффера тоже отказом.
- [2026-09-21] **Истечение офферов исполняется из существующего кабинетного свипа** — нового
  вызывающего слайс не заводит. Значит до `ARS-694` офферы гаснут только при открытом
  кабинете фермера, как и сегодня. Опровергло бы: требование гасить офферы независимо от
  фермера до появления планировщика.
- [2026-09-21] **Возврат — в `published`, а не в `offering`.** Партия возвращается на рынок
  «как новая»: прежние офферы истекли, рассылать заново слайс не берётся. Опровергло бы:
  решение перерассылать офферы при возврате.

## Implementation Notes  (append-only)

- [2026-09-21] **Matrix Test Audit** — строка на каждый `M-id`, сверка ПО ID. Прогон:
  `cat` миграции + части 1 теста + скрипта ремонта ДВАЖДЫ + части 2 теста →
  `python3 scripts/run_sql_rollback.py` (боевая база, откатываемая транзакция —
  изолированной среды нет, `QA-ENV-ISOLATION-01`).

  | id | тест | строка «прогнано и прошло» |
  |----|------|----------------------------|
  | M-001 | `ars_760_..._test.sql` | `ARS-760 M-001 OK: партия без интереса рынка осталась published по цене 1400` |
  | M-002 | там же | `ARS-760 M-002 OK: оффер expired, партия в точке решения` |
  | M-003 | там же | `ARS-760 M-003 OK: живой оффер и партия не тронуты` |
  | M-004 | там же | `ARS-760 M-004 OK: оффер погас по своему сроку, статус партии не тронут` |
  | M-005 | там же | `ARS-760 M-005 OK: снятые офферы отказом не считаются` |
  | M-006 | там же | `ARS-760 M-006 OK: accepted и rejected не тронуты` |
  | M-007 | `ars_760_..._repair_test.sql` | `ARS-760 M-007 OK: партия вернулась на рынок по прежней цене 1400` |
  | M-008 | там же | `ARS-760 M-008 OK: законно застрявшая партия осталась в точке решения` |
  | M-009 | там же | `ARS-760 M-009 OK: второй прогон ремонта не изменил ни строки` |
  | M-010 | там же | `ARS-760 M-010 OK: открытый кабинет не утащил возвращённую партию обратно` |
  | M-011 | `ars_760_..._test.sql` | `ARS-760 M-011 OK: партия осталась published с новой ценой 1300` |
  | M-012 | там же | `ARS-760 M-012 OK: новый круг довёл партию до точки решения` |
  | M-013 | там же | `ARS-760 M-013 OK: три прогона подряд дали ровно одно событие` |
  | M-014 | **теста на данных нет** — прибор из трёх грепов, id назван в выводе | `M-014 (1/3) OK … (2/3) OK … (3/3) OK … M-014 ПРОЙДЕН` |

  Сверх матрицы (добавлено по ревью якоря 7, `M-id` не заводили — матрица заморожена):

  | требование | тест | строка «прогнано и прошло» |
  |------------|------|----------------------------|
  | FR-001 (окно) | `ars_760_..._test.sql` | `ARS-760 FR-001 (окно) OK: протухший оффер прошлого круга отказом не считается` |
  | FR-005 (зеркало) | `ars_760_..._repair_test.sql` | `ARS-760 FR-005 (зеркало) OK: партия со снятыми офферами возвращена на рынок` |
  | FR-001 (второй путь) | `ars_760_..._test.sql` | `ARS-760 FR-001 (второй путь) OK: канонический rpc_lower_batch_price начал новый круг, статус «offering»` |

  **Отклонение, названное вслух: M-014.** Это единственная строка матрицы, у которой
  нет теста, называющего свой id в SQL-прогоне, — и по конструкции быть не может:
  требование про регистрацию файла в `SQL_FILES`, форму снапшота и наличие записи в
  `DECISIONS_LOG` проверяется по файлам репозитория, а не по данным. Прибором служат
  `cross_check.sh` `CHECK 11` (`OK: 175 rpc return-contracts match snapshot`) плюс три
  грепа, чей вывод называет `M-014` явно. Строка засчитана покрытой именно этим
  прибором, а не «по смыслу».

- [2026-09-21] **Фальсифицируемость проверена прогоном, а не заявлена.**
  ① Без миграции слайса тест падает на `M-001` («партия без единого оффера ушла в
  awaiting_price_decision») — то есть ловит ровно тот дефект, который слайс лечит.
  ② Подмена меры окна обратно на `created_at` (`sed` по телу миграции) роняет ровно
  `M-012`, а `M-001…M-011` проходят — значит `M-012` действительно является прибором на
  выбор поля, а не декларацией.

- [2026-09-21, после ревью якоря 7] **Приборов стало четыре, и все четыре показаны падением.**
  Запись выше (①–②) была неполной: она описывала приборы на *тело правила* и на *выбор
  поля окна*, но не на *сам факт наличия* сужающих членов. Ревью это вскрыло, добавлены
  ③ и ④; ни один `M-id` матрицы при этом не заведён — оба прибора закрывают буквы
  замороженных требований `FR-001` и `FR-005`, а матрица заморожена.
  ③ Удаление члена окна из `exists`-ветки → падает `FR-001 (окно)`; до правки не падало
  ничего (проверено: зелены все 13).
  ④ Сужение предиката ремонта до «ноль офферов» во всех трёх местах скрипта → падает
  `FR-005 (зеркало)`; до правки не падало ничего.
  ⑤ Подложенный дубликат `create or replace` той же функции в том же файле → `cross_check`
  `CHECK 1` даёт `CRITICAL` (до исправления порядка веток — молчал).
  ⑥ Удаление строки `published_at = now()` из `rpc_lower_batch_price` → падает
  `FR-001 (второй путь)`: партия после снижения цены возвращается в точку решения на
  первом же прогоне свипа. То есть храповик на втором пути был реальным, а не
  предполагаемым.

- [2026-09-21] **Боевой замер до выкладки** (сухой прогон ремонта, `ROLLBACK`): партий в
  точке решения **без** отказа рынка — **1**, по отказу — **0**. Единственная застрявшая
  — `f473d040` (20 голов, цена 2100, `published_at` = 2026-07-02, офферов ноль), та самая,
  что названа в `Intent`. Партия `c6ef90f8` в замер не попадает: она уже вышла из точки
  решения снижением цены — это и есть оговорка измерения из `Intent`.

## Spec Change Log  (append-only)

- [2026-09-21] **intent_gap · FR-001: мера окна `created_at` → `expires_at`.**
  Найдено на якоре 6 **до первой строки кода**: замороженное требование опиралось на ложную
  посылку о хранении офферов (что повторная рассылка создаёт новую строку). Проверено по коду:
  `d02_tsp.sql:1471` (`unique (batch_id, mpk_org_id)`), апсерты
  `20260918120000:299`/`:462`, `d02_tsp.sql:3060`, `d02_tsp.sql:3656` — `created_at` не в
  `do update` ни у одного. Правка: предикат окна + абзац обоснования внутри `FR-001`.
  propagated: `FR-005` (зеркало `FR-001`, поля не называет — правка не нужна),
  `M-011`/`M-012` (поля не называют — правка не нужна), `Clarifications` (решение записано).
  Замороженный блок изменён → подпись `g2_approved` переставлена человеком заново.

- [2026-09-21] **scope · FR-008: слайс правит два тела вместо одного.**
  Найдено на якоре 7 ревьюером краевых случаев и подтверждено на живом проде: канонический
  `rpc_lower_batch_price` ставит `offering` безусловно и `published_at` не трогает, поэтому
  храповик переживал слайс через второй путь снижения цены. Решение владельца 21.09 —
  чинить в этом же PR (отвергнуто: оставить с долгом — путь открыт по HTTP и помечен
  `[AI]`; отвергнуто: отозвать права у `anon`/`authenticated` — сломало бы AI-шлюз, если он
  её зовёт, и это другой класс решения). Правка: одна аддитивная строка `published_at =
  now()`; тело взято с прода, не из `d02`.
  propagated: `FR-008` (назвал вторую функцию), `§Verification` (добавлен прибор
  `FR-001 (второй путь)`), канон `BT-11`/`BT-20`, `cross_check.sh` (`DUP_WHITELIST`),
  `DECISIONS_LOG`, `IMPL_DEBT` (`SEC-CANON-RPC-ANON-EXECUTE-01`).
  Замороженный блок изменён → подпись `g2_approved` требует третьей перестановки человеком.

## Review Triage Log  (append-only)

Якорь 7, итерация 1. Ревьюеры оценок не ставят — оценка ниже моя, проверена на месте.

| # | находка (источник) | вердикт + чем проверено | маршрут |
|---|--------------------|--------------------------|---------|
| 1 | **Окно `expires_at >= published_at` не закреплено ни одним утверждением**: `M-011` проходит на гейте статуса, `M-012` проверяет, что окно не мешает (дыры в проверке) | **high.** Проверено прогоном, а не рассуждением: член окна удалён целиком → **зелены все 13 сценариев**. Моё утверждение в `§Verification`, что `M-011` — прибор на петлю, было ложным | **patch** — добавлена фикстура `FR-001 (окно)`; её падение при удалении окна показано прогоном. `§Verification` исправлен |
| 2 | **Расширенный предикат ремонта (партии со СНЯТЫМИ офферами) не проверяется ни тестом, ни контрольным замером скрипта** (дыры в проверке) | **high.** Проверено: сужение предиката во всех трёх местах скрипта → падает ровно новый прибор, до правки не падало ничего. Это ровно тот класс, ради которого `FR-005` предикат расширяет | **patch** — фикстура `FR-005 (зеркало)` (офферы `withdrawn`+`rejected`); контрольный замер скрипта приведён к предикату ремонта вместо более узкой формулировки `§Verification` |
| 3 | **`DUP_WHITELIST` снимает и проверку дубля ВНУТРИ файла** — сторож `L-1` для имени выключен (дыры в проверке) | **medium.** Проверено на месте: `continue` стоял до цикла подсчёта. Вред отложенный — правило слайса могло бы быть молча перекрыто дубликатом, и ни `CHECK 11`, ни `prod_diff` не показали бы (оба берут последнее определение) | **patch** — порядок веток в `CHECK 1` исправлен для ВСЕХ имён; проверено подложенным дубликатом: ловится как `CRITICAL`, после отката — зелено |
| 4 | **Канон противоречит сам себе**: `BT-20` добавлен в таблицу §3.3, но mermaid-FSM §3.2 по-прежнему знает из `awaiting_price_decision` только два выхода (сверка замысла) | **medium.** Проверено чтением §3.2. Буква `FR-016` выполнена, смысл — нет: документ утверждает два разных набора переходов | **patch** — стрелка `BT-20` добавлена на диаграмму, ярлык `BT-09` уточнён («all Offers of current round expired») |
| 5 | **`unrequested`**: значение `metadata.trigger` события `price_decision_due` сменено `review_due` → `market_refused` (сверка замысла) | **подтверждено.** Ни один `FR`/`M` этого не просит, `Design contract` говорит «существующий `price_decision_due` пишется как сегодня», читателей у ключа нет | **убрано.** Обосновать нечем: обоснование требует id, а id — решения владельца по замороженному блоку. Значение возвращено к `review_due`, причина записана комментарием в коде |
| 6 | **Член окна в отрицательной ветке (`not exists … pending`) недостижим**: шаг (а) уже погасил все просроченные `pending` тех же организаций (дыры в проверке) | **подтверждено как факт, вреда нет.** Отложенная публикация живёт в `scheduled_publish_at`, не в `published_at`, поэтому у уцелевшей `pending`-строки всегда `expires_at > now() >= published_at` | **оставлено с комментарием.** `FR-001` формулирует обе половины предиката «в нынешнем выходе на рынок»; убрать член — изменить букву замороженного требования. Избыточность названа в коде вслух |
| 7 | **`tests/*.sql` не подключены ни к одному раннеру** — ни CI, ни `package.json`; все 17 SQL-тестов запускаются только вручную (дыры в проверке) | **преэкзистентное**, к слайсу отношения не имеет | **defer** → `IMPL_DEBT.md` `QA-SQL-TESTS-NO-RUNNER-01` |
| 8 | **`tests/tsp_happy_path_test.sql` падает на фикстуре** — организация МПК зашита константой, пользователя в ней нет; тест числится прибором для пяти кейсов и не проверяет ничего (найдено при прогоне `§Verification`) | **преэкзистентное.** Прогнан дважды — с миграцией слайса и без; падение байт-в-байт одинаковое | **defer** → `IMPL_DEBT.md` `QA-HAPPY-PATH-FIXTURE-DEAD-01`. Регресс, ради которого его звали (принятие живого оффера), закрыт сценарием `M-003` |

Якорь 7, итерация 2 — ревьюер краевых случаев (пришёл позже двух других).

| # | находка | вердикт + чем проверено | маршрут |
|---|---------|--------------------------|---------|
| 9 | **Второй путь снижения цены обходит окно**: канонический `rpc_lower_batch_price` ставит `offering` безусловно и `published_at` не трогает — храповик выживает через него | **high, подтверждено на живом проде.** Функция задеплоена (`pg_get_functiondef` вернул тело), `published_at` в нём не встречается ни разу, `status = 'offering'` ставится без условия, `execute` выдан `authenticated` и `anon`. То есть путь достижим по HTTP, а не теоретический | **patch** (решение владельца 21.09 — чинить в этом же PR): одна аддитивная строка `published_at = now()` в том же `UPDATE`. Тело взято **с прода**, не из `d02` (там Слайс-9-aware версия, `DEBT-PROD-DRIFT-01`). Добавлен прибор `FR-001 (второй путь)`; без строки он падает — показано прогоном |
| 10 | **Заявление в комментарии миграции** «`M-011` сохраняется: у прежнего `expired`-оффера `expires_at` раньше нового `published_at`» верно только для `rpc_lower_price` | **подтверждено, тот же корень, что #9** | закрыто правкой #9: после неё утверждение верно для обоих путей |
| 11 | **`FR-016`① утверждает, что перехода `awaiting_price_decision → published` в каноне нет** — а `rpc_lower_price` выполняет его при каждом снижении цены с июля | **подтверждено.** Тело `rpc_lower_price` (`20260702200000:70-90`) ставит `status = 'published'`. Канон `BT-11` описывает переход в `offering`, чего задеплоенный фермерский путь не делает | **patch** — `BT-11` получил оговорку о реальности, `BT-20` теперь называет **оба** источника перехода, а не только скрипт ремонта |
| 12 | **Порядок «ремонт после правила» держался одним комментарием** | **подтверждено** чтением скрипта | **patch** — в ремонт добавлен сторож: читает комментарий задеплоенной функции и роняет прогон, если правило ещё старое |
| 13 | **Снижение цены не снимает старые предложения**: истечение оффера по СТАРОЙ цене читается как отказ от новой | **подтверждено.** В теле `rpc_lower_price` нет ни одного оператора по `offers`. До слайса дефект был латентным (правило не читало статусы офферов), слайс сделал его материальным, но не создал | **defer** → `IMPL_DEBT.md` `TSP-LOWER-PRICE-STALE-OFFERS-01`. Правка второго живого RPC — расширение объёма; вред ограничен основанием, а не фактом (партия реально побывала на рынке с новой ценой) |
| 14 | **Явный отказ (`rejected`) не считается отказом рынка** — а на проде есть живая партия `90940549` в `offering` с единственным `rejected`-оффером | **подтверждено на проде.** По старому правилу она получила бы экран решения по возрасту, по новому — не получит никогда | **решение владельца 21.09: оставить как решено.** `FR-005` и `Assumptions` относят `rejected` к «не отказу» сознательно. Следствие названо вслух и заведено в `IMPL_DEBT.md` `TSP-REJECTED-NOT-REFUSAL-01` |

**Общий корень находок 1 и 2** (один дефект породил обе): тесты проверяли только
**разрешающее** направление каждого предиката — что он пропускает то, что должен. Ни одной
фикстуры, которую предикат обязан **исключить**, в наборе не было, поэтому оба сужающих
члена — окно правила и расширение ремонта — можно было удалить, не уронив ни одного
утверждения. Маршрут обеих — `patch`: дефект в приборах, не в замысле и не в спеке.

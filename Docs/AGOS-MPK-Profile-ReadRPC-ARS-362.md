---
status: agreed           # гейт замысла пройден 2026-09-04; код (/build) — после подписи g2_approved
g2_approved: 2026-09-04 · Dias Zhagaparov
baseline_commit:         # пусто до /build
brain_spec: "[[projects/agos/specs/mpk-cabinet]]"
task_home: "Linear ARS-362 (MP-2.1) · эпик ARS-351 · агрегат «Обзора» вынесен в ARS-646"
---

# MP-2.1 · Читатель профиля организации — `rpc_get_org_profile`

> **Пространство id — своё.** `FR-001` этой спеки ≠ `FR-001` в
> `AGOS-Dok6-Slice10-MPK-Profile.md` (экранный контракт-потребитель). Ссылки на чужие id
> всегда с именем документа.
>
> Родня: `Docs/AGOS-MPK-Profile-EngSpec-v0_1.md` (архитектура, `D-MPK-*`) ·
> `Docs/AGOS-MPK-Profile-DataModel-ARS-359.md` (писатели и таблицы) ·
> `Docs/AGOS-Dok6-Slice10-MPK-Profile.md` §4/§6.1/§9 (кто это читает и с какими правами).

<frozen-after-approval>

## Intent

Снабженец мясокомбината открывает раздел «Предприятие» своей консоли и должен увидеть данные
своего предприятия: реквизиты, производственную площадку, банковские реквизиты и то, какие
критические поля сейчас на проверке у TURAN.

Сегодня он не видит почти ничего. Пять RPC `ARS-359` умеют только **писать**; ни одной читающей
функции для раздела не существует, а таблицы `mpk_profiles` · `mpk_sites` · `org_bank_accounts` ·
`org_field_reviews` закрыты от роли `authenticated` (`revoke all`), поэтому и прямое чтение из
UI невозможно. Клиенту доступны лишь название, БИН, регион и телефон из `rpc_get_my_context`.
Из-за этого сборка экрана (`ARS-624`) остановлена — PR #179.

Слайс даёт **один читатель своей организации**: одним вызовом на открытие вкладки отдаёт всё,
что экран показывает, и вместе с данными — эффективные права, чтобы экран знал, где он
read-only, а где нет.

## Boundaries & Constraints

**Always**

- **FR-001** Один вызов на открытие вкладки: реквизиты, площадка, банк, правки на проверке и
  эффективные права приходят одним ограниченным payload'ом.
- **FR-002** Владение проверяется **в базе**: вызывающий обязан состоять в запрошенной
  организации либо быть админом TURAN. `p_organization_id` от клиента не считается
  доказательством (класс дефекта `VET-02`).
- **FR-003** «Не участник» и «организации не существует» дают **один и тот же** отказ — ответ не
  подтверждает существование чужой организации.
- **FR-004** Банковские реквизиты приходят только держателю `mpk.bank.manage` или админу.
  Остальным приходит явный признак «доступ закрыт» — не молчаливое отсутствие блока: раздел
  остаётся читаемым, под правом лежит только этот подблок
  (`AGOS-Dok6-Slice10-MPK-Profile.md` §6.1, задеплоенная политика
  `org_bank_accounts_read_authorized`).
- **FR-005** Ответ несёт поле версии контракта — потребитель может отличить редакции.
- **FR-006** Append-only история видима: актуальная запись и история приходят **разными ключами
  ответа** (актуальная отдельно, история — списком по убыванию версии), а не одним списком, где
  актуальность надо вычислять на клиенте. Относится к версиям банковских реквизитов и к
  разрешённым правкам критических полей; ничего не скрывается и не удаляется.
- **FR-007** Аддитивно (`P7`): новых таблиц нет, сигнатуры существующих RPC не меняются, писатели
  `ARS-359` не трогаются.
- **FR-008** Ошибки типизированы и безопасны для показа: наружу идёт код и человекочитаемая
  причина, никогда не текст SQL-исключения.
- **FR-014** Первый ответ вкладки укладывается в **p95 < 200 мс**. Замер проводится на
  сид-данных той среды, где идёт приёмка; изолированной среды нет
  (`IMPL_DEBT` QA-ENV-ISOLATION-01), поэтому **условия замера называются в пакете G3** — иначе
  цифра не сравнима между прогонами. Требование живёт здесь, а не только в трекере: иначе на
  приёмке провал неотличим от успеха.

**Never**

- **FR-009** Слайс не строит агрегат «Обзора» (`rpc_get_mpk_profile_overview`) — дом **ARS-646**.
- **FR-010** Слайс не строит карточку МПК для фермера и **не решает раскрытие по состоянию
  сделки** — решение владельца ожидается 07.09. Карточка будет **отдельной проекцией и
  единственным домом факта раскрытия**; когда она появится, тело `rpc_get_org_profile`
  переводится на неё, второй источник для превью
  (`AGOS-Dok6-Slice10-MPK-Profile.md` §4.2) не заводится.
- **FR-011** Слайс не чинит мёртвые SELECT-политики и гранты
  (`IMPL_DEBT` MPK-PROFILE-READ-PATH-01) — дом **ARS-358**. Путь `SECURITY DEFINER` их не
  требует.
- **FR-012** Слайс не пишет UI и не трогает `useMpkProfile`/`useOrganization` — дом **ARS-624**.
- **FR-013** Читатель ничего не мутирует и не эмитит событий.

## I/O & Edge-Case Matrix

| id | сценарий | вход / состояние | ожидаемое | обработка ошибки |
|---|---|---|---|---|
| **M-001** | happy path | участник организации с `mpk.profile.edit` и `mpk.bank.manage` | полный payload: организация · профиль · площадка · банк · правки на проверке · права | — |
| **M-002** | профиль ещё не заполнен | `mpk_profiles`/`mpk_sites`/банк пусты | секции пустые, но **отличимые** от «нет доступа»; блок организации заполнен | — |
| **M-003** | нет права на банк | участник без `mpk.bank.manage` | признак «доступ закрыт»; реквизитов нет **в сетевом ответе**, не только скрыты в UI | — |
| **M-004** | чужая организация | `p_organization_id` организации, где вызывающий не состоит | отказ, неотличимый от несуществующей организации (`FR-003`) | типизированная ошибка доступа |
| **M-005** | нет сессии | вызов без аутентификации | отказ «требуется вход» | типизированная ошибка аутентификации |
| **M-006** | БИН на проверке | висит pending-правка `bin_iin` | `bin_iin` = **прод-значение**; правка приходит отдельно с прежним/предложенным значением, актором и временем | — |
| **M-007** | несколько версий банка | 2+ версии реквизитов | актуальная помечена, предыдущие приходят историей по убыванию версии (`FR-006`) | — |
| **M-008** | админ TURAN | `fn_is_admin()` | доступ к запрошенной организации, включая банк | — |
| **M-009** | организация не МПК | `org_types` без `mpk` | тот же payload с пустыми МПК-секциями, **не** отказ: 403 по типу обещал бы проверку, которой нет | — |
| **M-010** | организация не названа | `p_organization_id = null` | тот же отказ, что `M-004`: вкладка всегда про **одну** организацию, «все мои» этот читатель не отдаёт | типизированная ошибка доступа |
| **M-011** | внутренняя ошибка чтения | сбой на стороне БД | наружу код и человекочитаемая причина; текст SQL-исключения наружу не уходит (`FR-008`) | потребитель показывает честный текст + retry |
| **M-012** | тяжёлая организация | 20 версий банка + 3 правки на проверке + заполненная площадка | ответ укладывается в p95 < 200 мс на сид-данных среды приёмки; условия замера названы в пакете G3 (`FR-014`) | — |

**Не наблюдаемо снаружи — строк матрицы намеренно нет:** `FR-005` (поле версии контракта —
проверяется контракт-снапшотом `cross_check` CHECK 11), `FR-007` (аддитивность — проверяется
диффом и `prod_diff`), `FR-009`…`FR-013` (границы скоупа: доказываются отсутствием кода, а не
поведением). Отметка стоит явно, чтобы «нет строки» не читалось как «забыли».

</frozen-after-approval>

---

## Design contract (сигнатуры, не тела)

- **Data model.** Новых сущностей и полей нет (`FR-007`). Читаются: `organizations` (+ `regions`
  по `region_id` за именем), `mpk_profiles`, `mpk_sites` (активная первичная), `org_bank_accounts`
  (версии), `org_field_reviews`. Авторитет каждого факта остаётся у домен-владельца
  (`D-MPK-CANON-05`); слайс не пишет ничего, история уже лежит в append-only таблицах (`P12`).
- **API / RPC.** `rpc_get_org_profile(p_organization_id uuid) returns jsonb` —
  `security definer` · `stable` · `set search_path = public, pg_temp`;
  `revoke execute … from public, anon` + `grant execute … to authenticated, service_role`
  (образец — `rpc_get_org_membership_verification`, ARS-361). Регистрация имени в
  `rpc_name_registry` (`D-NEW-A`) + строка в `Docs/AGOS-Dok3-RPC-Catalog-v1_5.md`.
  Ключи ответа (контракт): `contract_version` · `organization` · `profile` · `primary_site` ·
  `bank {access, current, history[]}` · `field_reviews {pending[], resolved_recent[],
  resolved_total}` · `permissions {mpk.profile.edit, mpk.bank.manage}`.
  Ошибки: `AUTH_REQUIRED` (`M-005`) и один отказ доступа на `M-004` — оба `42501`, как у
  писателей ARS-359.
- **Events.** Не эмитит и не потребляет (`FR-013`).
- **UI contract.** Потребитель — `SCR-P2` §4.1 (`ARS-624`), персона — снабженец. Состояния:
  пусто (`M-002`) · банк без доступа (`M-003`) показывается блоком с пояснением «реквизиты
  доступны бухгалтеру и администратору», не скрывается · ошибка — честный текст + retry,
  техническая деталь в `console.error` (`IDENTITY-14`).
- **Файлы.** Тело + грант + registry — в канонический `d01_kernel.sql` (домен профиля живёт там);
  поверх — миграция `supabase/migrations/<ts>_ars_362_org_profile_read.sql`
  (порядок деплоя: d-файлы → `supabase/migrations/`). Отдельных patch-файлов не заводить.

## Slices → Tasks

| задача | тир | закрывает id | приёмка (словами снабженца) |
|---|---|---|---|
| **ARS-362** MP-2.1 | **semantic** | FR-001…FR-014 · M-001…M-012 | «Открываю "Предприятие" — вижу свои реквизиты, площадку, состояние банковских данных и что сейчас на проверке у TURAN; чего мне не положено — сказано прямо, а не спрятано» |

Тир `semantic`, а не `mechanical`: новый read-контракт с межорганизационной изоляцией и
правовой границей (`FR-003`/`FR-004`) — форма ответа проектируется с человеком, не автономно.

## Verification (G3 inputs)

- `cross_check.sh` (вкл. CHECK 11 — контракт-снапшот RPC): наличие функции, гранты, запись в
  `rpc_name_registry`.
- **DB-контракт-тесты на живых ролях** (не через UI) — `M-003` · `M-004` · `M-005` · `M-008` ·
  `M-010` · `M-011`: проверяется **payload**, а не отрисовка. Это же требование `FR-004`.
- `M-001` · `M-002` · `M-006` · `M-007` · `M-009` — сценарии `qa/scenarios` домена через
  `/qa-run` на сид-данных среды приёмки; `M-012` там же — замер p95 (`FR-014`).
  **Пакет G3 обязан назвать условия замера** (среда, сид, число прогонов): изолированной среды
  нет (`IMPL_DEBT` QA-ENV-ISOLATION-01), поэтому без названных условий цифра не сравнима между
  прогонами. Кейсы читающие — мусора в живой БД не оставляют, в отличие от мутирующих
  `06-tsp-mpk`.
- `prod_diff.py` после деплоя — merge ≠ деплой.
- converge intent↔reality по этой спеке и по `AGOS-Dok6-Slice10-MPK-Profile.md`.
- **Preview-доказательства не будет:** слайс без UI. Доказательство — сетевой payload и вывод
  контракт-тестов; скриншот здесь ничего не подтверждает.

## Open Questions

**Живых записей нет.** Три вопроса гейта закрыты владельцем 2026-09-04 (см. `Clarifications`),
ответы записаны как `FR-004`, `FR-009`, `FR-010`.

## Clarifications  (append-only)

- [2026-09-04] Q: банковские реквизиты в ответе — любому участнику организации (буква §6.1
  экранной спеки) или только по праву? → A: **только `mpk.bank.manage` или админ** (`FR-004`),
  следуем задеплоенному `ARS-359`; строка §6.1 исправлена как ложное утверждение о реальности,
  замороженный блок экранной спеки не тронут · отвергнуто: буквальное чтение §6.1 — развернуло бы
  осознанный least-privilege мимоходом, в read-RPC, без ADR; маска для всех (банк + последние 4
  знака IBAN) — ближе к духу «видит, но менять не может», но вводит третье состояние
  отображения, которого нет в матрице экранной спеки.
- [2026-09-04] Q: ARS-362 держала два RPC — брать ли в слайс агрегат «Обзора»? → A: **расщепить**,
  агрегат вынесен в **ARS-646** вместе со связями `blocks ARS-628` и сводкой допуска для
  `ARS-629`; в Scope ARS-362 оставлен указатель, куда делась половина · отвергнуто: оставить
  ARS-362 частично открытой — задача перестаёт быть join'ом «замысел↔работа», ровно так уже
  произошло ложное закрытие `ARS-624`; строить оба RPC — заход перестаёт быть тем, что заказан,
  и тянет за собой d02/d12/d13.
- [2026-09-04] Q: `FR-007` экранной спеки требует ОДНУ read-model на превью и фермерскую выдачу.
  Как не создать второй дом факта, пока карточка ждёт решения 07.09? → A: **плоский self-read**,
  а дом факта именуется **документом сейчас** (`FR-010` + `Assumptions` + `IMPL_DEBT` +
  строка в `ARS-624`) · отвергнуто: писать общую проекцию `fn_org_card_projection` сегодня — у неё
  ровно один вызывающий, а пишется она ради второго, которого нет (`HS-4`), и её содержимое и
  есть ответ Аршидина по раскрытию (ст. 171 ПК РК) — то самое решение, ради которого MP-3.3 и
  остановлена; ждать 07.09 целиком — MP-3.3 стоит ещё неделю без нужды.

## Assumptions  (append-only, ВНЕ frozen)

- **[2026-09-04]** Плоский self-read вместо общей проекции — *почему так:* решение владельца
  (выше); `P7` защищает сигнатуру и **форму ответа**, а не тело, поэтому перевод на проекцию
  позже будет обычным рефакторингом без смены контракта · *что опровергнет:* решение 07.09, из
  которого следует другая **граница ответа** (а не другое тело) — тогда контракт правится до
  деплоя.
- **[2026-09-04]** `resolved_recent` ограничен 20 последними + счётчик `resolved_total`;
  `pending` по конструкции ≤ 3 (не более одной правки на поле) — *почему так:* DoD требует
  ограниченного payload'а · *что опровергнет:* экран потребует полной истории правок с
  пагинацией.
- **[2026-09-04]** Регион отдаётся как `region_id` + имя из `regions.name_ru` — *почему так:*
  экран показывает регион текстом, второй запрос за справочником нарушил бы `FR-001`;
  денормализация **на чтении** второго дома факта не заводит · *что опровергнет:* мультиязычный
  интерфейс, где имя региона выбирается по локали вызывающего.
- **[2026-09-04]** Замер `FR-014`/`M-012` привязан к сид-данным **среды приёмки**, а не к
  staging-фикстурам — *почему так:* изолированной среды не существует, Supabase-проект один и
  `DATABASE_URL` не задан (`IMPL_DEBT` QA-ENV-ISOLATION-01); требование, привязанное к
  несуществующей среде, на G3 кончается либо waive, либо замером по боевой вопреки собственной
  формулировке · *что опровергнет:* появится изолированная среда — тогда замер переезжает туда,
  а формулировка `FR-014` уточняется под неё.
- **[2026-09-04]** `p_organization_id = null` даёт отказ, а не «все мои организации» (в отличие от
  `rpc_get_my_context`, где null = все) — *почему так:* вкладка «Предприятие» всегда про одну
  организацию, а молчаливый веер по всем орг-контекстам — лишняя поверхность утечки при
  `SECURITY DEFINER` · *что опровергнет:* появится экран-переключатель организаций, которому
  нужен список профилей одним вызовом.
- **[2026-09-04]** `rpc_get_my_context` остаётся источником для мобильного шелла, консоль профиля
  читает `rpc_get_org_profile` — *почему так:* это два **читателя** одного дома
  (`organizations`), а не два дома · *что опровергнет:* видимое расхождение одного и того же поля
  между шеллом и консолью.

## Implementation Notes  (append-only)

_(пусто — код не начат; каждая будущая запись называет `FR-`/`M-` id, который двигает)_

## Spec Change Log  (append-only)

_(пусто)_

## Review Triage Log  (append-only)

_(пусто — ревью якоря 7 идёт в `/build`)_
